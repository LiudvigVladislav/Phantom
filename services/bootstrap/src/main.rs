/// PHANTOM Bootstrap — minimal network entry.
///
/// Provides: relay node addresses, transport config hints, version compatibility.
/// Does NOT store user messages or keys.
///
/// Skeleton — implementation pending (Alpha-0 task #15 prerequisite).
fn main() {
    println!("phantom-bootstrap skeleton — implementation pending");
}
