/// PHANTOM Directory — username lookup service.
///
/// Alpha-0 scope (ADR-005): username → public identity bundle only.
/// No phone discovery, no DHT. Privacy-preserving contact discovery is post-Alpha-0.
///
/// Skeleton — implementation pending.
fn main() {
    println!("phantom-directory skeleton — implementation pending");
}
