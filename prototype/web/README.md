# prototype/web

Next.js visual prototype — dark-first PHANTOM UI. Runs independently of backend/crypto.
Screens: chat list, onboarding, chat, profile, settings, nearby, connect (QR/invite).

```bash
npm install
npm run dev   # → http://localhost:3000
```
