# shared/core/messaging

Send/receive pipeline, message state machine, retry logic, ack model, ephemeral message scheduling,
attachment manifest, conversation engine. Uses transport abstraction — never calls relay directly.
