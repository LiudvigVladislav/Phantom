# PHANTOM — Master Timeline 2026

> **Living document.** Источник истины для трекинга всех треков работы. Обновляется по мере merge каждого PR — чекбоксы превращаются в `[x]`, в "Сделано" секцию добавляется коммит.

**Last updated:** 2026-06-21 — **✅ R3.6 sticky-per-route Fast REST degradation MERGED (master `e601cf3b`, PR #328) — Tele2 LTE failover + Wi-Fi recovery field PASS.** Layered on top of the bare 3.6 Fast REST fast-path (`d3935300`). Adds an ordered `WsSessionLifecycleEvent` channel (`Channel.UNLIMITED` + `receiveAsFlow` + checked `trySend`), a supervised single-collector dispatcher extracted as `internal class WsSessionLifecycleDispatcher` so unit tests drive the production code path with mock callbacks, and a sticky-recovery state machine (`StickyRecoveryState { None, PendingNewSession, InFlight }` + `lastObservedEpoch` monotonic floor + 60-second `ws_alive_60s` probation chokepoint cleanup hook inside `transitionToWsActive`). Field-validated 2026-06-21 01:46-01:54 on Tecno BF7-12 over Tele2 LTE → Wi-Fi route change: Mode 2 detected at 31.038 s from WS connect, `sticky_armed gen=1` 1 ms later, 10 Mode-2-killed Tele2 WS sessions (`gen=1 s=2..s=11`) during the sticky window with ZERO `candidate_session_regression` and ZERO `mode_switched reason=ws_frame_text_received` transitions, two control envelopes delivered via REST (`9e83b798` via `inbound_repair_ok promotion=true` 466 ms ingress→decrypt, `a0d26aaf` via plain `DECRYPT_TRACE ok` 142 ms ingress→decrypt). Wi-Fi recovery exact 60.015-second probation: `sticky_recovery_pending` → new WS `gen=2 s=13` → `sticky_recovery_started ws_epoch=13` → `sticky_cleared proof=ws_alive_60s total_suppressed=0 elapsed_ms_since_arm=391301`, ordering invariant satisfied. `wifi-A` delivered via `ws_deliver_in` on the recovered Direct, `inbound_repair_ok promotion=true` 267 ms later. ZERO `LIFECYCLE_CONSUMER_ERROR`, ZERO `fail_mac action=hold`, ZERO `OpkNotFound`. NOT_EXERCISED suppression branch (`total_suppressed=0` — Tele2 dead sessions died silently without delivering a `Frame.Text`); unit coverage holds (`WsLifecycleDispatchBehaviourTest` 8 behavioural tests against the production dispatcher + `RestStateMachineTest.cleanup_completes_before_onModeSwitched_so_throw_does_not_leak_sticky` behavioural proof). Side-finding (NOT part of R3.6 contract): the recovered Direct died again on Wi-Fi via a 106 s / 5-pong ping timeout (a separate WS-stability signature, NOT Mode 2 — feeds the next reconnect-quiescence / Direct-stability track). Release flags `MODE_2_FAST_PATH_ENABLED` and `MODE_2_STICKY_ENABLED` pinned to literal `"0"` in the release block; debug opt-in via `localOrEnv("fastRestMode2"/"modeSticky", ...)`; build-time invariant `require(!(mode2StickyEnabled && !mode2FastPathEnabled))` in `RestStateMachine.init`. Previous Last-updated (Stage 2B-B scope-doc merged) preserved below:

**Previous (Stage 2B-B scope-doc merged):** ✅ Trek 2 Stage 2B-B scope-doc merged (master `94ba8d7a`, PR #308) — semantic layer of long-poll backbone scope locked, implementation PR next. Ten binding client locks L1-L10 codifying the preflight synthesis D1-D15 + OQ-1..OQ-6 LOCKS + OQ-7 REJECTED (NO new decisions invented). L1 constant-time MAC compare / L2 verify-key state machine `KeyAbsent`/`KeyPresent(hex)`/`KeySuspended` with explicit 12-cell exhaustive transition matrix via TWO nested `when` blocks over sealed types (Pair-based `when` would NOT be exhaustive), downgrade protection, outcome classifier at publication site + forced-refresh-failure corollary surfaced as a single explicit sentence / L3 cursor advance strictly after `AckOutcome.Acked` via single combined `ackInboundAndAdvanceCursor(envelopeId): AckOutcome` orchestrator method (no split-window leak) with three-phase pattern (cancellable Phase 1 mutex acquire + cancellable Phase 2 retry loop + `finally`-scoped narrow `withContext(NonCancellable) { ... }` Phase 3 cleanup) + bounded local cursor-write retry 3 attempts backoffs `[100, 500]` ms BETWEEN attempts only + explicit `try/catch (CancellationException) { throw }` instead of `runCatching` + explicit `wasCancelled` flag distinguishes shutdown from structural exhaustion + debug `FAILED_MAC` hold path preserves relay copy / L4 `LongPollCursorRepository` read/write seam + legacy `pollLoop` in-memory cursor decommissioned / L5 `SeqMacVerifier` canonical encoding (`ushr` BE, `encodeToByteArray().size`, hex-decode key, receiving identity) / L6 both REST poll loops symmetric verify semantics + `_inboundStateMutex` serialises all shared in-memory state INCLUDING verify-key state machine, lock order `tokenMutex` outer / `_inboundStateMutex` inner with verify-key publication INSIDE `acquireOrRefreshToken`'s existing critical section (no re-entrant wrap) / L7 bad-MAC posture: telemetry + one latched forced refresh + both loops suspended on poison; no ack, no delete, no cursor advance EVER / L8 410 reauth capped exponential 5s floor 60s ceiling + no hard local logout + hard timer caps (breaker 120s / 410 reauth 60s / HTTP 429 `Retry-After` 120s clamp) / L9 `LongPollBreakerState` separate from `RestStateMachine`, `HalfOpen(probeInFlight)` with atomic permit + cancellation-safe `finally` release (prevents double-probe AND prevents permanent stuck-permit on claimant cancellation), breaker open does NOT alter MAC verify, eight numeric constants locked + `BreakerOpenReason` taxonomy / L10 `Csprng` jitter migration on all 11 sites. Ten server-contract points pinned at master `f8cdc91a` (seven Stage 2B-A points + three Stage 1.x additions). 33 M-cells total (M8-M17 + M-B7-M-B29) including round-5/6/7 additions M-B25 refresh-vs-poll race + M-B26 sparse-sequence crash recovery + M-B27 cursor-write bounded retry + M-B28 half-open probe singleton + M-B29 combined `ackInboundAndAdvanceCursor` cancellation at four precise injection points (Phase 1 mutex acquire / third-attempt mid-upsert / between-retries mid-delay / structural-no-suspension-point verification). Six commits C1-C6, each individually green and individually reviewable. Voice rule clean. Tele2 LTE smoke gate WORKING_RULES rule 8 binding for Stage 2B-B and NOT waivable; six scenarios S1-S6 (S6 promoted from optional under OQ-5 LOCK) ≥30 min on Tecno + Tele2 LTE; controllable trigger acceptable for S6 if natural Mode-2 does not reproduce. Seven review rounds closed sequential subtle concurrency/cancellation/state-machine edge cases — final APPROVE on head `1f12f5b6`. Previous Last-updated (Trek 2 Stage 2B-A scope-doc shipped) preserved below:

**Previous (Stage 2B-A SHIPPED):** ✅ Trek 2 Stage 2B-A scope-doc merged (master `9c02a3c8`, PR #304) — client long-poll shell scope locked, implementation PR next. Seven binding client locks L1-L7: LP+PP headers gated on `LONGPOLL_V2_ENABLED == "1"` (both together, no LP-alone / PP-alone) / read-timeout raised iff flag-on AND `pollHoldSecs in 1..480` / `wsActivePollJob` PARALLEL with WS (lifecycle on transport-session owner, NOT WS-down fallback, MUST NOT replace Direct WSS fast path) / `since_seq` read-only / `seq_mac` presence-parse only, NO verify, MUST NOT advance cursor on unverified MAC / release BuildConfig pin `"0"` + M4 contract test / out-of-scope MAC-verify+breaker+reauth-on-410+voice-media-padding+Tor-Ghost. 3-commit boundary B1 headers+flag / B2 timeout-gate / B3 parallel-job+DTO. 7 contract tests M1-M7. 7 server-contract points (verified against `e3794e6e` at scope time) MUST be re-verified at implementation start; pause if drift. WORKING_RULES rule 8 carve-out OK (shell-only, release pin); Tele2 LTE smoke gate transfers to Stage 2B-B. Strategic frame: backbone first → Direct WSS hardening → Reality Android repair → Tor/Ghost polish. The backbone is what makes the messenger un-killable at the product level even when WS/Reality misbehave; Stage 2B-A is the first half of installing it on the client. Previous Last-updated (Trek 2 Stage 1.x SHIPPED) preserved below:

**Previous (Trek 2 Stage 1.x SHIPPED):** ✅ Trek 2 Stage 1.x server prerequisite SHIPPED (master `e3794e6e`, PR #303). HMAC-SHA-256 `seq_mac` integrity tag store-time-computed over `(identity_hex, seq, envelope_id, sequence_ts_post_quantize)` with per-identity verify-key derived from a relay-side root key that never leaves the relay process; atomic `RestTokenStore::issue()` (structural single critical section across `by_token` + `by_identity`); post-rotation challenge replay returns `410 Gone` with body `"session token rotated; obtain a fresh challenge"`; `X-Phantom-Long-Poll` and `X-Phantom-Padded-Poll` headers fully decoupled (`padded_opt_in = LP || PP`); `MAX_POLL_HOLD_SECS_CAP = 480` dual-layer clamp + `PER_IDENTITY_HOLD_CAP = 3` HoldSlot CAS-bounded; 162 relay tests pass, `cargo clippy -p phantom-relay --tests -- -D warnings` clean. Scope-doc trio #300/#301/#302 landed before implementation; review round 2 (4 blockers, all closed in single bundled commit) + Round 3 cleanup (410 Gone test tightened, stale comment fixed, 5 clippy nits cleared). Operator must provision `RELAY_SEQ_MAC_KEY` (64-hex from `openssl rand -hex 32`) in VPS `.env` before redeploying; relay fail-to-start without the key. Trek 1 Stage 1 V3 xhttp CLOSED FAIL same day (commit `df082528`, brief `docs/tracks/rc-libxray-reality-wire1.md`): app-level matrix V1/V2/V3 exhausted, byte-identical 1440-byte Reality ClientHello stall localised to libXray Android gomobile write path, Reality demoted to diagnostic surface; sub-track RC-LIBXRAY-REALITY-WIRE1 scoped but not started. Previous Last-updated (Stage 2A SHIPPED) preserved below:

**Previous (Stage 2A SHIPPED):** **✅ Trek 2 Stage 2A SHIPPED (master `30dc16f5`, PR #298) — client-side long-poll foundations only, ZERO runtime behaviour change with `LONGPOLL_V2_ENABLED == "0"` release pin.** 11 commits (A1..A8 base + 3 Round 2 P2/P3 review fixes), ~1206/-6 LOC across 23 files. Foundations delivered: `pollHoldSecs` additive in `AuthSessionResponse` + `RelayCapabilities` projection (A1); SQLDelight `transport_seq_state` v19→v20 migration (A2); `LastSeenSeqRepository` interface + `SqlDelight*` + `Fake*` impls (A3); `socksProxyPort: Int?` skeleton through factory + Android OkHttp wiring + orchestrator (A4); `EnvelopeId` `@JvmInline value class` boundary wrapper with `TODO(stage3-migration)` markers at every `sendEnvelope` call site (A5); `LONGPOLL_V2_ENABLED` BuildConfig — debug `"1"` / release `"0"` — + AppContainer wiring + orchestrator gate (A6); `Csprng` interface + `LibsodiumCsprng` object backed by `LibsodiumRandom.buf` with rejection sampling for `uniformLong` (A7 + R2-1); 28 contract tests across 4 commonTest files including direct `EnvelopeId.random()` cases added in Round 2 (A8 + R2-2). Round 2 review surface: commit-by-commit verdict on the A1..A8 head — no blockers, two non-blocking findings (P2 missing direct `random()` test + P3 modulo bias). Both addressed before merge: R2-1 `c1dc537c` replaced modulo with rejection sampling (provably zero bias, expected one libsodium draw at Stage 2B jitter bounds); R2-2 `adf88c4e` added 2 direct `EnvelopeId.random()` tests + `libs.libsodium.bindings` in `transport.commonTest.dependencies` (production dependency surface unchanged); R2-3 `d7f064a7` corrected worst-case KDoc — previous claim "`Long.MAX_VALUE` re-draws ~50%" was wrong, actual ~50% worst case sits at `boundExclusive ≈ Long.MAX_VALUE/2 + 1` where floor division collapses from 2 to 1 (code path unchanged). Stage 2A guardrail held: no `X-Phantom-Long-Poll: 1` header set, no socket timeout raised on opt-in path, no `wsActivePollJob` spawned, no jittered hold consumption, no `lastSeenSeq` runtime read/write, no `poll_circuit_open` breaker, no `LONGPOLL_REAUTH_INTERVAL`, no `RestStateMachine.kt:31-32` docstring update, no voice-chunk outer padding (deferred entirely to Stage 3 per OQ2 amendment). Verify on master: `:shared:core:transport:jvmTest` / `:shared:core:crypto:jvmTest` / `:shared:core:storage:jvmTest` / `:apps:android:assembleDebug` all clean; CI on `d7f064a7` green (Android `build-and-test`, Relay `build-test`, Relay `lint`, Deploy `lint`). Operator policy: do NOT flip `RELAY_POLL_HOLD_SECS > 0` on production until Stage 2B client work is widely rolled out — Stage 1's kill switch on master remains the production guard. Next: **Stage 2B** = runtime layer (opt-in header B1 + raised timeout B2 + parallel `wsActivePollJob` B3 + jitter consumption via Csprng B4 + `lastSeenSeq` read/write via repo B5 + `poll_circuit_open` breaker B6 + `LONGPOLL_REAUTH_INTERVAL` default 15 min B7 + `RestStateMachine.kt:31-32` docstring B8 + field-test matrix + operator runbook B9). Two-layer preflight REQUIRED (HIGH-risk runtime). Voice padding remains Stage 3. Previous Last-updated (§14 Arm G mini-lock PR-G1 docs-only) preserved below as section state pointer:

**Previous (§14 Arm G mini-lock PR-G1):** 🟡 §14 Arm G mini-lock landed (PR-G1 docs-only) → Reality-tunneled WS heartbeat diagnostic locked. 7 Council decisions + 4 code-state notes verified against master `d0f41fbe`: clean TRANSPORT isolation, NOT structural bootstrap isolation (short-circuit before production `transport.connect(...)` — but `initMessagingFromStorage()` + `startReceiving()` already ran at `PhantomMessagingService.kt:344-393`, so bootstrap REST may still fire; PR-G3 grep-verifies absence or annotates), reuse production `xrayService` singleton from debug-only Arm G harness (safe at master `d0f41fbe` because `xrayServiceProvider` is a lambda materialised only on production transport path), SOCKS port DYNAMIC `127.0.0.1:<Ready.socksPort>` NOT hardcoded `10808` (cross-device V2RayNG collision risk per `OperatorXrayConfig.kt:43-49`), `withTimeout(15_000L) { state.first { it is Ready } }` wait condition with `outcome=xray_not_ready` fail-fast, echo round-trips REQUIRED for PASS (lifetime alone insufficient — Arm A.2 already showed Mode 2 with 0 echoes), 15-min field run on Tecno+Tele2 LTE, split PR structure G1/G2/G3, narrow rule 8 carve-out (debug-only + loopback SOCKS + no system proxy + no remote bind + no production preferences). Hard gates: `BuildConfig.DEBUG && DEBUG_RC_DIRECT_ARM_G_VIA_REALITY == "1"` strict parse + production stack untouched + xray logs redacted (no UUID/shortId/publicKey/token) + zero server-side change (reuses Stage 5E `:8443`) + `Inv-ParallelArmIsolation` + teardown calls both `rcDirectArmG?.stop()` AND `xrayService.stop()`. Discriminator three outcomes: PASS (lifetime ≥ 10 min + echo + no Mode 2) → Reality-primary realtime + 3.2b.1 safety net (~3-4 нед); PARTIAL (lifetime ≥ 10 min + echo fails) → REST + Matrix-style long-poll primary + Reality as REST-fallback (~6-8 нед); FAIL (Mode 2 persists OR byte-budget class persists) → pure REST + Matrix-style 25-sec long-poll Option A, abandon Direct WS (~6-8 нед). Next: PR-G2 Android code (`RcDirectArmG.kt` near-clone of `RcDirectArmA2`/`RcDirectArmD` + new `DEBUG_RC_DIRECT_ARM_G_VIA_REALITY` BuildConfig + AppContainer lazy + Service short-circuit between Arm D and production fall-through + `xrayService.stop()` in onDestroy). Time-box: 2026-06-12 hard deadline. xray REALITY production on `:8443` untouched.** Previous Last-updated (§13 T2 Outcome) preserved below as section state pointer:

**Previous (§13 T2 Outcome locked):** 🟡 §13 T2 Outcome locked → byte-threshold class directionally confirmed on Tele2 LTE Иркутская; relay-side `total_received=5120` of 40960 (12.5%) vs Android `T2_SLOW_POST_chunk_sent total_sent=40960` (all 8 chunks queue-accepted via per-chunk `sink.flush()`), then Android `T2_SLOW_POST_failed t=SocketTimeoutException elapsed_ms=130377`, relay `event=slow_post_aborted reason="read_error" elapsed_ms=163251`. The gap between client `total_sent=40960` and relay `total_received=5120` IS the discriminator. Observed upload-direction abort point SIGNIFICANTLY EARLIER than the documented `net4people/bbs #490` 14-32 KB range. Concurrent short-POST request bodies REACHED relay and were processed in the same 163-second window (3× `prekey_publish` server-side + 1× `rest_session_issued`); client-side response reception NOT uniform — `/auth/session` returned 200 to client, `/prekeys/publish` attempts 1 & 2 hit `prekey_publish_retry reason=SocketTimeoutException` (~60s readTimeout). Honest wording: external 14-32 KB class hypothesis directionally confirmed; magnitude does NOT match — observed upload cutoff ~5 KB, well below documented range; PREKEY response-direction failures add second strengthening signal that byte/duration-budget class extends to response path on long-held connections. Isolation caveat: T2 was concurrent with production bootstrap traffic on adjacent TCP connections. Architectural implication: bare Direct uplink demoted as primary realtime path on RU mobile — this is NOT a WS-specific finding but a long-connection-uplink finding subsuming Arm A.2 and Arm D verdicts. Next: Arm G WS-over-Reality through embedded libXray SOCKS5 → `:8443` Stage 5E Reality endpoint → relay. Hard time-box 1 week from §4 Arm A.2 Outcome PR landing (2026-06-05).** 5-step plan progress: 1✅ Arm A.2 outcome / 2✅ VPS tear-down / 3✅ T2 diagnostic (code #292) + this Outcome PR / 4⚡ pending Arm G / 5 pending final outcome. Master = this PR's squash commit on top of `a58ec03f` (PR #292 T2 diagnostic code).

Shipped:
- **#265 `99cb1d6f`** — RC-DIRECT-WS-DEATH1 mini-lock rev4 (Phase 1 plan).
- **#266 `08514aee`** — Phase 1 Arm B diagnostic code.
- **#269 `16ee99b9`** — Phase 1 outcome — evidence summary §13-§18 + durable log bundle.
- **#270 `ca620fe6`** — Phase 2 mini-lock rev1 (PCAPdroid wire-correlation plan §19-§30, two-tier evidence model).
- **#271 `358e063e`** — Phase 2 marker emit (`PHASE2_CAPTURE_MARKER` logcat line + `DEBUG_PHASE2_MODE` BuildConfig field, debug-gated, zero transport touch).
- **#272 `a4d3b45b`** — Phase 2 outcome — evidence summary §31-§39 + durable log bundle. PCAPdroid v12 capture set (6 sessions × 3 artifacts), tshark spot-checks confirmed (#1 + #2) and refined (#3 TCP-layer ambiguous). 3.2b.1 unfreezes; Mode 1 closed as return-path loss; Mode 2 closed as unstable TCP/TLS path with mixed sub-cases.
- **#273 `6c923c39`** — PR-1 of three locked PRs for opening RC-DIRECT-STABILITY1: CI loopback-only-ports gate (`.github/workflows/deploy-lint.yml`). Pre-merge enforcement of `Inv-BypassIsLoopbackOnly` before any Arm A experiment can add a temporary port binding on the production VPS. Python + PyYAML structural parse, short-circuits on irrelevant PRs, fail-closed on missing `127.0.0.1:` prefix or long-form dict syntax with missing `host_ip`.
- **#274 `3458c136`** — RC-DIRECT-STABILITY1 mini-lock §1-§12 + ADR-028 Direct Stability Architecture Intent + durable log bundle.
- **#275 `179327bf`** — Arm A PR-3a: loopback-only compose delta (`127.0.0.1:8081:8080`) + field playbook two-command bridge (`ssh -N -L` + `adb reverse`). VPS-deployed + 3 reachability checks passed.
- **#276 `a6d8bea9`** — Arm A PR-3b: `RcDirectArmA` Caddy-bypass diagnostic class + `DEBUG_BYPASS_URL` double-gate + AppContainer wire-up + Service short-circuit + lifecycle shield fixup.
- **#277 `bbc575a8`** — Arm C PR-4: OkHttp ping interval matrix diagnostic + Arm A outcome record (PARTIAL/PASS for loopback path) + Arm A.2 parking-lot fallback + baseline-semantics clarification.
- **#278 `c0022760`** — PR-5 docs bundle: Arm C outcome record (H-C as a production fix lever refuted; cadence is detection timing, not fix lever; Mode 2 carrier signature persists) + Arm D scope refine (payload `phantom:diagnostic:heartbeat-echo:v1:<seq>:<client_ms>`, default-off env flag, architect pre-review design locks for PR-6 absorbed) + new F-Mode2-cadence-invariant fact in §1.
- **#279 `5ef270b2`** — Arm D PR-6: relay heartbeat-echo handler in `services/relay/src/routes.rs` behind `RELAY_ENABLE_HEARTBEAT_ECHO=1` env flag (default off, strict `v == "1"` parse). Sole-construction-site helper `build_heartbeat_echo_response` returning `Message::Text` only, locked by `echo_response_uses_text_opcode_never_pong` unit test per Inv-DataFrameNotControlFrame. Per-frame `event=heartbeat_echo_received` / `_sent` / `_rejected` logs plus `echo_frames_received` / `echo_frames_sent` added to existing `session_summary` line.
- **#280 `756c0d81`** — Arm D PR-7: `RcDirectArmD.kt` Android diagnostic class (near-clone of `RcDirectArmC`) with heartbeat sender coroutine gated by `openedAt: CompletableDeferred<Long>` completed in `onOpen` so the first heartbeat fires after the handshake completes, not after `newWebSocket` returns. `CancellationException` rethrown before generic `Throwable` catch. `DEBUG_RC_DIRECT_HEARTBEAT_ECHO` double-gate + AppContainer wire-up + PhantomMessagingService short-circuit (precedence A → B → C → D → production).
- **#281 `a0353aba`** — Codeberg infrastructure fix v1: site footer Codeberg URL on `about.html` + `donate.html` swapped from `LiudvigVladislav` (GitHub username) to `VladislavLiudvig` (Codeberg username) + `mirror.yml` narrowed push scope from `refs/heads/*` to `refs/heads/master:refs/heads/master` + `refs/tags/*:refs/tags/*` so feature-branch divergence stops failing the workflow.
- **#282 `74312018`** — Codeberg infrastructure fix v2: same Codeberg URL correction extended to `site/index.html` (footer + JSON-LD `sameAs`) + `site/roadmap.html` (footer) + all four `site/.build/build_{about,donate,index,roadmap}.py` generators so the next site rebuild does not revert about/donate back to 404.
- **#283 `601d9d8d`** — Arm D outcome lock docs bundle: §4 Arm D Outcome subsection (client + relay counters, Mode 2 signature continuation, control/application asymmetry finding with conservative cause formulation across OkHttp / Caddy/TLS / carrier / interaction hypothesis set, first-session downlink Pong loss isolation to s=1, side-finding `session_summary` absence with two candidate explanations, §6 verdict FAIL, §5 next-step deviation to Arm A.2 ahead of Arm E) + MASTER_TIMELINE last-updated bump + PROJECT_LOG session entry. Zero code touch.
- **#284 `f7af95d8`** — Arm A.2 scope mini-lock docs bundle: §4 Arm A.2 active subsection promoted from §9 parking lot (stunnel primary candidate after structured four-lens trade-off review, HAProxy fallback, W/X/Y three-outcome discriminator with locked wording bounds, P2 hard gates for cert sourcing + relay proxy header dependency + time-box + no-production-promotion + security minimum) + §4 Arm A "deferred" bullet updated to point to new subsection + §5 decision tree D-FAIL row redirected to Arm A.2 + A.2 W/X/Y rows added + §7 implementation order rows 5c/5d/5e added with Arm E annotated as conditional on A.2 outcome + §9 parking lot Arm A.2 entry updated to mark promotion. Zero code touch.
- **#285 `b4fc4cd4`** — Arm A.2 PR-8a server-side overlay + stunnel config + operator runbook. Two new files in `deploy/` (`docker-compose.armA2.yml` overlay declaring `stunnel-arm-a2` service joining `phantom-internal` network with cert mount from `caddy-data:/data:ro`; `stunnel.armA2.conf` with `[relay-arm-a2]` block, TLS 1.2/1.3 hardening, strong cipher suites, `clients = 50` cap). Pre-code Gates 1 (relay proxy header dependency grep) + 2 (Caddy cert PEM format + path verification via SSH) both PASS — stunnel stays primary, HAProxy fallback not triggered. `docs/tracks/rc-direct-stability1.md` §4 Arm A.2 PR-8a implementation record subsection appended with Gate evidence + file inventory + 8-step operator runbook (enable heartbeat-echo flag / recreate relay / bring up stunnel / curl WS upgrade probe verify / capture window / tear down / revert flag / health check) + cert-rotation handling note + WORKING_RULES rule 8 server-side-only carve-out + rule 9 grep-verified-per-claim list. Zero application or relay code change. Host port locked at `:8443` at scope time — subsequently superseded by this PR's port fixup (host port collision with production `phantom-xray` discovered at deploy time).
- **#286 `79438806`** — Arm A.2 PR-8a port fixup: host port changed from `:8443` to `:8444` because `:8443` was held by production `phantom-xray` REALITY+WSS container (Stage 5E, 2026-05-07). Deploy-time finding from Path A operator runbook Step 3 execution 2026-06-05: `Bind for 0.0.0.0:8443 failed: port is already allocated`. INSIDE the stunnel container the listener stays `accept = 0.0.0.0:8443` (no change to `stunnel.armA2.conf`); only the Docker port-mapping in overlay changes from `8443:8443` to `8444:8443`. xray's `:8443` binding completely untouched. Forensic note + SSH evidence baked into §4 Arm A.2 PR-8a implementation record subsection. Diagnostic-design-lesson: future scope-lock PRs that declare a host port must SSH-verify free on target VPS before merge. W/X/Y semantics unchanged; only URL the device + curl probe target changes from `:8443/ws` to `:8444/ws`. Zero application or relay code change.
- **#287 `4b26b1a3`** — Arm A.2 PR-8a image fixup: `dweomer/stunnel` image replaced by minimal Alpine-built stunnel container. New file `deploy/stunnel-armA2/Dockerfile` (Alpine 3.20 digest-pinned `alpine:3.20@sha256:d9e853...` + `apk add stunnel` + raw stunnel `ENTRYPOINT`). Overlay `image: dweomer/stunnel@sha256:c46e11...` → `build: ./stunnel-armA2` + `image: phantom-stunnel-arm-a2:latest` for compose cache. Discovered when retried deploy after #286 landed succeeded at bind step but container exited 1 with `STUNNEL_SERVICE/ACCEPT/CONNECT` empty in logs — dweomer's entrypoint script generates config from env vars and ignores bind-mounted `/etc/stunnel/stunnel.conf`. Building from Alpine ourselves removes entrypoint-magic risk class entirely + reduces supply-chain trust to Alpine maintainers only. Also added `pid =` empty to `stunnel.armA2.conf` to coexist with `read_only` rootfs (prevents `/var/run/stunnel.pid` EROFS write attempt at startup). Diagnostic-design-lesson recurrence: future image-choice trade-off reviews must answer "does this image's entrypoint accept a bind-mounted config or generate one from env vars?" as a P1 question. Track doc §4 Arm A.2 PR-8a implementation record updated: new "Deploy-time finding — image switched from dweomer to Alpine-built" bullet; Files shipped section updated; "Image-refresh handling note" replaced by "Alpine-pin refresh handling note". W/X/Y semantics unchanged. Port mapping `8444:8443` unchanged from #286. xray REALITY production on `:8443` untouched. Zero application or relay code change.
- **#288 `e1b802c5`** — Arm A.2 PR-8a TLS-options cleanup: removed 4 `options = NO_*` lines from `deploy/stunnel.armA2.conf`. After PR #287 landed, retried `compose up -d stunnel-arm-a2` succeeded at the build step but the started container exited at config-parse with `/etc/stunnel/stunnel.conf:93: "options = NO_TLSv1.1": Illegal TLS option` (stunnel 5.72 + OpenSSL 3.3.7). Root cause: correct stunnel syntax is `NO_TLSv1_1` underscore, not `NO_TLSv1.1` dot — original PR-8a config shipped the dot form as a syntax bug masked by the dweomer entrypoint failure until the Alpine-built container reached the config-parse phase. Even the syntactically-correct NO_* flags are redundant with the `sslVersionMin = TLSv1.2` + `sslVersionMax = TLSv1.3` directives already present (and OpenSSL 3.x has removed several legacy option constants entirely — next NO_* line would likely be the next minefield). Option B (remove all 4 NO_* lines, rely on sslVersionMin/Max) chosen over Option A (surgical syntax fix). No security regression — publicly-negotiated minimum still TLS 1.2. Inline TLS-hardening comment expanded to explain why NO_* flag passthrough is intentionally not used so a future maintainer doesn't naively re-add. Third deploy-time finding bullet appended to §4 Arm A.2 PR-8a implementation record with cumulative-lesson note covering all three fixup PRs (#286 port + #287 image + this PR TLS options). Alpine-built image (#287), port mapping `8444:8443` (#286), and xray REALITY production on `:8443` all unchanged. Zero application or relay code change.
- **#289 `207752f9`** — Arm A.2 PR-8a `clients = 50` drop: removed `clients = 50` directive entirely from `deploy/stunnel.armA2.conf`. After PR #288 landed, retried `compose up -d stunnel-arm-a2` succeeded at the build step and TLS-option parse step but the container exited again at config-parse with `/etc/stunnel/stunnel.conf:115: "clients = 50": Specified option name is not valid here`. Root cause: `clients` is a global stunnel directive, not a per-service one — the original PR-8a config placed it inside the `[relay-arm-a2]` service section. Two options analysed: (A) relocate to global section above `[relay-arm-a2]`; (B) drop the directive entirely. Vladislav-formulated decision: chose (B) — four consecutive deploy-time surprises from stunnel-side config; less stunnel config = less risk of a fifth; the §4 Arm A.2 Security mini-lock clause "Connection cap at the stunnel level if available, **else at the relay level**" falls back cleanly; diagnostic-only safeguards (time-box + TLS-only + signed-challenge relay auth + `restart: "no"` + explicit teardown + non-production `:8444` port) are sufficient for the 15-min capture window; if edge-level connection limiting is needed later, implement it explicitly via HAProxy, nftables, or relay-side limits under a separate mini-lock. Inline config comment expanded to explain the fallback rationale so a future maintainer doesn't naively re-add. Fourth deploy-time finding bullet appended to §4 Arm A.2 PR-8a implementation record with cumulative-lesson note covering all four fixup PRs (#286 port + #287 image + #288 TLS options + this PR directive scope) + process improvement reference (pre-deploy stunnel config smoke-test against the target image before scope-lock). Files shipped section updated to remove `clients = 50` mentions. TLS-options cleanup (#288), Alpine-built image (#287), port mapping `8444:8443` (#286), and xray REALITY production on `:8443` all unchanged. Zero application or relay code change.
- **#290 `c49a5568`** — Arm A.2 PR-8b Android `RcDirectArmA2.kt` diagnostic class + `DEBUG_RC_DIRECT_ARM_A2_URL` BuildConfig field + AppContainer lazy wire-up + PhantomMessagingService short-circuit branch + teardown. Near-clone of `RcDirectArmD` (PR #280 squash `756c0d81`) with the only logical difference being the path: Arm A.2 targets `wss://relay.phntm.pro:8444/ws` (stunnel bypass) instead of production `wss://relay.phntm.pro/ws` (Caddy). Same heartbeat payload format (`phantom:diagnostic:heartbeat-echo:v1:<seq>:<client_ms>`), same lifecycle fixes from PR #276 (shield #1 + #2) and PR #280 (P1 openedAt.completeExceptionally on pre-onOpen failure + P2 CancellationException rethrow in heartbeat coroutine), same `SEND_TIME_MAP_CAPACITY = 32`, same `Inv-DataFrameNotControlFrame` (outbound WS Text only). Precedence per §7 step 5e: Arm A → Arm A.2 → Arm B → Arm C → Arm D → production. All Vladislav-locked review gates PASS (production `RELAY_URL` untouched, `RelayTransportFactory.kt:71` untouched, release-pinned `""`, no production path, single ws.send() with HEARTBEAT_ECHO_PREFIX constant, lifecycle clean, `./gradlew :apps:android:assembleDebug` BUILD SUCCESSFUL). Server-side bypass chain (#286 + #287 + #288 + #289) all stable. Zero production code change. Zero relay code change.
- **#291 `d2c22cd8`** — Arm A.2 outcome lock docs-only PR. Field test 2026-06-05 (Tecno Tele2 LTE, 15-min capture, PR-8b APK `phantom-arma2.apk` SHA `17fec82e...`, through stunnel `:8444` with `RELAY_ENABLE_HEARTBEAT_ECHO=1` on VPS): 21 ws_open / 20 ws_failure / 40 echo_sent / 0 echo_received; relay 20 ws_protocol_ping_received / 20 ws_protocol_pong_sent / 0 heartbeat_echo_received. Mode 2 carrier signature byte-perfect identical to Arm D (s=1: 30 s / 0 pong; s=2..s=20: ~45 s / 1 pong). Two different TLS stacks (Caddy Go TLS vs stunnel OpenSSL 3.3.7) + two different proxy paths → byte-identical death; proximal cause NOT the edge stack. Verdict per §4 Arm A.2 Discriminator: W refuted, X met, Y met — "Y with persistent control/application asymmetry continuation." Caddy strongly loses priority. Hypothesis-set EXTENDED from Arm D four-hypothesis to FIVE — added `net4people/bbs #490` cumulative-bytes-per-TCP-connection freeze (~14-32 KB) per Independent Audit input 2026-06-05 (explains linear lifetime ≈ 3× ping_interval, Ping-vs-Text byte budget asymmetry, per-connection reset). Wording bounds locked: does NOT claim Caddy innocent / carrier discriminates / TLS broken / WebSocket broken. Track does NOT close on this outcome — Vladislav-locked refined plan after four-architect external review (Gemini close+A / Claude-in-chat Arm G / Claude Independent Audit T2+Arm G with byte-threshold / working architect Arm G primary; ChatGPT docx off-track backlog) carries T2 byte-threshold + Arm G WS-over-Reality before final close, hard time-box 1 week. §5 X+Y rows fire concurrently (3.2b.1 escalates from "parked" to "needed"; Arm E deprecated; uplift realtime per ADR-028; Arm F mini-lock opens after final close). Files: §4 Arm A.2 Outcome subsection added (16 bullets) + memory entry `project_arm_a2_outcome_y_with_asymmetry_2026_06_05.md` + PROJECT_LOG entry + this MASTER_TIMELINE bump. Zero application or relay code change.
- **#292 `a58ec03f`** — §13 T2 slow-POST byte-threshold diagnostic. Discriminates hypothesis 5 (`net4people/bbs Issue #490` cumulative-bytes-per-TCP-connection freeze, 14-32 KB on RU mobile operators) from the other four hypotheses in the Arm A.2 outcome open set. Relay handler `slow_post_diag` at `POST /diag/slow-post`, gated by `RELAY_ENABLE_SLOW_POST_DIAG=1` strict `== "1"` parse, default off → route NOT registered → 404 (Vladislav addition B 2026-06-06). Mounted outside the standard 30-second `TimeoutLayer` so the T2 POST runs ~70-80 s (the layer's order in `router()` is: standard routes get the timeout layer, then T2 route is conditionally added on top). Streams body via `Body::into_data_stream()` with `futures_util::StreamExt`, logs `event=slow_post_chunk_received total_bytes=N elapsed_ms=T` per chunk + terminal `event=slow_post_completed` or `event=slow_post_aborted reason=...`. Validates `X-Phantom-Diag: slow-post-v1` AND `Content-Type: application/octet-stream` BEFORE reading body, caps body at 64 KB. Six relay integration tests passing (route off → 404, missing header → 400, wrong header value → 400, wrong content-type → 400, cap exceeded → 413, happy path 200 OK with correct `total_received` JSON). Android `T2SlowPostDiag.kt` is one-shot (not reconnect loop) — custom `RequestBody` streams 8 × 5120 bytes with `sink.flush()` after each chunk + `Thread.sleep(10_000)` between chunks; SEPARATE OkHttp profile from WS arms (Vladislav hard gate 1: connect=5s, write=30s, read=60s, callTimeout=180s). `BuildConfig.DEBUG_T2_SLOW_POST_URL` field (debug-only, release-pinned `""`). AppContainer lazy field + PhantomMessagingService short-circuit between Arm A.2 and Arm B (precedence A → A.2 → **T2** → B → C → D → production) + onDestroy teardown. Python preflight `scripts/t2-slow-post-preflight.py` writes raw HTTP/1.1 chunked framing with explicit `sock.sendall()` per chunk + `time.sleep(10)` between chunks (NOT `curl --limit-rate` because the latter smooths the throttle but doesn't guarantee discrete chunks with explicit per-chunk flush — Vladislav hard gate 3 + addition A). Track doc §13 mini-lock with all 5 hard gates + A/B/C/D additions documented inline. Three-outcome discriminator: relay receives 14-32 KB and aborts → byte-threshold strongly confirmed → Matrix-style 25-sec long-poll mandatory; relay receives all 40 960 bytes + 200 OK → byte-threshold refuted → Arm G next; > 32 KB but < 40 KB → same class, different threshold value. Wording bound: "refuted on HTTP POST through Caddy" does NOT mean "refuted globally" — WS-over-Caddy may hit a different mechanism. `./gradlew :apps:android:assembleDebug` BUILD SUCCESSFUL in 53s. Zero production code change. xray REALITY on `:8443` untouched.
- **#293 `d0f41fbe`** — §13 T2 Outcome lock docs-only PR. Field test 2026-06-05 (Tecno Tele2 LTE Иркутская, PR #292 debug APK `phantom-t2.apk` with `debugT2SlowPostUrl=https://relay.phntm.pro/diag/slow-post` through production Caddy after preflight PASS from wired-LAN+VPN dev machine, 163-second relay-side window 10:35:16 — 10:37:59 UTC): relay received only 5120 of 40960 bytes (12.5%); Android `total_sent=40960` (OkHttp queue-accepted all 8 chunks). Two `event=slow_post_chunk_received` entries fired at the same wall-clock instant (`elapsed_ms=0`, `total_bytes=4090` then `total_bytes=5120`, TCP/TLS segmentation of the first Android chunk visible to `axum`'s `Body::into_data_stream()`), then no further body chunks for 163 seconds, then `event=slow_post_aborted total_bytes=5120 reason="read_error" err=error reading a body from connection`. Tecno logcat (UTF-16 LE in `C:\temp\t2-tecno-tele2.log` → decoded to `C:\temp\t2-tecno-tele2-utf8.log`) captured the full T2 timeline: `T2_SLOW_POST_service_short_circuit` at `05:35:14.808`, `T2_SLOW_POST_armed` with full timeouts at `05:35:14.810`, all 8 `T2_SLOW_POST_chunk_sent` events from seq=1 (`total_sent=5120 elapsed_ms=344`) through seq=8 (`total_sent=40960 elapsed_ms=70365`), terminal `W T2_SLOW_POST_failed t=SocketTimeoutException msg=timeout total_sent=40960 elapsed_ms=130377` at `05:37:25.187`. (Initial PR #293 body wrongly claimed zero matches — UTF-16-vs-ASCII grep mismatch on my side; fixed in this fixup.) Concurrent short-POST request bodies REACHED relay and were processed in the same 163-second window — three `/prekeys/publish` (`event="prekey_publish" identity=bab6daa26fab3528 opk_count=40`) and one `/auth/session` (`event="rest_session_issued" identity=bab6daa2 token_prefix=22f706f5`) — server-side ingestion of short bodies was uniformly unaffected. Client-side response reception was NOT uniform, however: `/auth/session` returned 200 to the client (T2 short-circuit fired right after), but `/prekeys/publish` attempts 1 and 2 hit `PREKEY_TRACE prekey_publish_retry reason=SocketTimeoutException elapsedMs=60580/60853` on Android — the request body wrote, the relay processed it, but the response did not return within OkHttp's `readTimeout=60s`. Verdict: **byte-threshold class directionally confirmed; observed upload-direction abort point SIGNIFICANTLY EARLIER than the documented `net4people/bbs #490` 14-32 KB range — 5120 bytes on this carrier/device/hour.** The gap between Android `total_sent=40960` (queue-accept) and relay `total_received=5120` (physical egress) IS the discriminator hard gate 2 anticipated. The PREKEY response-direction failures add a second strengthening signal that the byte/duration-budget class extends to the response path on long-held connections. Honest framing locked in §13 Outcome: external 14-32 KB class hypothesis directionally confirmed, magnitude does NOT match, early cutoff strengthens (rather than weakens) the architectural implication that bare Direct mobile uplink is even less viable than the published threshold suggested. Isolation caveat locked: T2 was concurrent with production bootstrap traffic on adjacent TCP connections that may share carrier-allocated TCP/PDP context — verdict is "byte-threshold class confirmed" not "exact threshold 5120 ± ε confirmed." Wording bounds carried forward: does NOT claim `net4people` exact threshold confirmed on Tele2; does NOT claim hypotheses 1/2/3/4 refuted (Arm D / Arm A.2 control-vs-application asymmetry is orthogonal to byte-budget failure and may still be live); does NOT claim Direct WS is unfixable (Arm G discriminates whether wrapping in Reality changes the answer). Architectural implication: bare Direct uplink demoted as primary realtime path on RU mobile — this is NOT a WS-specific finding but a long-connection-uplink finding that subsumes the WS-specific Arm A.2 and Arm D verdicts. Any long-held connection on Tele2 LTE uplink is structurally untrustworthy regardless of payload type. §5 decision-tree row "Y met → uplift realtime per ADR-028" continues to fire; T2 strengthens the architectural component (long SSE responses through Caddy would also die at the byte threshold; Matrix-style 25-second long-poll becomes the mandatory primary REST realtime pattern; inside Reality must also use short-cycle framing such as mux / XHTTP). Track does NOT close on this outcome — Arm G (Reality-tunneled WS) is the remaining cheap micro-experiment from the §4 Arm A.2 Outcome carry-forward. Hard time-box 1 week from §4 Arm A.2 Outcome PR landing (2026-06-05). Operator-owned pre-merge step: revert `RELAY_ENABLE_SLOW_POST_DIAG=1` from VPS `.env` via idempotent `sed`; `compose up -d --force-recreate relay`; verify `slow_post_diag_enabled=false` in startup log + `POST /diag/slow-post` returns 404. xray REALITY production on `:8443` remains untouched throughout. Files: §13 T2 Outcome subsection added to track doc (~10 bullets mirroring the §4 Arm A.2 Outcome subsection pattern), PROJECT_LOG entry, this MASTER_TIMELINE bump, memory entry `project_t2_outcome_2026_06_05.md` + index. Zero application or relay code change.
- **#294 `f0b436a5`** — §14 Arm G mini-lock landed (PR-G1 docs-only). 7 Council decisions + 4 code-state notes verified against master `d0f41fbe` (clean TRANSPORT isolation, reuse production `xrayService` singleton from debug-only Arm G harness, DYNAMIC SOCKS port `127.0.0.1:<Ready.socksPort>` not hardcoded `10808`, `withTimeout(15_000L) { state.first { it is Ready } }` wait condition, echo round-trips REQUIRED for PASS, 15-min field run on Tecno+Tele2 LTE, split PR structure G1/G2/G3, narrow rule 8 carve-out). Three-outcome discriminator: PASS → Reality-primary realtime + 3.2b.1 safety net (~3-4 нед); PARTIAL → REST + Matrix-style long-poll primary + Reality as REST-fallback (~6-8 нед); FAIL → pure REST + 25-sec long-poll Option A, abandon Direct WS (~6-8 нед). Hard time-box 2026-06-12. Zero application or relay code change.
- **#295 `2c309852`** — §14 Arm G PR-G2 Android code: `RcDirectArmG.kt` Reality-tunneled WS heartbeat diagnostic (near-clone of `RcDirectArmA2`/`RcDirectArmD`) + `DEBUG_RC_DIRECT_ARM_G_VIA_REALITY` BuildConfig field + AppContainer lazy + PhantomMessagingService short-circuit between Arm D and production fall-through + 12 unit tests. Reuses production `xrayService` singleton; teardown calls both `rcDirectArmG?.stop()` AND `xrayService.stop()` in onDestroy. All Vladislav-locked review gates PASS (production stack untouched, xray logs redacted, `BuildConfig.DEBUG && DEBUG_RC_DIRECT_ARM_G_VIA_REALITY == "1"` strict parse, release-pinned `""`, zero server-side change — reuses Stage 5E `:8443`). `./gradlew :apps:android:assembleDebug` BUILD SUCCESSFUL.
- **#297 `dc6f52df`** — Trek 2 Stage 1 long-poll backbone — server-only additive + opt-in header. 8 locks Q0..Q7 from Vladislav (Q0 opt-in `X-Phantom-Long-Poll: 1` header for hold AND padded body shape per round 3 review; Q1 single `SessionResponse`; Q2 SEPARATE ack-deliver bucket per identity; Q3 `/relay/poll` carved out of `TimeoutLayer` with own 45-60s cap; Q4 padded poll responses 3072 B canonical, no silent envelope contract regression; Q5 WS path mirror to REST quantize; Q6 50K LRU cap on notifiers with `Arc::strong_count==1` guard; Q7 new logs 8-char only, existing 16-char tech debt deferred). Three review rounds — round 2 added `X-Phantom-Long-Poll` opt-in for hold; round 3 extended it to gate the padded body shape too. 100/100 tests pass. Operator policy: do NOT flip `RELAY_POLL_HOLD_SECS > 0` in prod until Stage 2 client work widely rolled out — production default kill switch is `RELAY_POLL_HOLD_SECS=0`. Production WS path byte-identical when `RELAY_POLL_HOLD_SECS=0`.
- **`df082528` (2026-06-09)** — Trek 1 Stage 1 / RC-LIBXRAY-REALITY-WIRE1 V3 xhttp closed FAIL. App-level matrix V1/V2/V3 exhausted; byte-identical 1440-byte Reality ClientHello stall persists across all V-shapes. Stall localised to libXray Android gomobile write path (single TCP segment of size MSS, then write stops; WSL2 Linux Xray sends 2 segments on the same config and reaches HTTP/2 200). Server + network + MSS clamp + bans all ruled out. Reality demoted to diagnostic surface for the RU mobile carrier; sub-track RC-LIBXRAY-REALITY-WIRE1 scoped (`docs/tracks/rc-libxray-reality-wire1.md`) but NOT started. Brief commit, no PR — closure record only.
- **#298 `30dc16f5`** — Trek 2 Stage 2A client-side long-poll foundations (no runtime behaviour change). 11 commits (A1..A8 base + 3 Round 2 P2/P3 review fixes), ~1206/-6 LOC across 23 files. A1 `pollHoldSecs` additive in `AuthSessionResponse` + `RelayCapabilities` projection / A2 SQLDelight `transport_seq_state` v19→v20 migration `20.sqm` / A3 `LastSeenSeqRepository` interface + `SqlDelight*` + `Fake*` impls / A4 `socksProxyPort: Int?` skeleton through factory + Android OkHttp wiring + orchestrator / A5 `EnvelopeId` `@JvmInline value class` boundary wrapper + `TODO(stage3-migration)` markers / A6 `LONGPOLL_V2_ENABLED` BuildConfig debug `"1"` / release `"0"` + AppContainer wiring + orchestrator gate / A7 `Csprng` interface + `LibsodiumCsprng` object (rejection sampling for `uniformLong` per R2-1) / A8 28 contract tests across 4 commonTest files. Round 2 review fixes: R2-1 `c1dc537c` modulo → rejection sampling in `Csprng.uniformLong` (provably zero bias); R2-2 `adf88c4e` direct `EnvelopeId.random()` tests + `libs.libsodium.bindings` in `transport.commonTest.dependencies`; R2-3 `d7f064a7` worst-case KDoc fix (actual ~50% rejection sits at `boundExclusive ≈ Long.MAX_VALUE/2 + 1`, not `Long.MAX_VALUE`). Stage 2A guardrail held: with `LONGPOLL_V2_ENABLED == "0"` (release pin) every existing transport path on master behaves byte-identically — no opt-in header set, no socket timeout raised, no `wsActivePollJob` spawned, no jittered hold consumption, no `lastSeenSeq` runtime use, no `poll_circuit_open` breaker, no `LONGPOLL_REAUTH_INTERVAL`, no `RestStateMachine.kt:31-32` docstring update, no voice-chunk outer padding (deferred entirely to Stage 3 per OQ2 amendment). Verify: `:shared:core:transport:jvmTest` / `:shared:core:crypto:jvmTest` / `:shared:core:storage:jvmTest` / `:apps:android:assembleDebug` all clean; CI on `d7f064a7` green. Next: Stage 2B runtime layer.
- **#300 / #301 / #302 (2026-06-10)** — Trek 2 Stage 1.x server prerequisite scope-doc trio. #300 locked the scope mini-lock as `docs/tracks/trek2-stage1x-server-prereq.md`; #301 absorbed Round 3 review amendments (per-identity HMAC key derivation, length-prefixed UTF-8 canonical encoding NOT UUID, two-header decoupling with explicit Lock-2 matrix); #302 was the final voice cleanup to strip all attribution from the scope doc. Outcome: scope doc is the binding contract for Stage 1.x implementation. Zero application or relay code change.
- **#303 `e3794e6e`** — Trek 2 Stage 1.x server prerequisite SHIPPED. Implements the 4 server-side locks scoped in `docs/tracks/trek2-stage1x-server-prereq.md`. New `services/relay/src/seq_mac.rs` module: `SeqMacRootKey(Zeroizing<[u8; 32]>)` with `[REDACTED]` `Debug`, per-identity verify-key derivation `HMAC(root_key, "phantom-seq-mac-key-v1\x00" || identity_hex)`; `RelayConfig::seq_mac_key: Arc<SeqMacRootKey>` field; relay fails-to-start without `RELAY_SEQ_MAC_KEY`. Lock-1 store-time MAC computation in `mirror_envelope_to_rest_store` over `(identity_hex, seq, envelope_id, sequence_ts_post_quantize)` with length-prefixed UTF-8 canonical encoding (101 bytes + `envelope_id_len` bytes). Lock-2 padded-vs-held header decoupling: `padded_opt_in = long_poll_opt_in || padded_poll_opt_in`, hold path stays gated on LP alone, padding gate switches to `padded_opt_in`. Lock-3 atomic `RestTokenStore::issue()` (structural single critical section across `by_token` + `by_identity` in lock-order matching `purge_expired`); post-rotation challenge replay returns `410 Gone` with body `{"error":"session token rotated; obtain a fresh challenge"}`; new `refresh_specific_token_if_live(token)` helper. Lock-4 `HoldSlot { notify: Notify, hold_count: AtomicU8 }` widened per-recipient notifier map; `HoldGuard::try_acquire(slot)` CAS-bounded by `PER_IDENTITY_HOLD_CAP = 3`; `MAX_POLL_HOLD_SECS_CAP = 480` dual-layer clamp at config-parse and runtime; `PollOutcome { Ready, HoldCapExceeded }` enum return for 429 + `Retry-After: 30` mapping. Round 2 review (4 blockers, all closed in single bundled commit `8cf7d582`): structural atomic issue + 410 Gone replay path + recipient `to` 64-hex + WS messageId byte-length validation at the request boundary + removal of `.expect()` on client-controlled input (`mirror_envelope_to_rest_store` now returns `Option<u64>`). Round 3 cleanup `e5391094` tightened the replay test to pin `StatusCode::GONE` + locked body strings, fixed a stale comment that claimed a non-existent WS test, cleared 5 clippy nits. Voice rule clean. 162 relay tests pass single-threaded; `cargo clippy -p phantom-relay --tests -- -D warnings` clean. Operator deployment step: provision `RELAY_SEQ_MAC_KEY` (64-hex from `openssl rand -hex 32`) in VPS `.env` BEFORE redeploying Stage 1.x or the relay fails to start.
- **#304 `9c02a3c8`** — Trek 2 Stage 2B-A client long-poll shell scope merged. New `docs/tracks/trek2-stage2b-a-client-shell.md` (165 lines). Seven binding client locks L1-L7: LP+PP headers IFF `LONGPOLL_V2_ENABLED == "1"` (both together) / read-timeout raised IFF flag-on AND `pollHoldSecs in 1..480` (safety margin in `[2, 8]` seconds) / `wsActivePollJob` PARALLEL with WS (lifecycle on transport-session owner, NOT WS-down fallback, MUST NOT replace Direct WSS fast path) / `since_seq` read-only / `seq_mac` presence-parse only NO verify, MUST NOT advance cursor on unverified MAC / release BuildConfig pin `"0"` + M4 contract test / out-of-scope MAC-verify + breaker + reauth-on-410 + voice-media-padding + Tor-Ghost. 3-commit boundary B1 headers+flag / B2 timeout-gate / B3 parallel-job+DTO. 7 contract tests M1-M7 (header emission / timeout-gate 9-cell matrix / kill-switch / release BuildConfig pin / cursor frozen / `seq_mac` presence parse without verify / parallel job continues while WS up). Server contract surface (7 points pinned at master `e3794e6e`) MUST be re-verified against current master at implementation start; pause if drift. WORKING_RULES rule 8 carve-out OK (shell-only, release pin); Tele2 LTE smoke gate transfers to Stage 2B-B. Strategic frame: backbone first → Direct WSS hardening → Reality Android repair → Tor/Ghost polish. Zero application or relay code change.
- **#306 `0ac29cf5`** — Trek 2 Stage 2B-A client long-poll shell SHIPPED. Implements the 7 binding client locks L1-L7 from PR #304. LP+PP headers gated on `LONGPOLL_V2_ENABLED == "1"` with both-together discipline; read-timeout floored at legacy 10_000 ms when flag-on AND `pollHoldSecs in 1..480`; `wsActivePollJob` parallel-with-WS lifecycle on transport-session owner (NOT WS-down fallback); read-only `LongPollCursorReader` SAM bridged to `SqlDelightLastSeenSeqRepository`; `seq_mac`/`seq_mac_verify_key` presence-parsed + cached in session-scoped slot; release pin `"0"` with M4 contract test. Zero runtime change in release. Sets the foundation for Stage 2B-B semantic layer.
- **#307 `f8cdc91a`** — Trek 2 Stage 2B-B preflight bundle (Round 1 + Round 2 + synthesis) merged. Four Round 1 project-subagent reports (architect/security/kmp/tester), one Round 2 independent sonnet cross-check that surfaced 7 blockers Round 1 missed (UTF-8 byte length envelope_id_len, HMAC key hex-decode-before-use, identity_hex binding, breaker-as-MAC-downgrade, 4 test gaps), one final synthesis with 15 LOCKED decisions D1-D15 + OQ-1..OQ-6 LOCKS + OQ-7 EXPLICITLY REJECTED. OQ-1 skip-and-continue (NO carve-out — D15 preserves the rule under all retry counts) / OQ-2 sequential commits OK cursor-after-storage / OQ-3 MISSING_MAC=drop no quarantine / OQ-4 no hard local logout capped 410 backoff / OQ-5 S6 mandatory ≥30 min Tele2 controllable trigger OK / OQ-6 decommission legacy pollLoop cursor in 2B-B. Replacement D15 (locked) safe bad-MAC posture: telemetry only on repeat / one forced session refresh latched per-envelope_id via existing tokenMutex / suspend BOTH REST loops symmetrically (legacy pollLoop also enforces identical seq_mac verify + KeyAbsent/KeyPresent/KeySuspended state to close R2-S-B5 MAC downgrade hole) / Direct WSS + Reality + Tor remain operational / no ack no delete no cursor advance ever. Two-layer preflight discipline confirmed as the canonical shape for HIGH-risk runtime PRs.
- **#308 `94ba8d7a`** — Trek 2 Stage 2B-B scope-doc merged. New `docs/tracks/trek2-stage2b-b-scope.md` (570 lines) codifies the 15 preflight decisions D1-D15 + six user locks OQ-1..OQ-6 + OQ-7 REJECTED into ten binding client locks L1-L10. NO new decisions invented — every paragraph references a synthesis lock by tag or carries a single-sentence corollary that the synthesis already established. L1 constant-time MAC compare / L2 verify-key state machine `KeyAbsent`/`KeyPresent(hex)`/`KeySuspended` with explicit 12-cell exhaustive transition matrix via TWO nested `when` blocks over sealed types + downgrade protection + outcome classifier at publication site + forced-refresh-failure corollary surfaced as a single explicit sentence / L3 cursor advance strictly after `AckOutcome.Acked` via single combined `ackInboundAndAdvanceCursor(envelopeId): AckOutcome` orchestrator method with three-phase pattern (cancellable Phase 1 mutex acquire + cancellable Phase 2 retry loop + `finally`-scoped narrow `withContext(NonCancellable) { ... }` Phase 3 cleanup) + bounded local cursor-write retry 3 attempts backoffs `[100, 500]` ms BETWEEN attempts only + explicit `try/catch (CancellationException) { throw }` + explicit `wasCancelled` flag + debug `FAILED_MAC` hold path preserves relay copy / L4 `LongPollCursorRepository` read/write seam + legacy `pollLoop` in-memory cursor decommissioned / L5 `SeqMacVerifier` canonical encoding (`ushr` BE, `encodeToByteArray().size`, hex-decode key, receiving identity) / L6 both REST poll loops symmetric verify semantics + `_inboundStateMutex` serialises all shared in-memory state INCLUDING verify-key state machine, lock order `tokenMutex` outer / `_inboundStateMutex` inner with verify-key publication INSIDE `acquireOrRefreshToken`'s existing critical section / L7 bad-MAC posture telemetry + one latched forced refresh + both loops suspended on poison no ack/delete/cursor-advance EVER / L8 410 reauth capped exponential 5s floor 60s ceiling + no hard local logout + hard timer caps (breaker 120s / 410 reauth 60s / HTTP 429 `Retry-After` 120s clamp) / L9 `LongPollBreakerState` separate from `RestStateMachine`, `HalfOpen(probeInFlight)` with atomic permit + cancellation-safe `finally` release + breaker open does NOT alter MAC verify + eight numeric constants locked + `BreakerOpenReason` taxonomy / L10 `Csprng` jitter migration on all 11 sites. Ten server-contract points pinned at master `f8cdc91a` (seven Stage 2B-A + three Stage 1.x additions). 33 M-cells total (M8-M17 + M-B7-M-B29). Six commits C1-C6 each individually green and reviewable. Voice rule clean. Seven sequential review rounds closed subtle concurrency/cancellation/state-machine edge cases — final APPROVE at head `1f12f5b6`. Tele2 LTE smoke gate WORKING_RULES rule 8 binding for Stage 2B-B and NOT waivable; six scenarios S1-S6 (S6 promoted from optional under OQ-5 LOCK) ≥30 min on Tecno + Tele2 LTE. Zero application or relay code change.
- **#309 `2ddb922a`** — Trek 2 Stage 2B-B implementation — long-poll backbone (C1-C6). Six commit boundary delivering the 10 client locks L1-L10 from #308. L1 constant-time MAC compare / L2 verify-key state machine + downgrade protection / L3 combined `ackInboundAndAdvanceCursor` three-phase pattern + bounded local retry / L4 `LongPollCursorRepository` read/write seam + legacy pollLoop in-memory cursor decommissioned / L5 `SeqMacVerifier` canonical encoding / L6 symmetric verify semantics on both loops + `_inboundStateMutex` serialises all shared state / L7 bad-MAC posture (telemetry + one latched forced refresh + both loops suspended on poison) / L8 410 reauth capped exponential / L9 `LongPollBreakerState` separate from `RestStateMachine` + `HalfOpen(probeInFlight)` atomic permit / L10 `Csprng` jitter migration on all 11 sites. Six review rounds closed sequential concurrency/cancellation/state-machine edge cases.
- **#310 `345d9761`** — Trek 2 Round 14 paced padded poll. Relay-side `RELAY_POLL_CHUNKED_FLUSH=1` env flag chunks the padded poll response into 4×1152 byte chunks with ~300 ms cadence between flushes (canonical `byteCount=4608` total). Mitigates the carrier-induced response-direction stall observed on Tele2 LTE for long-poll-padded bodies. Deployed to VPS 2026-06-16 ~14:59 UTC. Live traffic confirms `chunked_flush=true`.
- **#311 `df34222d`** — RC-CRYPTO-PAIR-X3DH-INIT Sprint 2a outbound role guard SHIPPED. `DefaultMessagingService.kt:434` guard `existingState != null && role == INITIATOR && !sessionSuspect` routes RESPONDER-tagged sessions into the bootstrap branch with a fresh `x3dhInit`. Wi-Fi smoke PASS: emu outbound event #3 took `bootstrap_path`, Tecno envelope `21350350` decrypted via `inbound_repair_ok bootstrap=true elapsedMs=77`, zero `action=hold`. Two review rounds closed (P1/P2 legacy `rs1:` blob default INITIATOR guard no-op + P3 KDoc time-evolution).
- **#312 → #313** — Docs-only PROJECT_LOG entries: Sprint 2a closure on Wi-Fi smoke evidence (LTE retry deferred to integration phase) + Stage 2B-D pre-promotion integration LTE smoke verdict (Round 14 transport PASS + Sprint 2a guard PASS + integration E2E FAIL on `OpkNotFound` → Sprint 2b promoted to primary with mandatory OPK lifecycle/restart-resilience scope).
- **#314 `cfa765d2`** — Sprint 2b OPK lifecycle + pending/active session scope-lock. Track-doc at `docs/tracks/sprint-2b-opk-pending-session-scope.md` (314 lines after 4 review-amendment rounds). 10 locks L1-L10 + 9 server-contract pins from Round 3 relay investigation (replace-wholesale + 10/hour publish + 60/min fetch + 60/min WS). Three-PR sequence: 2b-A zero-schema factory-lambda re-snapshot + ADR-009 amendment / 2b-B schema with `opk_reservation` + `pending_ratchet_state` companion + two-phase consume with DEFERRED OPK consumption + cap-8 LRU + pending-coordinated sweep + ADR-029 / 2b-C promotion wiring + outbound pending-INITIATOR reuse via `bootstrap_artifacts_blob` + Stage 2B-D Tele2 LTE smoke. Sprint scope-locked merged 2026-06-15. Council durables at `C:\temp\sprint-2b-{reconnaissance,council-layer1,council-layer1-round2}-2026-06-15\`.
- **#315 `30617121`** — Sprint 2b-A: factory-lambda re-snapshot in `PreKeyApi.publishBundle` + `PreKeyApiClient.publishWithRetry`. `PreKeyLifecycleService` helper `opksProvider` + 4 call sites pass `{ oneTimePreKeyRepository.getAll() }` with NO bootstrap exception; `MigrationManager` degenerate `{ publishRequest }`. L2 `DefaultMessagingService.kt:2411` decrypt-existing-first UNCHANGED (M-2bA-3 regression lock). ADR-009 amended inline. All 5 cells M-2bA-1..5 + Sprint 2a U1/U2/U3 + assembleDebug green; 4/4 GitHub CI checks SUCCESS.
- **#316 `b40c2908`** — Sprint 2b-B storage half: L3 pending companion + L4 two-phase DEFERRED OPK consume + L5 `opk_reservation` + L6 join-sweep + L7 cap-8 LRU + ADR-029 v1. Schema v20→v21. Runtime half lands in 2b-C.
- **#317 `7e421728`** — Sprint 2b-C runtime: pending→active promotion + L4 deferred-consume + 3-DH/legacy-fixture branch matrix + `OpkNotFoundMetric` stub + outbound INITIATOR pending reuse via `bootstrap_artifacts_blob`. Closes the OPK-lifecycle foundation for Sprint 2b.
- **#318 `fd8f93d9`** — Docs: M-OPK-3 Wi-Fi run INCONCLUSIVE → runbook Known Limitation + Stage 2B-D framing. L10 amendment demoting M-OPK-3 from binding gate; Stage 2B-D Tele2 LTE remains the real promotion gate.
- **#319 `45750ac9`** — Docs: 2026-06-16 Stage 2B-D Tele2 LTE smoke PASS + PR #310 MERGED + Round 14 LIVE on production relay. All three layers PASS (Round 14 wire 19×4608 + Sprint 2a guard 3 hits + Sprint 2b-C inbound-repair 3× `promotion=true`; zero `OpkNotFound` / `fail_mac action=hold` / `promotion=false`).
- **#320 `a213b3ab`** — Stage 2B-D rollout flag flip: `apps/android/build.gradle.kts:591` `LONGPOLL_V2_ENABLED "0" → "1"` release pin + M4 contract test flipped in tandem. Release APKs now activate Round 14 path on production relay. Sprint 2b + Stage 2B-D track CLOSED.
- **#321 `907b0a84`** — T2 instrumentation MERGED. Option A measurement-only PR: `RELAY_T2_DIAG` env flag + M13-style mutex + body-counter middleware on `/prekeys/publish` + timeout-trigger detector + `/diag-upstream-shape` streaming probe + client-side HTTP-version trace (`PreKeyApiClient.t2DiagPublishTraceEnabled` + `BuildConfig.RELAY_T2_DIAG_CLIENT` debug `"1"` / release `"0"`). Default-off, byte-identical release APKs to pre-PR.
- **#322 `4dacd9d0`** — T2 diagnostic round 2. Phase-trace + skip-body experiment flag to discriminate between smarter-retry / upstream chunking / skip-body-read on 2xx / server `204 No Content` / HTTP/2 unpin. Shipped after the T2 field run flipped the original diagnosis: server-side `/prekeys/publish` SUCCEEDS on Tele2 LTE (3 attempts × 5863-byte body × `frames=1` × `elapsed_ms=0` × `201 Created`); client OkHttp times out at ~60.6 s in the response-read path (`SocketTimeoutException` at `Http1ExchangeCodec$FixedLengthSource.read`) → response-direction stall after server-side success.
- **#323 `5cf18e5f`** — T2 carrier-ceiling prekey-publish bug CLOSED. Two-layer fix: Layer 1 `AndroidNativeOkHttpPreKeyPublishTransport.publish()` unconditional skip-body on 2xx; Layer 2 `PreKeyApiClient.publishWithRetry()` 201 branch synthesises `Stored(storedOpks=0)` on empty body. `PUBLISH_SKIP_SUCCESS_BODY_READ` BuildConfig + `skipBodyReadOnSuccess` constructor/factory param retired. Post-merge sanity smoke PASS on Tecno Tele2 LTE 2026-06-16 08:01 local: single attempt, 701 ms total wall-time (vs 186 000 ms pre-fix), `body_read_skipped` fires 1 ms after `headers_received status=201 protocol=http/1.1`, all 4 FAIL signals = 0 (`body_read_threw` / `JsonDecodingException` / `prekey_publish_retry` / `publish_fail_giving_up`). Side-finding (NOT T2 verdict): WS ping timeout ~30 s after publish = Direct WSS Mode 2 class, future Direct WSS hardening track pointer. Reliability backbone functionally complete for current alpha surface.
- **#324 `2779c164`** — Docs: T2 carrier-ceiling prekey-publish bug CLOSED + post-merge Tele2 LTE smoke PASS evidence.
- **#325 `63899f1f`** — Docs: voice notes on Tele2 LTE smoke PASS — voice transport-independent contract empirically confirmed. 4 voice notes (9-15 chunks each) sent Tecno ↔ emu, all `message_ready path=AUDIO_LOCAL`, media errors = 0 (`download_failed` / `decrypt_failed` / `sha256_mismatch` / `chunk_not_ready_deadline_exceeded`). T2 #323 fix did not regress (0 retry). Sprint 2b-C crypto held (`inbound_repair_ok promotion=true`, 0 `OpkNotFound`). Voice manifest envelope delivered through the long-poll backbone while emu did 14 mode switches WS ↔ REST and Tecno had `ws_ping_timeout_diag=32`. ADR-028 §"Layer 3" "voice = REST media" + multi-transport framing 2026-06-09 verified on media payload. Side-finding: first message yellow dot 8 s (PR-G3/G4-era `prekey_fetch_result=timeout`) — UX bug surface for DWS-UX track.
- **#326 `e05af400`** — Docs: ADR-011 honesty pass — align four docs with post-PR-R0.4a actuation posture.
- **#327 `929b2072`** — DWS-UX telemetry baseline + UX honesty fixes — no actuation. `ws_ping_timeout_raw` + REST overlay actual `TransportKind` + Error label discriminator + `retryWaitingMessages` source field. Track 1 of the DWS-UX execution plan locked 2026-06-17 after Ruflo verdict + 4-agent audit. Hard constraints: NO ACK deadline / inbound stall / quiescence / Recovering suppression / optimistic placeholder / Direct WSS root-cause fix / Calls / TURN / Reality / attachments in this PR.
- **#328 `e601cf3b`** — **R3.6 sticky-per-route Fast REST degradation MERGED.** Layered on top of bare 3.6 fast-path (`d3935300`). Single ordered `WsSessionLifecycleEvent` channel (`Channel.UNLIMITED` + `receiveAsFlow` + checked `trySend`) replaces the legacy `wsSessionEnded` SharedFlow; supervised single-collector dispatcher extracted as `internal class WsSessionLifecycleDispatcher` for honest unit-test coverage; sticky-recovery state machine (`StickyRecoveryState { None, PendingNewSession, InFlight }` + `lastObservedEpoch` monotonic floor + 60-second `ws_alive_60s` probation cleanup-hook chokepoint inside `transitionToWsActive`). Field smoke PASS 2026-06-21 01:46-01:54 on Tecno Tele2 LTE → Wi-Fi: Mode 2 detected at 31.038 s, sticky_armed gen=1, 10 Mode-2-killed Tele2 WS sessions with ZERO flap, two REST deliveries with decrypt OK, Wi-Fi recovery exact 60.015-second probation (`pending → started → cleared` ordered, `proof=ws_alive_60s`, `total_suppressed=0`), `wifi-A` via `ws_deliver_in` on recovered Direct. ZERO `LIFECYCLE_CONSUMER_ERROR`, ZERO `fail_mac action=hold`, ZERO `OpkNotFound`. NOT_EXERCISED suppression branch (`total_suppressed=0` — silent dead sessions), unit-covered. Release pins `MODE_2_FAST_PATH_ENABLED` + `MODE_2_STICKY_ENABLED` stay `"0"` post-merge. Side-finding (NOT R3.6 contract): recovered Direct died again on Wi-Fi via 106 s / 5-pong ping timeout — separate WS-stability signature, NOT Mode 2, feeds the next reconnect-quiescence / Direct-stability track.
- **#329 `1c1d1b4d`** — Docs: backfill MASTER_TIMELINE + PROJECT_LOG from 2026-06-11 through R3.6 ship (PR #328).
- **#330 (Draft / HOLD)** — RC-RECONNECT-QUIESCENCE1 feature PR opened 2026-06-23 on `ecd67e04`; head currently `6f49cd89` with architects-APPROVED code, Android CI `28023219092` SUCCESS 3m16s, APK SHA-256 `65ebaebf3a3f72e0eb8bc4bf381bcc85f72482370b0c401ad36a8d6f99803e35` with all three RC debug flags forced to `"1"`. 2026-06-25 v2 field-smoke was BLOCKED before the Tele2 LTE phase by a prekey-publish debounce race on the emulator (NOT a failure on the reconnect-quiescence layer). PR stays Draft / HOLD until baseline message exchange is field-stable end-to-end. Not blocked by PR #333 alone (see #333 entry) — gated on the separate Emu outbound REST send recon track that scopes the new transport-layer baseline blocker observed 2026-06-26.
- **#331 `740c7c28`** — Docs: RC-RECONNECT-QUIESCENCE1 retrospective mini-lock + PROJECT_LOG entry. Rule-3 backfill capturing the architecture review of PR #330 in repo-readable form.
- **#332 `768f23e6`** — Docs: RC-PREKEY-PUBLISH-DEBOUNCE-RACE mini-lock + PROJECT_LOG entry. Track-doc at `docs/tracks/rc-prekey-publish-debounce-race.md` (124 lines, 6 invariants, 8 acceptance gates). Mini-lock landed on master FIRST per WORKING_RULES rule 3 BEFORE the feature branch opened.
- **#334 `a9969681`** — Docs: RC-PREKEY-PUBLISH-DEBOUNCE-RACE merge close-out + 2026-06-26 field-smoke verdict capture (prekey goal field-confirmed; baseline blocked by separate emulator outbound REST connectivity issue; RC PR #330 stays HOLD pending the next recon track).
- **#335 `e4e91e37`** — Docs: REST-SEND-CONNECTIVITY-RECON1 mini-lock landed on master per WORKING_RULES rule 3. Track-doc at `docs/tracks/rest-send-connectivity-recon1.md` (six hypotheses H-A through H-F, seven candidate instruments I-1 through I-7 explicitly NOT pre-committed, six acceptance-gate verdicts, three Park conditions). Facts-first scope; no code; PR #330 untouched.
- **#336 `298a4471`** — Docs: REST-SEND-CONNECTIVITY-RECON1 I-1 deliverable. Minute-by-minute cross-device timeline reconstruction from the 2026-06-26 baseline-blocker log corpus. H-A REFUTED (Tecno on the same Wi-Fi at the same wall-clock minute completed `connectEnd elapsedMs=366` to `65.108.154.152:443` while the emulator burst was failing). H-B strongly supported by I-1 but explicitly NOT final-confirmed until I-2. Clock-anchor offset ≈ +13:41:35 derived from reciprocal bundle-fetch entries. No code; no fix scope-lock.
- **#337 `2ff13030`** — Docs: REST-SEND-CONNECTIVITY-RECON1 I-2 host synthetic probe design. Two-round review (Round 1 initial design; Round 2 fix for the timestamp-discipline premise error — `logcat -v time | Tee-Object` does NOT bind logcat lines to host clock; corrected to a `ForEach-Object` host-clock prefix discipline via `[DateTime]::UtcNow` so the first tab-separated field of every logcat line is `host_recv_ts` and the device-local time stays as payload context only; plus a `curl.exe --version` sanity check for the `%{exitcode}` 7.75.0 dependency). 152 → 184 lines, single track-doc. Defined PASS / FAIL / Park / Inconclusive criteria on a ± 10 s window around each `connectFailed` event. No code; no fix scope-lock; PR #330 untouched.
- **#339 `5934310e`** — Docs: DIRECT-WSS-MODE2-RECON1 mini-lock opened per WORKING_RULES rule 3. Track-doc at `docs/tracks/direct-wss-mode2-recon1.md` (141 lines): two-phase recon (Phase A clean baseline Direct WSS on master HEAD across three network classes Tecno Wi-Fi / Tecno Tele2 LTE / emu Wi-Fi, no fix-candidate APK; Phase B RC PR #330 validation with the three RC release flags forced to `"1"`). Eight Phase A hypotheses + six Phase B hypotheses. Eight instruments K-1 through K-8 explicitly NOT pre-committed (K-1 first deliverable). Three Park conditions. Not a re-investigation of Mode 2 root cause — `rc-direct-stability1.md` carry-forward facts (`F-Mode1`, `F-Mode2-pp0`, `F-Mode2-pp1`, `F-NotKtor`, `F-NotCount`, `F-CaddyAtMax`, `F-Mode2-cadence-invariant`) are inputs, not scope. No code; no fix scope-lock; PR #330 untouched.
- **#TBD (this PR)** — **QUIESCENCE-VALIDATION-L1-SYNTHETIC-MINI-LOCK opened — implementation scope-lock for MB half of H-ME.** Operator greenlit opening the MB-half implementation scope-lock immediately after the methodology recon closed with H-ME verdict (PR #349 squash `54f2e50d`). New track-doc at `docs/tracks/quiescence-validation-l1-synthetic-mini-lock.md` (227 lines) formalises the L1 synthetic-trigger implementation contract before any code lands. **Eleven binding locks L-13.3.1 through L-13.3.11 split across §4 + §5 + §7:** §4 carries the eight non-blocker / non-race locks (L-13.3.1 / .2 / .3 / .4 / .5 / .8 / .10 / .11); §5 carries the two BLOCKER locks (L-13.3.6 / L-13.3.7); §7 carries the race-contract lock (L-13.3.9). Together §4 + §5 + §7 bind all eleven. **§5 promotes the two security council BLOCKERS to formal preconditions that the implementation PR's initial diff MUST already satisfy** — §5.1 L-13.3.6 ProGuard narrowing with Option A "stack on already-landed PR #330 narrowing commit cited verbatim" (PR's initial diff carries no ProGuard changes; inherits from base) OR Option B "include ProGuard narrowing + R8 verification in the same initial implementation PR diff, before or alongside any synthetic-trigger code" (PR's initial diff carries the narrowing changes itself). A PR adding `debugForceMode2Synthetic` without Option A or Option B in the same initial diff is rejected at review without exception. §5.2 L-13.3.7 S6-style four-layer protected operator surface (debug-overlay manifest only / signature-level `android:permission` / runtime BuildConfig re-check inside component / AppContainer re-check before reaching `KtorRelayTransport.debugForceMode2Synthetic`) with four-layer mapping required for any operator-trigger shape, raw `am broadcast` BroadcastReceiver without explicit four-layer mapping forbidden. **§6 formalises the `SyntheticTriggerResult` typed-return contract** concretely as a sealed class with five required members `Fired / RefusedDisabled / RefusedNotConnected / RefusedDurationOutOfRange(requestedMs, minMs, maxMs) / RefusedAlreadyFired` plus an L-13.3.5-derived optional sixth `RefusedNoRestCapability` if the implementation PR chooses to handle the `maybeRetryBootstrap()` branch via the result type. **§7 carries the L-13.3.9 race-contract lock and pins three implementation-PR-binding resolution requirements** (does NOT resolve the race itself — the implementation PR resolves it within these requirements): §7.1 single epoch snapshot at top of method (no re-reads; `internal val currentSessionEpoch: Long?` accessor if needed) / §7.2 receiving-side dedup mechanism chosen from D-1 rely on `RestStateMachine` epoch filter / D-2 dispatcher-level dedup by `(sessionEpoch, closeOrigin)` tuple / D-3 method-level mutex on `_wsSessionLifecycle` / §7.3 adversarial race-window tests that force a real session death between guard and `trySend` and assert no panic / crash / silent state corruption. **§8 binds the implementation PR's test set to the source-grounded acceptance matrix** at `C:\temp\quiescence-h-me-council-2026-06-29\tests.md` as test floor not ceiling (~12+ positive markers source-grounded against PR #330 head `6f49cd89`, 5+ negative-presence checks, 7 structural diff-review checks, full PASS / FAIL timelines, one-shot vs repeat semantics). **§9 specifies the implementation PR's hand-off contract** — cites this mini-lock SHA + H-ME verdict SHA `54f2e50d`, states explicitly that B1 closure requires the MC half ALSO PASS (this PR delivers MB only), documents §5.1 Option A or B choice, documents §5.2 four-layer surface mapping with companion wiring test, includes §6 sealed class, picks §7.2 D-1 / D-2 / D-3 with rationale, includes §7.3 adversarial tests, re-verifies all matrix markers against current master at opening. **§10 records three park conditions:** P-1 PR #330's narrowing plan changes / P-2 operator unavailable / P-3 architectural change in `KtorRelayTransport` or dispatcher. **§11 enumerates what is NOT pre-decided** so the implementation PR retains design freedom on exact Kotlin file naming, exact signature permission string, exact component name, exact dedup option, exact Gradle task implementation. Per §9 hand-off rule of the parent methodology recon and per this mini-lock's own §9: implementation PR opens ONLY after this mini-lock merges; pre-merge implementation work rejected at review. MC half scope-lock opens separately (NOT this mini-lock's question). Controlled Wi-Fi smoke run procedure opens AFTER both halves merge AND MC PASS + MB PASS land on record. RC PR #330 Draft / HOLD unchanged. DIRECT-WSS-MODE2-RECON1 §11 / §12 unchanged. Methodology recon stays Closed with verdict H-ME.
- **#349 `54f2e50d`** — **QUIESCENCE-VALIDATION-METHODOLOGY-RECON1 closed with verdict H-ME — state-machine integration validation + L1 synthetic field trigger.** Operator pivoted to closure per §6 acceptance gate #2 (combination MC + MB = H-ME) after N-1 + N-2 + N-3 produced sufficient evidence. NOT N-4 (no third network class identified within reasonable operator effort). NOT N-5 (release-gate amendment is not validation). NOT H-MC alone (master HEAD fakes are interface-shaped per N-2; state-machine tests alone cannot assert field-shape match). Recon status flips Open → **Closed (verdict H-ME)**; `Last known master at close: 98ee7e09`. New §13 appended (~190 LOC) plus §1 status line update. **Council review by four agents pre-close (durables `C:\temp\quiescence-h-me-council-2026-06-29\`):** architect (H-ME RIGHT WITH CAVEATS — five caveats promoted to §13.3 locks L-13.3.1 through L-13.3.5); implementation (L1 FEASIBLE WITH CAVEATS — Boolean injection over provider → L-13.3.8, typed `SyntheticTriggerResult` → L-13.3.10, one-shot-per-epoch latch → L-13.3.9); tests/adversarial (acceptance matrix ~1450 words, source-grounded against PR #330 head `6f49cd89` and master HEAD `98ee7e09`, real markers `mode_2_signature_matched action=fast_path` / `sticky_armed` / `ws_reconnect_quiesced` / `ws_recovery_probe_granted` / `ws_reconnect_resumed` / `sticky_cleared proof=ws_alive_60s` / `ws_reconnect_open proof=ws_alive_60s` cited from real source, Mode-2 window pinned `MODE_2_MIN_DURATION_MS=25000`/`MAX=65000` from `RestStateMachine.kt:1732-1747` → L-13.3.11); security (L1 SAFE WITH CONDITIONS — **two BLOCKERS** promoted to L-13.3.6 ProGuard wildcard `-keep class phantom.core.transport.KtorRelayTransport { *; }` on master HEAD preserves debug surface in release APK; L-13.3.7 S6-style four-layer protected operator surface required, raw `am broadcast` foreclosed). **11 binding locks documented in §13.3:** both halves required (no MC-alone, no MB-alone) / L1 mandatory L2 rejected / sequential dispatcher order load-bearing / `closeOrigin="synthetic"` non-branching discipline / `maybeRetryBootstrap()` scope gap / ProGuard narrowing precondition BLOCKER / S6-style operator surface BLOCKER / Boolean injection / one-shot-per-epoch latch / typed `SyntheticTriggerResult` return / acceptance matrix is test floor. **Forward-pointer (NOT scoped here):** separate L1 implementation mini-lock PR opens AFTER this closure merges; carries forward the 11 locks; picks S6-style operator-surface concrete shape; specifies typed `SyntheticTriggerResult` enum; binds to ProGuard narrowing precondition (either stack on PR #330's narrowing or include equivalent in L1 track); adopts the acceptance matrix at `C:\temp\quiescence-h-me-council-2026-06-29\tests.md` as test floor. L1 implementation PR opens only after mini-lock merges. Controlled Wi-Fi smoke runs only after L1 implementation lands AND MC integration tests are in place. **PR #330 status unchanged** — stays Draft / HOLD; B1 gate from `direct-wss-mode2-recon1.md` §11 closes only when **MC PASS + MB PASS** land on record per L-13.3.1. DIRECT-WSS-MODE2-RECON1 §11 / §12 unchanged.
- **#348 `98ee7e09`** — **QUIESCENCE-VALIDATION-METHODOLOGY-RECON1 N-3 progress — synthetic-trigger debug-flag design exercise.** Operator greenlit N-3 the third instrument from §5 immediately after PR #347 squash `88cf810a`. Source-read + design sketch only; no code change; no operator devices touched; PR #330 untouched. New §12 appended to `docs/tracks/quiescence-validation-methodology-recon1.md` (~150 LOC). **Mode 2 detection chain on master HEAD mapped — two downstream consumers of one upstream `WsSessionLifecycleEvent.Ended`, dispatched SEQUENTIALLY in `WsSessionLifecycleDispatcher.dispatch(Ended)`** (state-machine consumer first at dispatcher line 55 `submitStateEvent`, telemetry consumer second at line 59 `feedDegradationDetector`). Consumer A — state-machine actuation: `dispatcher → submitStateEvent → RestStateMachine.onWsSessionEnded` → 3-condition signature check → if `mode2FastPathEnabled = true` → `transitionToRest("mode_2_fast_path")` + if `mode2StickyEnabled = true` → `armSticky()` emits `sticky_armed`. Consumer B — telemetry-only on master: `dispatcher → feedDegradationDetectorOnWsSessionEnded → WsDegradationDetector.recordAndEmit` (per `WsDegradationDetector.kt:59` class KDoc "No `markSuspect` action. This detector only logs."). Both `mode2FastPathEnabled` and `mode2StickyEnabled` are `Boolean` constructor parameters on `RestStateMachine` (commonMain) — Android-side `AppContainer.kt:1348-1357` reads `phantom.android.BuildConfig.MODE_2_FAST_PATH_ENABLED == "1"` / `MODE_2_STICKY_ENABLED == "1"` and passes the Booleans down; commonMain code never reads `BuildConfig` directly. PR #330's quiescence chain hooks on top of `armSticky()`. **Five candidate injection levels L0–L4 sketched + per-level honesty profile assessed: L1 (lifecycle event channel `_wsSessionLifecycle.trySend(WsSessionLifecycleEvent.Ended(...))`) is the cleanest candidate** — HIGH production-path coverage (bypasses only OkHttp WS + parser + finally-block assembly; both Consumer A state-machine actuation and Consumer B telemetry detector downstream code runs unchanged); the 3 bypass gaps are mitigated by existing `PingTimeoutTextParserTest` 13 cells + an honesty-constraint code-review check that the synthetic populate the SAME fields with the SAME shape + a `Connected` precondition refusing to fire without a real active session. **L2** acceptable for state-machine-only validation but loses Consumer B (telemetry detector) coverage. **L3** (calling `armSticky()` direct) and **L4** (gate-level direct, only on PR #330 anyway) ruled out by the §5 prohibition on parallel test-only paths. **L0** ruled out by infeasibility on master HEAD without server coordination OR deeply-invasive ping-watchdog hook. **Verdict per §5 framing on H-MB risk profile:** the synthetic trigger CAN be designed at L1 so it exercises the SAME production path the field hits, without becoming a parallel test-only path — provided six load-bearing honesty constraints hold (same constructor / `closeOrigin="synthetic"` discipline / no dispatcher-or-state-machine branch on closeOrigin / BuildConfig gates remain orthogonal / `Connected` precondition / field-validation scope honesty on what's NOT validated). §12.4 sketches the L1 design surface respecting commonMain / Android isolation (shared code cannot see `phantom.android.BuildConfig`; established pattern at `AppContainer.kt:1348-1357` reads on Android side and injects Boolean to shared constructor) — one new Android-side BuildConfig flag `DEBUG_FORCE_MODE_2_DETECTION` defaulting `"0"` in `apps/android/build.gradle.kts` release block (mirroring existing `MODE_2_FAST_PATH_ENABLED` pattern with companion `Mode2DebugForceReleaseBuildConfigPinTest` in `androidUnitTest`); one new constructor parameter on `KtorRelayTransport` — `debugForceMode2Enabled: Boolean = false` (or `debugForceMode2Provider: () -> Boolean = { false }` if lazy re-eval is needed) injected by AppContainer reading the BuildConfig literal on Android side; one new `internal fun debugForceMode2Synthetic(durationMs: Long): Boolean` method on `KtorRelayTransport` (`internal` only — no `public` alias), gated by the injected Boolean/provider, refuses if `_state.value !is TransportState.Connected` or if `durationMs` lies outside `MODE_2_MIN_DURATION_MS..MODE_2_MAX_DURATION_MS`, builds the synthetic event satisfying the signature check, calls `_wsSessionLifecycle.trySend(...)`; one operator-facing trigger surface on the Android side (debug menu OR `am broadcast` intent handler — design left open at sketch level). Per §9 hand-off rule: N-3 progress note does NOT propose H-MB, H-ME, or any combination methodology. N-3 does NOT decide L1 is the right shape (presupposes methodology choice). N-3 does NOT propose code — §12.4 sketch is design-exercise output per §5, NOT a scope-lock. N-3 does NOT decide whether PR #330's gate accepts the L1-injected signal cleanly (out of "master HEAD" source-read scope). N-4 / N-5 remain candidates; operator chooses which (if any) runs next; do NOT auto-start. If operator parks further N-x, recon parks per §7 P-2 with N-1 + N-2 + N-3 evidence on record. RC PR #330 Draft / HOLD unchanged. DIRECT-WSS-MODE2-RECON1 §11 / §12 unchanged.
- **#347 `88cf810a`** — **QUIESCENCE-VALIDATION-METHODOLOGY-RECON1 N-2 progress — fake-transport surface review on master HEAD.** Operator greenlit N-2 the second instrument from §5 immediately after PR #346 squash `e568d97b`. Source-read only; no operator devices touched; PR #330 untouched. New §11 appended to `docs/tracks/quiescence-validation-methodology-recon1.md` (~110 LOC) catalogues **16 fake-transport doubles + 3 auxiliary doubles** on master HEAD (16 fake-transport doubles = 11 `RestFallbackTransport` doubles across 8 test files + 2 `RelayTransport` doubles + 3 `PreKeyPublishHttpTransport` doubles; plus 3 auxiliary — `FakeMediaAuthTokenProvider`, `CapturingTransportManagerLog`, `FakeLastSeenSeqRepository`) and assesses each against the five quiescence-surface dimensions from §5 (Mode 2 detector input signals / sticky window timing / recovery probe outcome / 60s probation / self-reentry / dup / loss). **Per-dimension fidelity verdict — two honest counts both apply: per the §5 framing (5 dimensions): 3 NOT MODELLED (Mode 2 detector input signals — no `RelayTransport` fake emits `WsSessionLifecycleEvent`; sticky window timing — gate absent per §10; recovery probe outcome — gate absent) / 1 PARTIAL but bypasses fakes (60s probation / `ws_alive_60s` proof — `RestStateMachineTest` exercises the numeric via controllable `now` driven directly into the production state machine, no fake transport in the loop, gate-side invocation absent) / 1 MIXED (self-reentry / dup / loss — three sub-surfaces with distinct fidelity: WS-side self-reentry NOT MODELLED, WS-side gate-coordinated dup / loss NOT MODELLED, REST-side dup / loss FAITHFULLY MODELLED via `Idempotency-Key` plumbing + `RestInboundDeduplicator` `Emit / SkipNoAck / ReAck` discipline + end-to-end FIFO via internal test seams on production `KtorRelayTransport`); per the §11.3 row-level table (7 rows splitting the MIXED dimension): 5 NOT MODELLED / 1 PARTIAL / 1 FAITHFULLY MODELLED.** Structural finding (§11.4): master HEAD fakes are **interface-shaped, not lifecycle-shaped** — each component is unit-test-scaffolded in isolation (orchestrator via fake, state machine via controllable `now`, detector via virtual state provider), no fake emits cross-component signals (`WsSessionLifecycleEvent`, gate transitions, `sticky_armed` event lines) a quiescence-chain integration validation would have to observe. Closest existing lifecycle-shaped pattern: `WsLifecycleCollectorSideEffectsTest` (android, 3 tests) bypasses transports entirely and drives `WsSessionLifecycleEvent.Ended → toLegacyEndedEvent → feedDegradationDetectorOnWsSessionEnded` against a real `WsDegradationDetector` — but stops at detector verdict, does not exercise downstream gate behaviour (because gate does not exist on master HEAD). Per §9 hand-off rule: N-2 progress note does NOT propose a methodology. N-2 does NOT decide whether the fakes could be extended into a gate-validating shape (that question presupposes a methodology choice). N-2 does NOT decide whether PR #330's own added test files provide an integration-shaped infrastructure (those are on PR #330's branch only, out of "master HEAD" scope). N-3 / N-4 / N-5 remain candidates; operator chooses which (if any) runs next; do NOT auto-start. If operator defers further N-x, recon parks per §7 P-2 with N-1 + N-2 evidence on record. RC PR #330 Draft / HOLD unchanged. DIRECT-WSS-MODE2-RECON1 §11 / §12 unchanged.
- **#346 `e568d97b`** — **QUIESCENCE-VALIDATION-METHODOLOGY-RECON1 N-1 progress — existing-test inventory on master HEAD.** Operator greenlit N-1 the first instrument from §5 after PR #345 squash `aab66577`. Source-read only; no operator devices touched; PR #330 untouched. New §10 appended to `docs/tracks/quiescence-validation-methodology-recon1.md` (+106 LOC) catalogues 26 relevant test files (commonTest transport + androidUnitTest) and maps per-Phase-B-hypothesis coverage on master HEAD. **Inventory summary: 0 of 6 hypotheses FULL-COVERED on master HEAD. 3 of 6 PARTIAL (H-330-Preserves-REST, H-330-Probe-Lives-60s, H-330-No-Message-Loss-Or-Dups — related invariants well-covered in `RestStateMachineTest` 67 @Test / `RestFallbackOrchestratorBreakerTest` 41 @Test / `KtorRelayTransportFifoTest` 5 @Test / `KtorRelayTransportPendingOutboundTest` 13 @Test / `RestInboundDeduplicatorTest` 9 @Test / `AckInboundAndAdvanceCursorTest` 16 @Test / `BodyTimeoutContractTest` 6 @Test, but the gate-coordinated quiescence-window flow is not exercised). 3 of 6 NOT-COVERED (H-330-Quiesces-Storm, H-330-Single-Probe-Per-RouteChange, H-330-No-Self-Reentry — the load-bearing `WsReconnectGate.kt` 447-LOC component containing the `Open / Quiesced / ProbeAvailable / ProbeClaimed / CandidateProving` state machine is ABSENT on master; PR #330 introduces it).** Side-finding flagged but explicitly OUT OF SCOPE per §5 framing "master HEAD": PR #330 adds eight test files of its own (`WsReconnectGateTest` 1172 LOC, `HybridRelayTransportIntegrationTest20` 686 LOC, `TransportRewalkCoordinatorTransactionTest` 667 LOC, `KtorRelayTransportDisconnectAndJoinTest` 848 LOC, `KtorRelayTransportRunReconnectLoopTest` 480 LOC, `RestFallbackOrchestratorQuiescenceWiringTest` 184 LOC, `ReconnectQuiescenceReleaseBuildConfigPinTest` 117 LOC, `KtorRelayTransportInternalTestSeams` 153 LOC) but whether those tests honestly cover the six hypotheses at the state-machine / integration level is a separate question about PR #330's CI — not N-1's question by the §5 framing, not opened here. Per §9 hand-off rule: N-1 progress note does NOT propose a methodology. N-1 does NOT decide H-MC's feasibility (that is N-2's job — fake-transport surface review). N-2 next when operator schedules; do NOT auto-start. RC PR #330 Draft / HOLD unchanged. DIRECT-WSS-MODE2-RECON1 §11 / §12 unchanged.
- **#345 `aab66577`** — **QUIESCENCE-VALIDATION-METHODOLOGY-RECON1 mini-lock opened.** Operator greenlit the methodology recon flagged as forward-pointer in `direct-wss-mode2-recon1.md` §12. New track-doc at `docs/tracks/quiescence-validation-methodology-recon1.md` opens a facts-first recon to discriminate among six candidate methodologies for honestly closing B1 when Wi-Fi does not reproduce Mode 2 AND Tele2 LTE breaks the REST fallback substrate. Six candidate methodologies (H-MA third network class / H-MB synthetic in-app debug-only trigger / H-MC state-machine integration validation against existing fake-transport infrastructure / H-MD = MC + MA / H-ME = MC + MB / H-MF release-gate amendment fallback as the realistic alternative if MA-ME all turn out infeasible). Five candidate instruments N-1 through N-5 explicitly NOT pre-committed in advance (N-1 existing-test inventory + N-2 fake-transport surface review = cheapest source-read pair; N-3 / N-4 / N-5 conditional on N-1 + N-2 evidence). Six acceptance-gate verdicts (MC-sufficient / MD / ME / MA-only / MB-only / MF release-gate amendment). Three Park conditions. Out-of-scope explicit: no code change in this recon; no fix scope-lock; specifically NOT pre-locked — extending B1's hypothesis set, weakening §11's release / rollout gate from inside this recon, dropping any of the six Phase B hypotheses, accepting "letter met / spirit not" verdicts as B1-PASS. The recon does NOT propose a methodology; it discriminates among candidates and forwards a chosen path back to DIRECT-WSS-MODE2-RECON1 for execution. PR #330 stays Draft / HOLD throughout; its gating now includes the closure of this recon plus the §11 release / rollout gate, in series.
- **#344 `364a7a48`** — **DIRECT-WSS-MODE2-RECON1 §12 K-6 outcome — Wi-Fi non-regression PASS / quiescence NOT EXERCISED + methodology recon forward-pointer.** Operator-executed K-6 on 2026-06-28: Tecno Wi-Fi + emu Wi-Fi pair (~30 min), VPN OFF, fix-candidate APK SHA `65ebaebf3a3f72e0eb8bc4bf381bcc85f72482370b0c401ad36a8d6f99803e35` built from PR #330 head `6f49cd89` with three RC flags forced to `"1"` via env vars at build (verified in BuildConfig.java), NO commits to PR #330's branch. NEW verdict class beyond §11's anticipated B1-PASS / B1-PARTIAL / B1-FAIL: all six Phase B hypotheses meet the letter (none FAILED) but the spirit (mechanism actually tested) NOT met. K-6 hypothesis matrix: 1 PASS (H-330-No-Message-Loss-Or-Dups — zero `relay_send_return ok=false`), 4 NOT EXERCISED (H-330-Quiesces-Storm, H-330-Single-Probe-Per-RouteChange, H-330-Probe-Lives-60s in quiescence context, H-330-No-Self-Reentry — Mode 2 detector never fired so no quiescence chain ran), 1 VACUOUSLY PASS (H-330-Preserves-REST — REST events present but not stress-tested by quiescence window). What K-6 DID establish: PR #330 with three flags `"1"` does not regress Wi-Fi delivery; messages flowed both directions; reconnect ratios (Tecno 33/16 = 2.06×, emu 33/16 = 2.06×) unchanged vs K-1 baseline (2.0× both); Sprint 2b-C inbound_repair_ok promotion=true works on Wi-Fi (7 `pending_fallback_fail` per side all followed by successful repair, zero `DECRYPT_TRACE fail_mac`, zero `OpkNotFound`, zero `action=hold` — positive against K-2 LTE side-finding's "Sprint 2b-C might regress" worry). What K-6 did NOT establish: core PR #330 quiescence chain (`sticky → probe → ws_alive_60s recovery`) NOT field-validated; the five `mode_switched ... reason=ws_alive_60s` events observed (Tecno 4 + emu 1) are R3.6 sticky-per-route `WsCandidate → WS_ACTIVE` transitions WITHOUT preceding `sticky_armed` — same log field name, different semantic context, NOT quiescence-recovery probes. Phase B status: B1 acceptance gate NOT closed; PR #330 stays Draft / HOLD; release / rollout gate from §11 stays in force. Methodology forward-pointer (NOT scoped here, NOT a track opened by this PR): how to validate the quiescence mechanism when Wi-Fi does not reproduce Mode 2 (no symptom) AND Tele2 LTE breaks the REST fallback substrate (no working venue). Three candidate paths listed as forward-pointer only — (a) third network class (different LTE carrier, tethered Wi-Fi, degraded-RSSI Wi-Fi), (b) synthetic / instrumentation-only triggers, (c) state-machine / integration-level validation against a fake transport. Separate facts-first recon `QUIESCENCE-VALIDATION-METHODOLOGY-RECON1` (working name) is the natural place to discriminate but NOT opened by this PR; conditional on operator greenlight. Side-findings preserved (NOT actioned): operator's QR-add → reverse-add warm-up flow was same paired-bootstrap pattern as K-1 (not a deviation); receiver-side decrypt counts under-counted because the analysis script's regex missed `inbound_repair_ok` events that carry plaintext bytes too — future K-runs should add `inbound_repair_ok` to the receiver-side delivery count. No code change. No fix scope-lock. No commit to PR #330's branch.
- **#343 `2bdbae98`** — **DIRECT-WSS-MODE2-RECON1 §11 Phase B amendment — B1 / B2 split + explicit release / rollout gate.** Operator decision after TELE2-LTE-REST-BREAKER-RECON1 closure: split Phase B into B1 (Wi-Fi mechanical validation of PR #330's quiescence contract) + B2 (separate Tele2 LTE delivery-blocker track, NOT yet opened — forward-pointer only). B1 scope: build PR #330 fix-candidate APK with three RC flags forced to `"1"` (K-5 build-time op only, NO commits to PR #330's branch), run one Tecno Wi-Fi session paired with emu Wi-Fi (~30 min), compare against K-1 baseline; B1-PASS = all six Phase B hypotheses hold = mechanism mechanically correct on Wi-Fi (NOT ship-ready). K-3 (emu Wi-Fi) NOT promoted as B1 prerequisite. B2 scope: Tele2 LTE end-to-end delivery is governed by rc-direct-stability1.md §13 + TELE2-LTE-REST-BREAKER-RECON1 §11 closure, NOT this recon — opens on its own as a separate facts-first mini-lock when operator schedules it. Release / rollout gate: PR #330 does NOT ship to LTE users on B1-PASS alone — requires either (i) B2 closure with fix that demonstrates Tele2 LTE end-to-end delivery in a separate field run, OR (ii) alternative LTE transport (Reality / alternative endpoint / relay-side hold-policy change) with affirmative LTE delivery evidence. "Mechanism mechanically correct on Wi-Fi" is NOT sufficient for shipping to a Tele2 LTE user population. The B1 / B2 split is a refinement of the three-paths set forwarded by TELE2-LTE-REST-BREAKER-RECON1 §11; it preserves the path-(c) Wi-Fi-validation direction but separates "validate the mechanism we can" from "ship to users". No code change. No fix scope-lock. No commit to PR #330's branch.
- **#342 `4302f56d`** — **TELE2-LTE-REST-BREAKER-RECON1 closure verdict (Option 2 — close on M-1 + M-5 + T2 carry-forward, without M-2 packet capture).** §11 closure verdict appended to `tele2-lte-rest-breaker-recon1.md`. Composite verdict: Gate 2 `H-NetworkSideCancellation supported via M-1 + M-5 + carry-forward correlation with rc-direct-stability1.md §13 T2 outcome` (NOT packet-confirmed; corpus alone cannot fully exclude relay-side response truncation under load, HTTP/2 stream backpressure, or TLS-layer renegotiation race) AND Gate 5 `H-Tele2LTESpecific supported via single-variable K-1 Wi-Fi vs K-2 Tele2 LTE comparison + §13 corroboration` (NOT confirmed against other LTE carriers — verdict bounded to "this surface is observable on Tele2 LTE"). Other §4 hypotheses: H-OrchestratorCancellation REFUTED, H-BreakerCooldownTooAggressive DEMOTED (not root cause), H-BreakerOpenBlocksInboundDispatch REFUTED at source-read level, H-OkHttpInterruptionRace REFUTED. Net architectural picture: K-2 inbound gap = two independent failures on the same network class, both below the application layer (Tele2 LTE long-held downlink body loss + Tele2 LTE Direct WSS Mode 2 pp0); neither in app code at master HEAD. Forward-pointer for parent direct-wss-mode2-recon1.md Phase B (NOT this recon's scope): three candidate paths for operator decision — (a) re-scope Phase B to Wi-Fi only with explicit Tele2 LTE governance honesty, (b) open separate fix-track for the long-held-connection problem before Phase B continues, (c) accept the LTE limitation and validate Phase B on Wi-Fi as best-available. Candidate fix-shape sets listed as forward-pointer only (NOT promoted from this recon): short-poll mode on LTE, decoupling callTimeout from readTimeout in long-poll path, server-side keep-alive byte trickle to defeat NAT teardown, switching to a different relay endpoint or transport on LTE-detected paths. Side-findings preserved (NOT actioned): Round 14 padded poll body ~4608 bytes sits close to T2 cumulative-bytes threshold class (~5 KB cutoff per §13); AndroidNativeOkHttpRestFallbackTransport.kt:330 couples callTimeout and readTimeout to the same 35 s budget on long-poll paths; K-2 Emu-side Sprint 2b-C OpkNotFound / fail_mac on 2 of 3 messages (single observation only). No code change. No fix scope-lock. PR #330 untouched; RC #330's gating now becomes whichever of the three architectural-question paths the operator chooses for the parent recon. Parent direct-wss-mode2-recon1.md track-doc gets a corresponding amendment in a separate follow-up docs PR.
- **#341 `51e72e03`** — Docs: TELE2-LTE-REST-BREAKER-RECON1 M-1 + M-5 combined progress note. §10 appended to track-doc (+64 LOC). M-1 (re-read of K-2 corpus, no operator action): all 11 op=poll callFailed exception=InterruptedIOException events totalMs in 35002-35006 ms band (distribution 1 × 35002 / 1 × 35004 / 5 × 35005 / 4 × 35006); 12 op=poll responseHeadersEnd status=200 events; zero op=poll responseBodyEnd events across the full 30-minute window. M-5 (source-code read at master HEAD `b3943641`): 35 s = (pollHoldSecs=30 + POLL_HOLD_SAFETY_MARGIN_SECS=5) × 1000 ms at RestFallbackOrchestrator.kt::computeLongPollReadTimeoutMs line 3689, threaded into OkHttp callTimeout via AndroidNativeOkHttpRestFallbackTransport.kt; WS deliver-in path (HybridRelayTransport.kt::startWsPassthroughCollectors line 446) launches scope.launch independent of breaker state. Hypothesis matrix update: 4 refuted (H-OrchestratorCancellation, H-BreakerOpenBlocksInboundDispatch, H-OkHttpInterruptionRace, plus H-BreakerCooldownTooAggressive demoted as not root cause); 2 strongly supported (H-NetworkSideCancellation, H-Tele2LTESpecific) but NOT packet-confirmed without M-2. Two-round review (Round 1 initial draft; Round 2 wording fixes for `35005 ± 1 ms` → factual `35002-35006 ms` distribution, and softened "Tele2 LTE network actively breaks" → "evidence strongly points to...; final on-the-wire confirmation requires M-2" + explicit cannot-fully-exclude clause naming relay-side response truncation, HTTP/2 stream backpressure, TLS-layer renegotiation race). Single file. No code change; no fix scope-lock; PR #330 untouched. Recon does NOT close on this PR — closure verdict landed in the follow-up PR per Option 2.
- **#340 `b3943641`** — **DIRECT-WSS-MODE2-RECON1 Phase A K-1 + K-2 progress + TELE2-LTE-REST-BREAKER-RECON1 mini-lock opened.** §10 Phase A progress appended to `direct-wss-mode2-recon1.md`. K-1 (Tecno Wi-Fi, ~44 min, 2026-06-27, master HEAD `5934310e` debug APK SHA `3d3317bd...`): CLEAN baseline — 21 WSS sessions per device, lifetime ~136 s × 7 successful ping/pongs (mode), `F-Mode1` re-confirmed with slight cadence shift vs historical ~150 s × 8 pongs; REST delivered (88-90 events per device, `relay_send_return ok=false = 0`); cross-device sync `±2-3 s` per session-number as side-finding. Evidence at `C:\temp\direct-wss-mode2-k1-tecno-wifi\` with SHAs. K-2 (Tecno Tele2 LTE, ~30 min, 2026-06-27): NOT CLEAN — Direct WSS Mode 2 captured cleanly (21 sessions × ~31 s × 0 pong, textbook `F-Mode2 pp0`, 54 `WebSocket connect` ≈2.5× sessions worth of reconnect storm), but `H-REST-Survives` REFUTED on LTE: 11 × `op=poll callFailed exception=InterruptedIOException`, breaker cooldown ramped fully `5 → 10 → 20 → 40 → 80 → 120 → 120 s` (7 `breaker_open ConsecutiveRestFailures`), then `poll_call_skipped` dominated the rest of the window; **zero `inbound_deliver` / `DECRYPT_TRACE` / `poll_received` on Tecno across the entire 30 min**. Side-finding only (not scoped): Emu hit Sprint 2b-C `OpkNotFound action=fall_through_to_hold` → `fail_mac action=hold` on 2 of 3 messages (`f8b8bff8` + `5f86fce8`); first bootstrap message `1efed06e` decrypted OK. Side-finding only: analysis-script `DELIVERY OK` label was misleading because it counted only sender-side `relay_send_return ok=false`; future K-runs need to also count receiver-side `inbound_deliver` / `DECRYPT_TRACE` / `fail_mac` / `breaker_open` to avoid the same false-positive classification. Evidence at `~/Downloads/direct-wss-mode2-k2-tecno-lte/` with SHAs. Phase B BLOCKED pending TELE2-LTE-REST-BREAKER-RECON1 closure (K-1 Wi-Fi alone is not sufficient because PR #330's quiescence contract targets Tele2 LTE Mode 2 primarily). K-3 (emu Wi-Fi) NOT promoted as next instrument — would produce a third baseline but would not address the Tele2 LTE REST-fallback question. New track-doc `docs/tracks/tele2-lte-rest-breaker-recon1.md` opens TELE2-LTE-REST-BREAKER-RECON1: six hypotheses (H-OrchestratorCancellation / H-NetworkSideCancellation / H-BreakerCooldownTooAggressive / H-BreakerOpenBlocksInboundDispatch / H-Tele2LTESpecific / H-OkHttpInterruptionRace), five instruments M-1 through M-5 explicitly NOT pre-committed (M-1 = re-read existing K-2 corpus for orchestrator-state-transition timeline, M-5 = source code read, both cheap; M-2 / M-3 / M-4 conditional on M-1 + M-5 evidence), six acceptance-gate verdicts, three Park conditions. No code; no fix scope-lock; PR #330 untouched; RC #330's gating now TELE2-LTE-REST-BREAKER-RECON1 closure, NOT the broader Direct WSS / Mode 2 track directly.
- **#338 `6d7809f4`** — **REST-SEND-CONNECTIVITY-RECON1 PARKED + VPN-TRANSPORT-COMPAT-RECON1 mini-lock opened.** §10 verdict appended to `rest-send-connectivity-recon1.md`: attempt #1 INVALIDATED (Mac host VPN ↔ AVD QEMU NAT/DNS proxy contamination), attempts #2 + #3 both NOT TARGET REPRO with VPN OFF (~39 min each, on different host paths). Attempt #2 host probe: 1581 data rows / 1564 SUCCESS / 17 `ec=28` fails (SHA `80153e4f...`, file `C:\temp\smoke-pr333-baseline-i2-v2.1\host-probe.tsv`) — includes a 15-fail cluster `18:47:36 → 18:50:47Z`. Attempt #3 host probe: 1672 data rows / 1650 SUCCESS / 22 fails (SHA `15527f29...`, file `C:\temp\smoke-pr333-baseline-i2-v2\host-probe.tsv`) — includes a 6-fail cluster `21:53:10 → 21:54:03Z` and a denser 13-fail cluster `21:56:23 → 21:58:41Z`. None of these host-side fail clusters co-incident with emu / Tecno transport events (devices idle through both windows). Recon parks per §7 P-1; original 2026-06-26 evidence stays on file; I-1 hypothesis matrix (H-A REFUTED, H-B strongly supported but not final-confirmed) remains the last successful discrimination. Side-findings preserved: host-probe burst clusters on BOTH clean attempts (potential future I-3 seed if a future co-incident emu burst is caught), Direct WSS Mode 2 ping-timeout pattern (~10 hits per side per attempt; belongs to the Direct WSS / Mode 2 track), first-message 8 s prekey-fetch-timeout UX delay (matches the 2026-06-17 voice-smoke side-finding; belongs to a future DWS-UX-class follow-up). New track-doc `docs/tracks/vpn-transport-compat-recon1.md` opens VPN-TRANSPORT-COMPAT-RECON1: seven hypotheses (H-VPN-AVD-DNS / H-VPN-AVD-NAT / H-VPN-OS-DNS / H-VPN-HOST-ROUTING / H-VPN-REAL-DEVICE-AFFECTED / H-VPN-DWSS-ONLY / H-VPN-REST-ALSO), five instruments J-1 through J-5 explicitly NOT pre-committed (J-1 = host VPN ON/OFF reachability matrix first), four acceptance-gate verdicts (AVD-only / real-device-affected / path-specific / REST-also), three Park conditions. No code; no fix scope-lock; PR #330 untouched; recon does NOT block RC #330 — RC #330's gating is the Direct WSS / Mode 2 track, the principal forward direction for transport reliability work.
- **#333 `5a5ce15b`** — **RC-PREKEY-PUBLISH-DEBOUNCE-RACE implementation MERGED.** Three commit rounds, three CI passes (Android CI `28188108133` + Relay CI `28188108079` + Deploy lint `28188108105` SUCCESS on merge head `b6be5e2b`), three review verdicts. Transport: `PreKeyApiClient` gains `PublishResult.Deferred` sealed-class variant (debounce default returns `Deferred`, not synthetic `Stored(0)`) + `forceJoinInFlight: Boolean = false` parameter (force-join suspends on `publishMutex.lock()` instead of short-circuiting). Messaging-crypto: `PreKeyLifecycleService.verifyBundleOnRelay` passes `forceJoinInFlight = true` exactly when both `signed_prekey_age_days == null` AND `remaining_opks == 0` (Inv-ForcePathOnZeroRecord verbatim, relay-contract source-verified). Per-session budget `MAX_FORCE_REPUBLISH_PER_SESSION = 5` + `forceRepublishBudgetMutex` cap on the verify-driven path. New `PREKEY_TRACE upload_deferred` line replaces the pre-fix false-success `upload_ok stored_opks=0`. `MigrationManager.PublishResult.Deferred` arm throws explicit `PublishUnexpected`. Five new tests in `PreKeyLifecycleServiceTest` (deterministic race-repro chain, force-flag on zero record, AND-condition negative test, bootstrap default-false, budget exhaustion) + two new tests in `PreKeyPublishReliabilityTest` (debounce default returns `Deferred`, force-join mutex wait). Six historical `PreKeyApi` fakes updated to match interface signature change. Field-smoke baseline replay 2026-06-26 on `b6be5e2b` APK SHA-256 `3d3317bd184c464337a6dd6e59bc2571eccbe659953b5e458a6802456decfb54` REPRODUCED the exact mini-lock race scenario and confirmed the fix path (Tecno timeline: bootstrap publish #1 in flight → concurrent `verify_status spk_age_days=null opks_remaining=0` → `verify_republish_triggered` → bootstrap completes → force-republish #2 waited on mutex and completed `attempt=1`; subsequent verify cycles see `spk_age_days=0` with `opks_remaining` decrementing 40 → 39 → 38; pre-fix `upload_ok stored_opks=0` from debounce absent). Full user-visible baseline NOT PASS due to a SEPARATE blocker: emulator outbound TCP to `relay.phntm.pro/65.108.154.152:443` failed mid-smoke with 5×5006 ms `SocketTimeoutException` then `send_fail_giving_up total_elapsedMs=53110 attempts=5` and `SEND_TRACE relay_send_return ok=false` for message `ac074425`; same destination produced `ws_auth connectFailed` and `op=poll connectFailed` on the emulator within the same window. This is sender-side transport / connectivity failure on the emulator, independent of the prekey path that #333 touches. PR #330 stays Draft / HOLD pending the next track.

**Phase 2 outcome (Vladislav-locked):**

- **Mode 1 (Wi-Fi 8-pong rhythm) — H-A confirmed (return-path loss).** Across both Wi-Fi captures: 11 Mode 1 deaths, 10 with relay-side `ws_protocol_pong_sent` anchor, **0 inbound TLS records** on device pcap within ±2 s around relay's send-time + RTT. Inv-PcapDoesNotMaskMode verified (Arm P3 Wi-Fi control 5 deaths × 8 pp × ~150 s, matches Phase 1 v9 baseline within tolerance).
- **Mode 2 (Tele2 LTE severe 0-1-pong rhythm) — H-A confirmed for pp=0 sub-case (3 deaths); TCP-layer ambiguous for pp=1 sub-case (36 deaths).** Both sub-cases share the same operational implication: link Tecno ↔ relay is unreliable. Inv-PcapDoesNotMaskMode verified (Arm P3 Tele2 control 23 deaths × 0-1 pp × 30-45 s, matches Phase 1 v11 Arm A baseline).
- **Rules out (Phase 1 + Phase 2 combined):** Ktor adapter as primary cause; OkHttp internal mis-handling as a primary or contributing mechanism (H-B/C/D refuted by Tier 1 raw wire — no Mode 1 or Mode 2 death has inbound TLS records present at the expected anchor).
- **Rules in:** Direct WebSocket on Tecno → relay path is unreliable on both Wi-Fi and Tele2, with different severity profiles per network. The root cause is **below app/Ktor and manifests as an unreliable TCP/TLS path; not proven to be an OkHttp/Ktor counting bug**. Mode 2 pp=1 sub-case leaves a relay-side TLS/WS delivery-stall candidate that Tier 1 evidence does not exclude.
- **3.2b.1 unpause decision per §24 acceptance gates:** **3.2b.1 unfreezes as UX-protection for both modes.** Inv-NoChangeUntilEvidence (Phase 1 mini-lock §3) is satisfied. Mode 1 closed as return-path loss; Mode 2 closed as unstable TCP/TLS path with mixed sub-cases.

**Track state:**

- RC-DIRECT-WS-DEATH1 closed (Phase 1 + Phase 2). RC-DIRECT-STABILITY1 opens as the primary fix track per strategic pivot.
- 3.2b.1 commonMain code path stays **unfrozen but parked** behind RC-DIRECT-STABILITY1 outcome per `Inv-NoSpinningUntilEvidence`. No 3.2b.1 design Council session and no 3.2b.1 code while RC-DIRECT-STABILITY1 has Arms still open.
- ADR-028 (Direct Stability Architecture Intent) shipped in this PR fixes the 4-layer reliability architecture as the durable contract for future stability / transport / privacy decisions.
- Council on revised 3.2b.1 scope **deferred** until RC-DIRECT-STABILITY1 §6 verdict (PASS / PARTIAL / FAIL) lands. If PASS, 3.2b.1 is deprioritised; if PARTIAL or FAIL, 3.2b.1 Council runs as next-session work.
- Mode 2 pp=1 TCP-layer mechanism discrimination parked per RC-DIRECT-WS-DEATH1 §38 — may partially close as Arm A bypass side-effect.
- Next code/relay PR after this lands: **PR-3 = Arm A** (compose-file loopback delta + `RcDirectArmA.kt` diagnostic class, both gated by PR #273 CI guard + new `Inv-BypassIsLoopbackOnly` + double-gated `DEBUG_BYPASS_URL`).
- `CHIP1` parked at `78bd979e`.

**Three parallel tracks unlocked by audits + v10 evidence (unchanged):**

- `RC-DIRECT-WS-DEATH1` — Phase 1 closed this entry; Phase 2 PCAPdroid next session
- `RC-REALITY-PROBE1` — why Reality `/health` probe times out at 20 s on Xray SOCKS on RU ISPs (unchanged, not started)
- `PR-UI-CONNECTION-LABELS1` — release-mode hide of "Tor" / "Reality" / "Direct" / "REST fallback" labels in both main UI/banner and notification text (unchanged, not started)

**Next session = Phase 2 mini-lock section** appending to `docs/tracks/rc-direct-ws-death1.md` planning PCAPdroid capture with two mode-specific targets (Mode 1 watching 9th Pong ~120 s in; Mode 2 watching 1st-2nd Pong at session open).

**Previous (2026-06-02 tue):** **✅ PR-WS-HEALTH-STATE1 transport diagnostics + adaptive-validation design track shipped through design phase (5 PRs in 4 days).** Master was `f692cdcf`.

Shipped:
- **#259 `8727031f` (2026-05-31, sun)** — Commit 3.3 ping/pong diagnostics (client + relay, NO behaviour). Field-verified Test #83 v7: relay sent 9th Pong, client OkHttp `okhttp_successful_ping_pongs=8`. What v7 proves: NOT a relay-side pong-send bug. What v7 does NOT prove: whether the 9th Pong physically reached the device's socket buffer and was mis-counted by the client stack, OR was lost on the return-path between relay and device — both possibilities feed `RC-DIRECT-WS-DEATH1` and require packet capture to discriminate. Proximal kill mechanism is OkHttp `RealWebSocket.writePingFrame()` firing on the next ping after a missed pong.
- **#260 `fde7267f` (2026-05-31, sun)** — Commit 3.2a design note (telemetry-first, no action).
- **#261 `82103ad0` (2026-06-01, mon)** — Commit 3.2a code: `WsDegradationDetector` + `WsDegradationCollectorBindings` + telemetry under `TransportRewalkCoordinator` log tag. Field-verified Test #83 v8 PASS on Tecno Tele2 LTE; 4 sessions died at 140 s rhythm, detector emissions match wire-confirm equalities.
- **#262 `ad3476ae` (2026-06-01, mon)** — Commit 3.2b design rev1→rev3: Adaptive Path Validation reframe after LLM Council 5-lens + v9 (Ростелеком Wi-Fi control, identical 140 s rhythm — carrier-independent) + v10 (Reality `/health` probe 20 s timeout, chain fallthrough to Tor 5 min cold-start, Direct REST proven 564 ms `/send`). Locked: no automatic rewalk; Direct stays in chain; emergency switch only if Direct REST itself fails AND validated alternative ready; `TransportCapabilitiesResolver` (PR-C1) remains sole calls gate; Tor delivery-validated does NOT unlock calls.
- **#263 `f692cdcf` (2026-06-02, tue)** — Commit 3.2b design rev4+rev5: external-architect audits absorbed (Codex memo + "Глобальный аудит транспортов мессенджеров" PDF). Three audit-converged contract corrections (drop strict 60 s `RestHealthMonitor` window; drop `≥1 inbound frame` validator criterion; introduce `TransportPathPreparer` lease pattern instead of touching `TransportManager` private state) + five self-review cleanups (D9 backlog trigger deferred to parking lot; commonMain/Android boundary made explicit; gates #5 + #7 rewritten to match new contract).

**Track state:**
- 3.2b.1 commonMain code (BackgroundPathValidator + RestHealthMonitor + WsAdaptiveActionPolicy + TransportPathPreparer + tests) is **DESIGNED but PAUSED** pending RC-DIRECT-WS-DEATH1 evidence.
- 3.2b.2 androidMain wiring is sequenced after 3.2b.1.
- `CHIP1` remains parked at `78bd979e` until transport stabilisation completes.

**Three parallel tracks unlocked by audits + v10 evidence:**
- `RC-DIRECT-WS-DEATH1` — 6-arm experimental matrix to localise where the 9th Pong is lost (current Ktor baseline / raw OkHttp `newWebSocket(...)` / Ktor `webSocketRaw` / app data-frame heartbeat diagnostic-only / packet capture on Tecno + relay logs / emulator + second handset parity). Hypothesis space: Ktor adapter masking control frames vs return-path loss vs Tecno Android stack vs MTU.
- `RC-REALITY-PROBE1` — why Reality `/health` probe times out at 20 s on Xray SOCKS on RU ISPs (Xray subsystem ready in ~0.5 s, so the stall is downstream). Layered probe split: SOCKS ready / TLS-ish egress / app relay health.
- `PR-UI-CONNECTION-LABELS1` — release-mode hide of "Tor" / "Reality" / "Direct" / "REST fallback" in BOTH main-UI/banner AND foreground notification text.

**Next session = `RC-DIRECT-WS-DEATH1` mini-lock design note. NOT 3.2b.1 code.** Both audits + Vladislav independently converged: localise root cause before building adaptive-validation scaffolding. If root cause turns out to be a localised OkHttp/Ktor config or Tecno-stack fix, 3.2b.1 may be deprioritised. If root cause is server-side / Hetzner / MTU, 3.2b.1 becomes a necessary UX shield.

**Previous (2026-05-29 fri, late):** **✅ Out-of-queue double-ship LIVE on phntm.pro:** PR #245 `3109126f` (funding.json Liberapay capitalization + BMAC handle typo fix — `phantom-messenger` → `Phantom-messenger` capital P, `phantommessenger` → `phantompro`) + PR #246 `dd0b3dce` (full phntm.pro site replacing the `noindex,nofollow` stub landing). Single-sweep deploy on Hetzner VPS: one `git pull` + `docker compose up -d --force-recreate caddy` + one Cloudflare Dashboard purge closed both PRs synchronously. Four `curl` verify checks all PASS on the live host: robots meta now `index,follow`, zero `noindex`, funding.json shows corrected donation URLs, all three inner pages (`about` / `roadmap` / `donate`) return HTTP/2 200. **What landed in the site PR:** 4 self-contained bilingual EN/RU HTML pages (`index` / `about` / `roadmap` / `donate`) with CSS + JS inlined and logo embedded as base64 data URI, browser-language autodetect + manual switcher with localStorage persistence, scroll-reveal + hover-lift animations (`prefers-reduced-motion` respected), clipboard-copy for BTC / XMR / ETH addresses, explicit `<meta name="robots" content="index,follow">` in every `<head>` baked into both committed HTML AND `.build/build_*.py` regen scripts. **Critical structural choices** locked in code review: `site/static/` (NOT `assets/`) to avoid collision with Caddy's existing `handle_path /assets/*` route for legal pages; `site/.build/` (NOT `_build_scripts/`) to leverage Caddy `file_server`'s dot-prefix hiding. **Roadmap security wording precision fix** during final pass: drafts' "Eight P1 security findings closed" was not verifiable against ADR-023 / ADR-024 / ADR-027 evidence (which cover 4 findings across 3 ADRs) — reworded to "Multiple P1 cryptographic findings resolved: prekey wrap, ratchet state wrap, signed-challenge authentication" in both `roadmap.html` and `.build/build_roadmap.py`. **docker-compose** edit was surgical: one line in `services.caddy.volumes` (`./landing:/srv/landing:ro` → `../site:/srv/landing:ro`); container path `/srv/landing` unchanged so Caddyfile root path stayed correct, no Caddyfile edit required. Old `deploy/landing/` directory preserved in git history as rollback target. **Process gates fired:** Rule 8 carve-out (docs/site/funding-typo) on both PRs; Rule 9 typo carve-out on #245, explicit 7-section grep-verified checklist + CI 3/3 + Vladislav-explicit "го" greenlight on #246. **Stabilization Sprint queue unchanged: next is PR-UI-CHAT-NEW-MSG-CHIP1** (position #3, mini-lock at `docs/tracks/chat-new-msg-chip.md`).

**Previous (2026-05-29 fri):** **✅ PR-CRYPTO-SESSION-REPAIR1 MERGED (PR #243 squash `a292ac4f`), 48/48 DMS + 56/56 other messaging + 52/52 storage + Android assembleDebug all green; CI 3/3 (build-and-test, build-test, lint) pre-merge.** Eight architect ACK rounds preserving the verification trail under WORKING_RULES Rule 9 (commits 1+2 plumbing → 3a hold branch → 3b/3c 5-test invariant matrix → 4 fresh-X3DH-on-suspect → 5 replay loop → 5a Replay Safety Patch → 5b Replay Ratchet Commit Ordering → 6 24h TTL) plus an explicit final-merge ACK. Closes the silent-message-loss class first hardened in agent memory as `bug_force_stop_ratchet_corruption_2026_05_27.md`: on Tecno after force-stop / re-install cycles, the receive path used to ack-deliver MAC errors and write `processed_envelopes.FAILED_MAC` — the message was silently lost and the only working remediation was `pm clear phantom.android` (identity wipe). **Production behavior unchanged** — the new path is gated on `holdMacFailures = phantom.android.BuildConfig.DEBUG` wired in `AppContainer.kt`; release APKs keep the existing ack-on-MAC + FAILED_MAC code path completely intact, debug / beta APKs run the hold → suspect → fresh X3DH → bounded-replay → 24h-TTL cycle. **Final shape of the replay loop after 5a + 5b**: decode WireFrame → load fresh session → `ratchet.decrypt` IN MEMORY → decode `MessagePayload` → require `TYPE_MESSAGE` → insert text row with disappearing-timer expiry → **`saveSession(newState)`** → upsert conversation + emit `_incomingMessages` + notification callback → markProcessed → ack → delete held row. Failures at steps 1–6 return BEFORE `saveSession`, leaving the receive chain un-advanced so the same row remains decryptable on the next replay cycle. **Architect invariants enforced and test-covered** (15-test replay catalogue, all green): outer per-envelope guard (no exception escapes back into `encryptUnderLock`), strict step order with insert as durability gate, anti-loop guarantee (no failure path re-sets `session_suspect`), 3-attempt diagnostic cap + 24h primary TTL, per-conversation scope inside `mutexFor(conversationId)`, disappearing-timer mirror, complex-payload tightened to keep-held + ratchet-un-advanced (commit 5's earlier ack-and-delete on complex would have silently discarded payloads the user never sees). **Test #87 manual MAC repro honestly documented as not reliably reproducible on demand** — the PR's correctness rests on the 48-test invariant matrix; post-merge observation point is a row appearing in `decrypt_failed_envelopes` on any debug / beta install. **Stabilization Sprint queue advances to position #3: PR-UI-CHAT-NEW-MSG-CHIP1** (branch `feat/pr-ui-chat-new-msg-chip1` exists with the early scaffold; mini-lock at `docs/tracks/chat-new-msg-chip.md`; designer handoff at `phantom-messengers/project/Scroll-to-bottom.html` with all tokens locked).

**Previous (2026-05-28 thu, late):** **✅ PR-LTE-NETCHANGE1 MERGED (PR #241 `899d45bd`), Test #88 functional PASS on Tecno Tele2 LTE.** Three architect review rounds + three additive commits (`61b4da0b` initial impl → `ea9b74a4` 3 P1 fixes → `b340eba5` P2 fix + KDoc nit) preserving the verification trail under WORKING_RULES Rule 9. Critical Scenario B verified end-to-end: `NETWORK_TRACE changed old=WIFI new=CELLULAR resolvedReason=WIFI_TO_CELLULAR` → `rewalk_start` → `service_restart_received` → `generation_claimed gen=2` → fresh `PROBE_TRACE chain_start ordered=[Direct, Reality, Tor]`. Closes the dead-`Event.NetworkChanged` handler gap empirically reproduced in PR #240 baseline. All five mini-lock steps shipped: `NetworkChangeObserver` (single NetworkCallback + 1500 ms debounce + fresh-snapshot read + 4-axis classifier) + `TransportRewalkCoordinator` (8-step rewalk owned outside Hybrid) + `HybridRelayTransport.submitNetworkChangedEvent()` narrow one-liner + `PROBE_TRACE reality_filtered reason=vpn_active` line + `AndroidNativeOkHttpDirectProbe` phase event listener (mirrors Ktor probe). Three architect guardrails enforced in code: ownership separation, debounce-then-fresh-snapshot, `networkPresent=false→true` bypasses rate-limit. Service re-entry contract via `EXTRA_REWALK_RESTART` intent extra + `connectGeneration: AtomicLong` token guards CAS cleanup race (P1-3). MAC error did NOT recur. **Stabilization Sprint queue advances to position #2: PR-CRYPTO-SESSION-REPAIR1** (mini-lock at `docs/tracks/crypto-session-repair.md`).

**Previous (2026-05-27 wed):** **✅ PR-RECV-DIAG1 MERGED (PR #234 `684c2be6`), Test #86 PASS on Tecno.** Eight-commit diagnostic-to-fix arc on `diag/pr-recv-diag1` that started as a "messages stopped arriving on Tecno" investigation and ended with three independent fixes: **v1.7 (the main one)** — `TransportManager.reorderChain()` was hoisting ANY `lastWorkingTransport` to first position, so a single successful Tor fallback locked the client to a 24h sticky preference and Direct was never tried again; fix saves the hint only when `kind == strategy.chain.first()` and clears non-primary hints on load. **v1.8** — inbound-stall (60s no-frames on an open WS while REST capability=false) now calls `maybeRetryBootstrap()` instead of returning silently; closes the case where the initial REST bootstrap failed at app start and there was no recovery path. **v1.6** — new `Event.InboundIdleTimeout` in `RestStateMachine`, `KtorRelayTransport.startIdleWatchdog` emits `InboundStalledEvent` once per session at `INBOUND_STALL_THRESHOLD_MS=60_000L`, `HybridRelayTransport` collector forwards it → `transitionToRest("inbound_idle_timeout")` → REST poll picks up envelopes from the mirror; catches the half-dead-WS case where TCP stays open but no frames arrive. **v1.2** — Mutex around `initMessagingFromStorage` closes the MainActivity+Service concurrent-init race that produced duplicate `transport.incoming` subscriptions. **Pre-merge audit:** branch name, all 8 commits, full diff of all 12 changed files — clean of Russian; PR body had Russian (drafted in chat language), rewritten in English before merge per Vladislav-explicit rule (English in all code/branches/PR descriptions, Russian only in chat). **New follow-ups recorded:** **PR-CRYPTO-SESSION-REPAIR1** (inserted at the head of the next-session queue before resuming UI work) — stale Double Ratchet state after force-stop cycles produces `Permanent decrypt failure (MAC error)` → `ack_deliver_send` → silent message loss; current only working remediation is `pm clear` (full wipe); architect-designed 4-step plan: stop ack-deliver on MAC error in debug/beta + DECRYPT_TRACE logs + session-repair path + non-destructive "repair without wipe" capability. **Network change detection** + **WS+REST duplicate envelope cleanup** also deferred. **Key lessons captured in agent memory:** (1) sticky fallback hint must not become primary; (2) never disable WS heartbeat as a diagnostic A/B — it's the trigger for `WsSessionEnded` which drives the existing auto-fallback; (3) logcat tag coverage matters — `TransportManager` logs are under tag `TransportManager`, NOT `PhantomTransport`, and missing tags produce false "code didn't run" verdicts; (4) verify architect / sub-agent claims with `grep` + code read before pushing (v1.5 episode where I shipped a doc-only constant claiming it added app-level Ping); (5) force-stop cycles can corrupt Double Ratchet local state to the point that only `pm clear` recovers. **Stabilization Sprint queue updated.** CHIP1 on HOLD on `feat/pr-ui-chat-new-msg-chip1` branch — not regressed by this PR but should resume only after CRYPTO-SESSION-REPAIR1 lands.

**Previous (2026-05-26 tue, very late):** **✅ PR-UI-CHAT-THREAD-CACHE1 MERGED, Test #82.1 PASS on Tecno.** PR #231 `d933b0b6` shipped Variant C: hot `ChatThreadStateHolder` (shared/core/messaging) + ChatList row-tap preload + ChatScreen snapshot-seed + bottom-anchor LazyColumn (reverseLayout=true) + pinned banner OUTSIDE LazyColumn + `CHAT_CACHE` + `CHAT_LIST open_state` logs. Logs from Test #82.1 verified `preload_done` precedes `ChatScreen subscribed`, `snapshot_hit` lands before first compose frame, `firstVisible=0` confirms visual bottom on every open, latest message visible immediately on ChatList tap with NO manual scroll. THREE chat-list lifecycle PARKs (#217, #226, #228) → ONE PASS (#231) on the Variant C escalation. Stabilization Sprint queue advances to item #3 (#2 RENDER-PERF1 is conditional and not currently needed — CACHE1 alone closed the data lifecycle). **Next: PR-UI-CHAT-NEW-MSG-CHIP1** — floating scroll-to-bottom button with new-messages count badge, 1:1 with Vladislav-designer handoff (`phantom-messengers/project/Scroll-to-bottom.html`). Mini-lock at `docs/tracks/chat-new-msg-chip.md` with all design tokens locked (44×44dp circle, 14dp anchors, cyan #00D4FF badge, MONO 10sp tabular figures, enter 180ms / exit 140ms / badge bump 220ms with locked cubic-bezier curves, tap = scroll-to-first-unread). Manual scroll jerks (still observed during Test #82.1) deferred to `PR-UI-CHAT-RENDER-PERF1` (queue item #2, now CONDITIONAL — fire only if Vladislav still feels them after queue item #5).

**Previous (2026-05-26 tue, late):** **🔥🔥🔥 Android Stabilization Sprint mode ENGAGED + 5-PR queue locked** by Vladislav after three chat-list lifecycle FAILs (AUTOSCROLL1 + BOTTOM-ANCHOR1 + THREAD-STATE1). **Feature freeze on Android core for the duration of the sprint:** no iOS port, no calls feature expansion, no groups beyond Alpha-0, no new transports beyond the WSS+REALITY+Tor+REST quartet, no attachments, no username directory. Only stabilization work in this order:
1. **PR-UI-CHAT-THREAD-CACHE1** — hot StateFlow holder → eliminate chat-open black wait. Mini-lock `docs/tracks/chat-thread-cache.md`. **Next to start (CACHE1).**
2. **PR-UI-CHAT-RENDER-PERF1** — CONDITIONAL escalation only if CACHE1 ships with `snapshot_hit count=N` log before first compose frame AND black wait still >50 ms. Then bottleneck = render cost (MessageBubble / AnimatedVisibility / Canvas), NOT a 4th data-lifecycle variant.
3. **PR-UI-CHAT-NEW-MSG-CHIP1** — floating "↓ N new messages" chip when user scrolled up + incoming arrives.
4. **PR-NOTIF-POLICY1** — conversation-level notification + InboxStyle + unread count + clear-on-open. Mini-lock `docs/tracks/notif-policy.md`.
5. **PR-D1e first-message bootstrap** — close 10–20 s delay + yellow-dot after Add Contact.

Sprint exits when all 5 queue items have PASS verdicts on Tecno real device. Until then, NO new Android feature work. Vladislav-locked 2026-05-26 (memory: `feedback_android_stabilization_sprint.md`). Funding-track work allowed out-of-queue only on explicit per-PR Vladislav greenlight (it's external/orthogonal to Android stabilization).

**Previous (2026-05-26 tue):** **PR-UI-CHAT-THREAD-STATE1 PARKED** per `docs/WORKING_RULES.md` rule 4 (TWO implementation attempts FAIL on Test #81 + #81.1 Tecno real device). PR #228 closed without merge. The Flow-backed source-of-truth migration was architecturally correct — all secondary acceptance criteria PASSED (incoming auto-updates, send auto-refreshes, no manual reload required, anti-pattern grep clean, CI green). But the SqlDelight cold-Flow first emission itself takes **0.8–1.3 s on Tecno with 30+ messages**: `collectAsState(initial = emptyList())` renders ChatScreen with empty list first, `LazyColumn(reverseLayout = true)` anchors to that empty state, and the black wait persists. v1.1 fix (`firstFlowEmitReceived` defer for markConversationRead + profile-card-send) eliminated `SEND_TRACE` from the blocking path but could not fix the Flow-emit gap itself. **Replacement track: PR-UI-CHAT-THREAD-CACHE1** (Variant C escalation per Vladislav-architect: hot `StateFlow<List<MessageEntity>>` held by an AppContainer-scoped `ChatThreadStateHolder`; ChatScreen reads `holder.snapshot(conversationId)` synchronously as initial value; preload triggered by ChatList row tap before navigation). Mini-lock at `docs/tracks/chat-thread-cache.md` with the architect's 6 questions answered (location, API, preload sites, cache policy LRU 8, zero DB/schema changes, minimum PR scope) + new test #82 acceptance + new anti-pattern signatures (cold-Flow + empty-initial is now an anti-pattern). **Rule 4 fires for the THIRD time on chat-list lifecycle** (AUTOSCROLL1 + BOTTOM-ANCHOR1 + THREAD-STATE1 ALL FAIL) → three is the limit, no fourth architectural variant without explicit Vladislav approval. Side-issue from Test #81.1: no "↓ new messages" chip when user scrolls up and incoming arrives → separate track `PR-UI-CHAT-NEW-MSG-CHIP1` after CACHE1 lands. Lesson hardened in agent memory as `feedback_chat_list_lifecycle_three_fails.md` (supersedes the two previous chat-list memories as primary guidance).  
**Previous (2026-05-25 mon):** **PR-UI-CHAT-BOTTOM-ANCHOR1 PARKED** per `docs/WORKING_RULES.md` rule 4 (THREE implementation attempts FAIL on Test #80 / #80.1 / #80.2). The `reverseLayout = true` architectural direction was not wrong — it correctly bottom-anchored item order. But ChatScreen still pull-loaded history into local state AFTER first compose, so Compose anchored LazyColumn to empty-state and either refused to follow or pile-up-animated history. No LazyColumn-layer tweak fixes this; the data source has to be reactive. **Replacement track: PR-UI-CHAT-THREAD-STATE1** (Variant A+ per Vladislav-architect: `MessageRepository.observeMessages()` Flow source via existing SQLDelight `asFlow` infrastructure + bottom-anchored render combined in one PR). Mini-lock at `docs/tracks/chat-thread-state.md` with Vladislav-locked 8 acceptance scenarios + mandatory pre-implementation checklist. Second time WORKING_RULES rule 4 fires in operation; rule is load-bearing.  
**Previous (2026-05-24 sun, late night):** **PR-UI-CHAT-AUTOSCROLL1 PARKED** per `docs/WORKING_RULES.md` rule 4 (two architectural attempts on the same track → park and redesign). PR #217 closed without merge. Test #79 + Test #79.1.1 both FAIL on UX: first attempt (`14c7f2aa`) targeted `messages.lastIndex` which undershoots the real LazyColumn last index because the column has E2EE prefix row + optional pinned banner + day separators on top of the messages; second attempt (`3d15615a`) targeted `listState.layoutInfo.totalItemsCount - 1` correctly but still produced a 1.8–2.3 second delayed jump — user saw black wait / mid-history flash before the chat snapped to the bottom. **Architectural, not bandaid-fixable.** Replacement track: **PR-UI-CHAT-BOTTOM-ANCHOR1**, mini-lock at `docs/tracks/chat-bottom-anchor.md` requiring `LazyColumn(reverseLayout = true)` + reversed item order + mandatory pre-implementation item-order mapping (per Vladislav 2026-05-24, to prevent reversed-date-separator class bugs). First time WORKING_RULES rule 4 fired in operation. Lesson hardened in agent memory as `feedback_scroll_to_bottom_not_chat_ux.md` — for chat UIs, `scrollToItem` after loading is NOT equivalent to bottom anchoring.  
**Previous (2026-05-24 sun, night):** Funding-track CLOSED for the day. Final iteration: PR #223 `247e3924` shipped the hybrid FLOSS/fund verification (both `webpageUrl` → `https://phntm.pro` for hostname-match auto-verify + new `.well-known/funding-manifest-urls` file in repo + `wellKnown` field on `repositoryUrl` + schema bump to v1.1.0 with `$schema` URL). Deployed via the canonical `git pull && docker compose up -d --force-recreate caddy` + Cloudflare cache purge. **FLOSS/fund badges went green per Vladislav verdict.** 9 PRs total for the funding-track today (#215 + #216 + #218 + #219 + #220 + #221 + #222 + #223 + close-PR), 3 VPS deploy cycles, 4 memory entries hardened (Wyoming jurisdiction, single-file bind-mount `--force-recreate`, FLOSS/fund crypto type=other, FLOSS/fund hybrid verification pattern). Next-session start unchanged: PR-UI-CHAT-AUTOSCROLL1 awaits Test #79 verdict on Tecno.  
**Previous (2026-05-24 sun, very late):** Third out-of-queue funding-track hotfix landed + deployed: PR #221 `a938ce6d` swapped `type: "cryptocurrency"` → `type: "other"` for the three crypto channels (bitcoin/monero/ethereum) after FLOSS/fund rejected the manifest on the schema enum. Exactly 3-line diff, CRLF preserved via byte-level Python replace. Deploy via the now-canonical `git pull && docker compose up -d --force-recreate caddy`. Live verified: `cryptocurrency` count 0, `other` count 3, JSON valid. URL https://phntm.pro/funding.json again submittable. Schema lesson hardened in agent memory as `feedback_floss_fund_schema_other.md` — fundingjson.org enum for crypto is `other`, NOT `cryptocurrency`. Next-session start unchanged: PR-UI-CHAT-AUTOSCROLL1 awaits Test #79 verdict.  
**Previous (2026-05-24 sun, late):** Two more out-of-queue PRs landed + deployed end-to-end: PR #218 `14236dd0` (Caddy route + single-file bind-mount serving funding.json at https://phntm.pro/funding.json — HTTP 200, Content-Type: application/json, CORS open, 1h cache) and PR #219 `073bc70f` (hotfix replacing two literal Cloudflare `[email protected]` placeholders left over in PR #215 — root cause was copy-from-HTML-with-cf-email-protection, not Anthropic PII redaction; byte-level Python fix preserved CRLF). Deploy on Hetzner production VPS via `git pull` + `docker compose up -d --force-recreate caddy`. **Lesson captured in agent memory:** single-file Docker bind-mounts require `--force-recreate` after `git pull` (new file inode is not picked up by the still-running container that mapped the old inode at create time). New row 10 in consolidated queue: PR-UI-SUPPORT-SCREEN (Settings → Support PHANTOM, Vladislav-asked but explicitly "это потом"). FLOSS/fund submission unblocked — manifest LIVE at https://phntm.pro/funding.json with `legal@phntm.pro` as contact. Next-session start unchanged: PR-UI-CHAT-AUTOSCROLL1 still queued, mini-lock at `docs/tracks/chat-autoscroll.md`, awaiting Test #79 verdict.  
**Previous (2026-05-24 sun, earlier):** Out-of-queue: PR #215 `789c3c9e` shipped `funding.json` (Wyoming USA, Liberapay+BMAC+BTC/XMR/ETH+bank-wire, 5 active plans totalling iOS hardware $2k one-time + audit $15k one-time + runway $60k/yr + infra $1.2k/yr + legal $1.5k/yr, `history: []`) plus `README.md` `## Funding` rewrite (old "Funding & sustainability" philosophy paragraph dropped per grant-positioning consistency; new short 3-line section with explicit Wyoming jurisdiction line). `curl -I https://raw.githubusercontent.com/LiudvigVladislav/Phantom/master/funding.json` returns HTTP 200, Content-Length 6378 — URL ready for FLOSS/fund submission. Wyoming confirmed by Vladislav against Articles of Organization (memory `project_willen_llc_wyoming_2026_05_24.md`); replaces the old "Delaware" line which was a historical typo and never matched reality. Out-of-queue, locked sequence unchanged. Next-session start (Vladislav greenlit 2026-05-24): **PR-UI-CHAT-AUTOSCROLL1** per `docs/tracks/chat-autoscroll.md` — fix `ChatScreen` not auto-scrolling to bottom on open when unread messages arrived in the background (secondary finding from PR-NOTIF-DIAG Test #78).  
**Previous (2026-05-23 sat):** PR-NOTIF-DIAG closed: `a0484602` (#213). One commit, four files (`PhantomNotificationManager.kt` + `PhantomApplication.kt` + `AppContainer.kt` + `DefaultMessagingService.kt`), +295 / −39 lines. Test #78 PASS per architect verdict. Full notification chain visible end-to-end across 8 incoming events on Tecno HiOS SDK 31 (Wi-Fi); no `callback_threw`, no `invoke_threw`, no `SecurityException`. **Primary finding:** all notifications for one conversation share `notificationId = conversationId.hashCode() = 687143777`, so Android replaces each new arrival in the same slot — that's the perezatiranie behaviour Vladislav reported as "notifications sometimes disappear". Pipeline is fine, it's a notification *policy* decision. Tracked as PR-NOTIF-POLICY1 (mini-lock at `docs/tracks/notif-policy.md`). **Secondary finding** (unrelated): `ChatScreen` doesn't auto-scroll to bottom on open when there are unread messages — UI bug, tracked as PR-UI-CHAT-AUTOSCROLL1 (mini-lock at `docs/tracks/chat-autoscroll.md`). Fourth PR end-to-end under `docs/WORKING_RULES.md` (REC3 → PR-DOC-HONESTY → REC-FOLLOWUP → NOTIF-DIAG). Locked next-session start (architect recommendation): **PR-UI-CHAT-AUTOSCROLL1** because it's the more user-visible bug Vladislav already caught by hand, followed by **PR-NOTIF-POLICY1**.  
**Previous (2026-05-22 fri):** PR-UI-REC-FOLLOWUP closed: `a625bde7` (#210). One commit, one file (`ChatScreen.kt`), +92 / −5 lines. `MediaMetadataRetriever`-derived duration replaces the 100 ms ticker as source of truth (helper `readAudioDurationMs(file)` + `tickerMs` retained as diagnostic fallback). New empty-voice gate inside `finalizeAndSendVoice` (`MIN_SENDABLE_VOICE_DURATION_MS=700` AND `MIN_SENDABLE_VOICE_BYTES=1024`). Test #77 PASS: ticker undercount confirmed and fixed — 11.3 s hold ticker=9900 → metadata=11118; locked-send wall-clock ~14 s ticker=12600 → metadata=14038. Group voice path explicitly out-of-scope per mini-lock; tracked as a separate follow-up. Third PR end-to-end under `docs/WORKING_RULES.md` (REC3 → PR-DOC-HONESTY → REC-FOLLOWUP). Locked next-session start: **Notifications diagnostic PR** (Vladislav reported flakiness during Test #75; needs diagnostic before fix attempt).  
**Previous (2026-05-21 thu, late, second close of the day):** PR-DOC-HONESTY closed: `d953b131` (#208) — single PR, three logical commits (`docs(known-issues): align with M1w/M2/REC reality (post-Alpha-1)` `1adc2d1e` + `docs(adr,timeline): advance ADRs and kill superseded deadlines` `94bace93` + `docs(log): refresh current state and consolidate Open follow-ups` `ac15797c`). ADR-011 + ADR-023 advanced to Accepted with grep-verified implementation footprint. KNOWN_ISSUES expanded with six new entries (ISSUE-018..-023) covering Tele2 Layer A/B, single-relay media ceiling, native OkHttp pattern, first-message bootstrap delay, receiver-side media cancel. Every calendar item killed by the 2026-05-14 strategic pivot + 2026-05-12 grants pivot marked with strikethrough + cause line. New "What works today" block in PROJECT_LOG; consolidated "Vladislav-locked order, 2026-05-21" queue at the top of Open follow-ups. Second PR end-to-end under `docs/WORKING_RULES.md`. Locked next-session start: **PR-UI-REC-FOLLOWUP** (recording-duration source fix + empty-voice race; both inside `finalizeAndSendVoice`, not at the gesture layer).  
**Previous (2026-05-21 thu, late):** PR-UI-REC3 SwipeCancel state visual closed: `770e61f4` (#205 REC3 initial render + REC3.1 single-row hint layout / live cancel re-eval at release + REC3.2 shortened "discard" hint + animated arrow nudge + REC3.3 hide Pause/Resume during press-hold + REC3.4 stop flipping isMicHeld on swipe / chip-hiding via swipeDragLeftPx). Five iterations on a single feature branch under the new `docs/WORKING_RULES.md` one-PR-per-layer + mini-lock discipline. Test #76.6 → #76.6d PASS on Tecno real device — chip stays hidden through swipe, Pause stays hidden through press-hold, cancel arms/disarms cleanly, lock + paused render unchanged. Open follow-ups (unchanged from prior session — REC3 is pure visual layer over the already-shipped REC2.4 gesture): full doc honesty PR (KNOWN_ISSUES Tele2 Layer A/B + M1w/M2 trilogy + REC*, ADR-011/-023 status advance, killed-deadlines cleanup), recording-duration source (ticker undercounts vs MediaMetadataRetriever), empty-voice race when heldMs ≥ 700 ms but durationMs ≤ 0, re-enable M2e early manifest after relay grows receiver-side media-cancel protocol, receiver-side download-cancel needs relay endpoints, notifications-flakiness diag PR.  
**Previous (2026-05-21 thu):** Voice recorder UX hardening: `52a9773f` (#197 PR-UI-REC2 Locked + hold-to-lock + REC2.1 routing + REC2.2 lifecycle + REC2.3 ACTION_DOWN state machine + REC2.4 Locked-send routing + swipe-left-cancel + recorder crash hardening) and `b117dcb9` (#198 PR-MEDIA-UPLOAD-CANCEL1 + CANCEL2 + CANCEL2.1). 9 real-device test rounds (#76 → #76.5b) drove the iterations; both PRs PASS the final acceptance matrix.  
**Previous (2026-05-20):** — Voice UI polish day: four PRs merged in sequence — `62fa397e` (#192 Voice Bubble Matrix + midnight day-sep + text-bubble timestamp footer) → `d50c8e31` (#193 PR-M2h.1 useDownloadPool default flipped `true → false` after Test #72 caught a 10 013 ms first-pooled-GET stall on Tecno Tele2) → `b0f2b135` (#194 PR-UI-VB1 AudioBubble pause keeps waveform progress + preload duration via `MediaMetadataRetriever` on `Dispatchers.IO`) → `08ccf906` (#195 PR-UI-REC1 RecordingPanel composable wiring states 1 + 2 from the Recording Panel Matrix design with `MediaRecorder.pause/resume`, 80 ms `getMaxAmplitude` poller, 100 ms timer that survives Pause/Resume, `RecordingPanelState` enum reserving Locked + SwipeCancel for follow-up PRs). Three real-device test rounds (Test #73 on the combined M2h.1 + VBM build, Test #74 on VB1, Test #75 on REC1) — every round green on Tecno Tele2 LTE + emulator with zero `sha256_mismatch` / `decrypt_failed` / `media_chunks_gone` / `AndroidRuntime`. M2h closed as fresh-default with `setDownloadPoolEnabled(Boolean)` runtime opt-in (pool code + sticky guard + telemetry all remain so a future adaptive-per-network probe can flip the pool on without re-introducing the code). Open follow-ups: PR-UI-REC2 Locked + hold-to-lock gesture; PR-UI-REC3 SwipeCancel + swipe-to-discard drag; notification flakiness diagnostic PR (Vladislav reported on Test #75 that notifications sometimes appear and sometimes don't — pre-exists today's work, not introduced by any of the four PRs).  
**Master HEAD (2026-05-19 evening summary kept for reference):**  
**Master HEAD:** Track A complete (5 PRs ✅). Track B items 1–8 ALL merged — **Kickstarter security blockers closed**. F11+F26 signed-challenge auth production-validated on Tecno МТС + emulator with the new APK 2026-05-09. **Transport reliability mini-sprint extended to 11 PRs (#103–#113) merged 2026-05-10/12 — closed-success 2026-05-11 21:32 МТС.** Reality+VPN audited and gated, Tor staged-UX shipped, bridge profile rotation live and re-tuned per МТС data, Briar's RU-tuned bridge set imported (Snowflake-via-Google-AMP fallback). **🎉 Test #6 confirms Ghost on МТС WITHOUT VPN works** — `Online via Tor · Ghost` in ~6 minutes via KitchenSink (1/4) on the very first attempt; bootstrap reached 100 %, probe 200 OK, WS handshake successful through onion. First captured logcat proof of Ghost-without-VPN on a Russian carrier network in PHANTOM's history. **🎉 First-message reliability sprint — PR-G3 (#123) + PR-G4 (#124) merged 2026-05-13, root cause "yellow dot for two minutes on first message to a new contact" closed.** Test #30 confirms: Phone bundle-fetch went from 8009 ms × 4 timeouts to **151 ms × 1 OK**; emu publish went from 20.7 s + 39.5 s `Connection reset` to **829 ms → 201**. **PR-H1a (`3db97b49`) merged 2026-05-13** — WS observability tags `[gen=N s=M] / conn_id` + server-side `ack_deliver_*` traces; ruled out zombie-writer hypothesis and pinpointed Test #33 reorder bug. **🎉 H2 line closed 2026-05-13 evening — PR-H2a (#127, `674ce231`) + PR-H2a.2 (#128, `72e59ce9`) + PR-H2b (#129, `7008cf3e`) all merged.** Test #34 verified H2a/H2a.2 fixed wire reorder (10/10 messages delivered + visible) but surfaced two MAC errors on read-receipt envelopes from relay redelivery hitting an already-advanced ratchet chain — H2b's processed-envelope ledger guard runs BEFORE `ratchet.decrypt`, covers ALL payload types (read receipts and other control payloads were never inserted into the messages table so they bypassed the legacy `messages.id` guard), and survives `pm clear`-free forward-only schema migration (15.sqm, schema v15 → v16). **🎉 H1 line closed 2026-05-13 night — PR-H1b (#131, `0baa4196`) diagnosed half-open TCP black-hole pattern from `session_summary` lines (server `pings_received=2-5` vs client `pings_sent=11`, `since_last_ping_ms ≈ 153 s` on every dying session — middlebox NAT eviction + radio park dropping outbound packets while OS thinks socket healthy), then PR-H1c (#132, `e946caba`) shipped six-layer recovery (inbound-frame liveness, OkHttp `pingInterval(15s)`, `ping_send_failed → forceReconnect+break`, `TransportState.Reconnecting`, AlarmManager proactive at 45 s, server-side TCP SO_KEEPALIVE). Test #37 verified end-to-end: detection 155 s → 30–46 s, recovery 5 s → ~1 s, zero message loss across reconnect cycles (H2b ledger neutralises re-delivered envelopes). UX is now routine "Reconnecting…" badge instead of "lagging" outage. **🎉 H1e line closed 2026-05-14 — PR-H1e (#134, `bcc501be`) merged after a 4-run diagnostic sprint on `diag/h1e-ws-ping-experiments`. Each heartbeat layer was isolated across ≥12 reconnect cycles per device on Tecno МТС + emulator-on-dev-Wi-Fi: Run 0 (ws_ping=15s, app_ping=on) Phone 46.5s/Emu 39.5s; Run B (ws_ping=5s, app_ping=on) Phone 21.8s/Emu 29.5s; Run C (ws_ping=15s, app_ping=off) Phone 72.0s/Emu 56.4s ← winner; Run B+C (ws_ping=5s, app_ping=off) Phone 30.9s/Emu 29.6s. Two independent findings: (1) suppressing the app-level RelayMessage.Ping/Pong loop roughly doubles WS lifetime; (2) a 5-second OkHttp WS Ping is itself a kill trigger — the tighter pong window plus the cadence override the gain from (1). Production policy adopted = Run C: `APP_LEVEL_PING_ENABLED=false`, OkHttp WS Ping hard-coded to 15 000 ms, AlarmManager proactive reconnect stays enabled, dead-socket watchdog continues ticking on PING_INTERVAL_MS. Test #41 confirmed zero MAC errors / ledger-dedup misses / FIFO flush issues across all 12 reconnects per device. The `diag/h1e-ws-ping-experiments` branch is kept on origin as a reproducible archive of all four runs.** Open question for Phase 1: should client-side TCP keepalive (constants are declared in `RelayTransportConfig.kt` but **not currently applied client-side** — the comment claiming they are is being honesty-fixed in Phase 0 PR (a)) be wired in via a SocketFactory? Branch `fix/transport-tcp-keepalive` (3 commits ahead) holds a 153-line `KeepAliveSocketFactory.kt` that could provide it; decision deferred to empirical Phase 1 test results. Server-side socket2 keepalive is already live on master. **🎉 2026-05-15 Tele2 LTE diagnostic session — PR-Diag (#143, `f3925cac`) + PR-R0 (#144, `817331a7`) merged.** Test #42 on Tele2 LTE Иркутская had appeared to fail Reality probe and fall through to Tor, which was first read as "TSPU blocks Hetzner CIDR" and triggered a multi-VPS Reality pool plan. SSH server-side diagnostic + Termux curl from the same Tele2 SIM disproved that hypothesis: HTTPS GETs (`/health`, `/auth/challenge`, WS Upgrade `/ws`) all work fine from Tele2 — server-side Caddy log instead showed the real failure as `POST /prekeys/publish` with `bytes_read: 0, status: 408, duration: 30.05s, Content-Length: 13903`. OkHttp HTTP/1.1 upload body gets stuck on Android mobile carriers (same class as PR-G4's HTTP/2 stream-stuck) **and** a race condition was launching two parallel publish POSTs from independent reconnect generations at the same millisecond (visible in Caddy access log as duplicate POSTs from different ephemeral ports). PR-R0 ships a dedicated `createPreKeyPublishHttpClient()` (HTTP/1.1, `ConnectionPool(0)`, `Connection: close`, tuned timeouts), `kotlinx.coroutines.sync.Mutex` for publish debounce, 3-attempt retry with 500→1500 ms backoff, and extended `PREKEY_TRACE` logging. PR-Diag adds an OkHttp `EventListener` per-probe-step + `PROBE_TRACE chain_start / prepare_start/done/fail / probe_called/returned/outer_timeout / chain_attempt_success/failed / chain_all_failed / tor_state / xray_state` so future field tests answer "which phase died" without SSH cross-referencing. Acceptance criterion for PR-R0: Caddy access log must no longer show `POST /prekeys/publish bytes_read=0 status=408 duration=30s` from the app, and on Tele2 LTE prekey publish must complete without forcing fall-through to Tor purely on publish-timeout grounds. **Verification on Tele2 LTE Иркутская handset is pending — Phase 1 matrix (Tests #43-#47) stays paused until that retest succeeds.** Lesson logged: `bytes_read=0` + clean duration that equals client `readTimeout` is a client-side bug signature, not a network-filter signature; network filters produce more chaotic timing. **🎉 2026-05-16 — Tele2 reliability sprint Round 2 (PR-R0.2 #147 → R0.3 #150 → R0.4a #149 → R0.4b #151) and REST fallback transport (PR-D0r #152, master `1c1a91a9`; PR-D1 #153, master `6f6ade20`) all merged in one day.** Test #45 (2026-05-15) confirmed PR-R0.2 win — OPK batch 100 → 40 dropped publish body 13903 → 5863 bytes which now slips under the 8192-byte Tele2 middlebox curtain (`prekey_publish_ok status=201 elapsedMs=902`, Direct stays Direct end-to-end on Tele2 LTE Иркутская without falling to Tor). Test #48 (2026-05-16) on the R0.3+R0.4a+R0.4b APK exposed two NEW Tele2 layers that were hidden behind the prekey-publish failure: **Layer A** — WS Upgrade returns 101 but every subsequent WS Frame.Text is silently dropped upstream (server-side `pings_received=0, inbound_frames=0` across 20+ phone sessions, each ~153 s = server read timeout; phone OkHttp `pingInterval(15s)` timeout at 31 s is *symptom*, not cause); **Layer B** — `POST /prekeys/publish` body=5863b reaches server `status=201 duration=2.9ms resp=18b`, but the 18-byte response never reaches phone within 60 s (Caddy + phone log proved server sent it). PR-R0.4c (disable OkHttp pingInterval) was considered then **KILLED** as wrong direction — server-side `pings_received=0` proves phone pings never reach, disabling client-side timer just extends zombie WS lifetime. Locked direction: **REST short-poll fallback** with server-side idempotency (POST response may drop → client retries same Idempotency-Key, server dedupes). PR-D0r adds four endpoints (`/auth/session`, `/relay/send`, `/relay/poll`, `/relay/ack-deliver`), per-identity LRU idempotency cache (10 K keys × 24 h TTL), bearer token (1 h, retry-safe via `(identity, challenge, signing_pubkey, signature)` cache), `sha2::Sha256` body-hash, capability discovery in `/auth/session` response (`rest_fallback:true, max_send_body_bytes:4096, poll_max_envelopes:1`), unified WS↔REST envelope store (mirror helpers wired from both `routes.rs` WS arm and `rest_fallback.rs` paths), and `sealed_sender` preserved end-to-end through send → poll → live delivery. 10 integration tests cover all 4 review blockers including 4 NEW: replay-with-different-signature → 401, sealed_sender preservation, WS-simulated send mirrors into REST poll, WS-simulated ack clears REST poll. PR-D1 ships the Kotlin client library (commonMain `RestFallbackTransport` / `RestStateMachine` / `RestFallbackOrchestrator` + androidMain native OkHttp impl + expect/actual factory + 28 unit tests) currently **INERT** — orchestrator is constructed nowhere, capability gate keeps it dormant if relay returns `rest_fallback:false`. Wire-up into `PhantomMessagingService` lands separately as PR-D1b (architect design queued). Smoke-test against the deployed relay 2026-05-16 confirmed `/auth/session` 422 on empty body, `/relay/send` 401 no-auth, `/relay/poll` 401 no-auth — all new endpoints live, all blockers from Vladislav's first review closed. **🎉 2026-05-16/17 — Live messaging wire-up + bootstrap-ordering fix landed (PR-D1b → PR-D1c → PR-D1c.1 → PR #157 squash-merged to master `d7a05273`).** PR-D1b (#155, `e9bdf462`) wired the `RestFallbackOrchestrator` into `PhantomMessagingService` so the live messaging path actually consults the REST fallback when the state machine flips. PR-D1c (#157 first commit `bd171279` + rounds 2/3 `e09a8569` / `4a60bde2`) migrates pending WS outbox envelopes to REST on mode switch — closes the bootstrap-ordering bug where the *first* envelope of a new session was lost when the state machine flipped mid-flight. PR-D1c.1 (#158 merged into the D1c branch as `61a051f1`) fixes three connected REST session-token lifecycle bugs uncovered by Test #50: bootstrap now caches the issued token, `kotlinx.coroutines.sync.Mutex` serialises concurrent `acquireOrRefreshToken()` callers (replacing an unused `Any()` lock), `sendEnvelope()` acquires the token inside the retry loop with CAS via a `staleToken` parameter so a 401-refresh propagates to subsequent attempts. **Test #51 (2026-05-17, Tele2 LTE Иркутская on Tecno `103603734A004351` + emulator-5554) = partial PASS.** End-to-end chain proven on real hardware: `token_cached reason=bootstrap` → WS first send → 40 s ACK wait → `mode_switched WsActive → RestActive reason=active_outbound_threshold` → `migrate_pending_arm/start/send` → `token_reused reason=send` → `send_response status=201` → `migrate_pending_ok` → emulator `handleDeliver` + `Decrypt OK after bootstrap` (X3DH session created from the migrated bootstrap envelope). Six text messages each direction over ~3 minutes all delivered. Steady-state: every subsequent send/poll/ack reuses the cached token (zero extra `/auth/session` calls), `status=201` on every send, H2b ledger guard handles REST poll redelivery. **Remaining UX blockers** (next-PR scope, not D1c regression): (1) ~40 s wait for 2 active counter ticks before WS→REST switch is the dominant latency cost — to be closed by **PR-D1d fast active-outbound degrade** (10 s per-envelope ACK deadline, switch on first expiry, reset only by ACK of same envelope-id); (2) voice chunks 11-55 KB rejected by REST `max_body=4096` cap (`route_send mode=RestActive` → `send_oversize`) — to be closed by **PR-D2a** (UI gating in `Limited realtime`) then **PR-D2b** (chunked audio under cap, separate larger PR). Minor: prekey upload occasionally times out on the response (Tele2 Layer B downstream drop) — fallback via GET `/prekeys/status` queued as PR-R0.5/PR-PK1 at low priority. **🎉 2026-05-17 evening — PR-D1d merged (#160, master `3e4293c3`) closing the active-outbound latency gap.** Test #52 on the same Tele2 LTE Иркутская handset proved the 10 s per-envelope ACK deadline does what the contract said: first stuck outbound `397df3c7` armed at `+0.001 s`, expired at `+10.004 s`, `mode_switched WsActive → REST_ACTIVE reason=active_outbound_ack_timeout`, `migrate_pending_send bodyBytes=1808` → `send_response status=201 elapsedMs=625` → `migrate_pending_ok` → emulator `handleDeliver` + `Decrypt OK after bootstrap`. Total user-visible latency from send_start to relay-accepted = **~11 s** (was ~40 s on Test #51). Steady-state direction (emu → phone, healthy ACK path) cancels the deadline within ~100 ms — `outbound_ack_deadline_cancelled reason=ack_received` — no false REST switch. Six text messages each direction round-tripped over ~3 minutes on Tele2 LTE; once the orchestrator entered `RestActive` after the first migrate, every subsequent send/poll/ack stayed on REST with `status=201`. **Tele2 WS keepalive baseline unchanged:** phone-side WS continues to die every ~31 s with `pings_received=0 inbound_frames=0` — this is the Test #48 Tele2 Layer A profile the REST fallback was designed for, **not** a D1d regression. **Voice and calls are explicitly out-of-scope for D1d** (Test #52 logs contain zero `audio_chunk` / `VOICE_TX` lines). **🎉 2026-05-17 night — PR-D2a merged (#162, master `210b827f`) closing the "voice / calls on Limited realtime" UX gap as a temporary guard.** Test #53 → #53.1 → #53.2 sequence on Tecno `103603734A004351` (Tele2 LTE Иркутская) verified the contract end-to-end: in `RestActive`, mic + call buttons refuse with an English Snackbar ("Voice messages are temporarily unavailable in Limited realtime mode…" / "Calls are temporarily unavailable…"), `MEDIA_CAPABILITY blocked feature=voice|call mode=RestActive source=ui` fires under the `PhantomHybrid` tag, second-layer guards in `DefaultMessagingService.sendAudio` (`VOICE_TX blocked_limited_realtime … source=send_layer`) and `CallManager.startCall` (`CALL_TX blocked_limited_realtime … source=call_manager`) cover any path that bypasses the UI. Voice guard also tears down an in-progress recorder cleanly if the transport degrades mid-recording. Bonus evidence on Test #53.1 emulator: REST migrate path captured the real oversize case `migrate_pending_skip_oversize id=e96223a6 bodyBytes=7608 max=4096` — concrete telemetry for PR-D2b to design against. Localisation journey: D2a.1 introduced `res/values/strings.xml` (EN) + `res/values-ru/strings.xml` (RU), but Test #53.1 visual on a Tecno with RU system locale showed one RU sentence inside an otherwise EN-inline chat UI; D2a.2 dropped `values-ru/` so the Snackbar matches the rest of the inline UI on every device locale (a proper RU translation lands later as part of a centralised localisation pass for the whole UI). **Voice on REST is still not delivered — D2a only refuses politely; the real fix is PR-D2b voice-over-REST chunking** (body ≤ 4096 b, target ≈ 2.5–3 KB for headroom, ACK strictly after save, receiver reassembly by `voiceId`, single DB row after `assembly_complete`). **🎉 2026-05-17 late night — PR-D2b.1 merged (#164, master `9f1f346b`) closing the durability foundation half of D2b.** Architect-requested two-PR split: D2b.1 = durable chunk core + sender hardening (no UX gate flip); D2b.2 = open the gate + add 15-sec recorder cap + Test #54 on Tele2. D2b.1 ships: `AUDIO_CHUNK_BYTES = 8 KB → 3 KB` so envelopes pass REST `max_send_body=4096` (Test #53.1 emulator captured the real oversize `bodyBytes=7608` case); per-chunk `transport.send()` result check (failed chunks throw out of `runCatching`, local row stays `QUEUED`); new `voice_chunks` SQLDelight table + forward-only migration `16.sqm` (v16 → v17) with `PRIMARY KEY (voice_id, idx)`; durable 1:1 receive path that saves chunk BEFORE ack-deliver, then assembles + inserts (keyed on `voiceId`, `INSERT OR IGNORE`) + upserts + emits + notifies + deletes chunks; startup finalizer that resumes voices interrupted by a previous process death; 24 h TTL with `VOICE_RX partial_expired` log per evicted voice. Round 2 of the architect review added an already-inserted pre-check (so the finalizer cannot double-emit / double-notify / double-bump unread after a crash between live insert and previous `deleteByVoiceId`), `deleteOlderThan` whole-voice semantics matching `findExpiredSummaries` (pre-fix row-level sweep left mixed-age voices permanently unassemblable), and a range guard rejecting `chunkIndex >= chunkTotal` payloads. 10 storage contract tests + 5 messaging tests cover the new behaviour. Group voice intentionally stays on the in-memory path (no `groupId` column in `voice_chunks`); group durability is queued separately. **Voice on Limited realtime is still gated closed by D2a's `canSendVoice` lambda** — the durable path is wired but not yet exercised in production until PR-D2b.2 flips the gate and adds the 15-sec recorder cap. Residual risk acknowledged: `processedEnvelopeRepository.markProcessed` runs inside the decrypt mutex BEFORE `voiceChunkRepository.insertChunk`, so a crash between decrypt+ledger-mark and chunk insert would let the relay redelivery be skipped by the ledger; this is the pre-existing H2b receive-pipeline compromise, closing it requires a separate transaction redesign and is out of scope for D2b. Calls remain a **separate C-track** (PR-C1 `TransportCapabilities` typed gate replacing the `state == WsActive` shorthand; PR-C2 Reality endpoint pool + WS-handshake probe — current `/health` probe doesn't catch the Tele2 Layer A silent-WS-drop pattern; PR-C3 TURN-over-TLS:443 or custom Opus-over-Reality). See [`docs/PROJECT_LOG.md` — Open follow-ups](../PROJECT_LOG.md#open-follow-ups--unfinished-items) for the complete queue (PR-R0.5 prekey GET-status fallback, PR-D0r sealed-sender empty mirror, C1/C2/C3 call architecture, paused Phase 1 connectivity matrix, KNOWN_ISSUES Tele2 entry, etc.). **🎉 2026-05-18 — PR-M1w merged (#172, master `561de17c`) — encrypted media-upload voice messages live for 1:1 chats.** Voice on Limited realtime is now production-functional: record → encrypt (XChaCha20-Poly1305, AAD=mediaId) → chunk → upload via `AndroidNativeOkHttpMediaUploadTransport` (HTTP/1.1, fresh client per call, ConnectionPool(0), retryOnConnectionFailure(false), Connection: close, 10 s timeouts — mirrors the locked R0.1/R0.3 pattern) → manifest envelope via ratchet → receiver durable enqueue + ACK + GET chunks + decrypt + sha256 verify + write to `filesDir/voice/<mediaId>.<ext>` → Chats unread badge + notification with contact name + playable bubble. Six rounds of real-hardware testing (Tests #56-#61, Tecno Tele2 LTE + emulator) caught and fixed: (#56) AppContainer DI gap — security-reviewer agent had hallucinated "DI complete with lines 738-740 wiring all three params" — `git log` proved AppContainer was never touched in M1w; runtime `MEDIA_RX no_download_repo` log was source of truth → wire-up commit `115b442b` + capability flip `canSendVoice = true` for `RestActive` + `WsCandidate`; (#57) manifest wrapper mismatch — sender sent raw `VoiceManifestV2`, receiver decoded as `MessagePayload(type=message, text="")` → empty bubble + envelope-id in notification — fixed by wrapping in `MessagePayload(type=TYPE_VOICE_V2, text=manifestJson)` + Ktor `SocketTimeoutException` now wrapped as `MediaTransportException` so retry loop fires (`cc456a36`); (#58) Tele2 LTE Ktor download = 1 chunk / ~31 s — architect verified via VPS logs that relay served chunks in single-digit ms while phone waited a full timeout between GETs — third instance of "replace Ktor with native OkHttp fresh client per call on Tele2" pattern after R0.1 and R0.3 (`9ef4a76e`); same commit also moved `sendAudioV2` upload chain into `scope.launch` so phone upload survives Compose lifecycle cancellation; (#59) UI/state propagation — orchestrator updated DB but ChatScreen never reloaded the bubble — `runVoiceV2DownloadTask` now re-reads the row and emits `IncomingMessage` after `download_complete`; sender status flips UPLOADING → SENT immediately after `manifest_sent`; notification title now uses `payload.senderUsername` not raw hex (`47a1b038`); (#60) Chats unread badge — `handleVoiceV2Manifest` upsert dropped `unreadCount`; mirrored normal-message path with `unreadCount + 1` + `🎤 Voice message` preview (`0a48c8cf`); (#61) full PASS. Architect verdict: M1w voice delivery PASS end-to-end, native media transport PASS, AUDIO_LOCAL/message_ready PASS, unread/notification UX PASS. Old `audio_chunk` path remains in code as JVM/test fallback and for in-flight Alpha-1 messages but is no longer the production sender path. **Known follow-up: PR-M2 performance pass** — recorder bitrate (5 sec voice currently 39-57 KB, target 10-20 KB via Opus voice profile @ 16-32 kbps), download parallelism = 2, upload parallelism = 2 (only after Tele2 stability proven), optional batch endpoint later. **Lessons logged in agent memory:** `feedback_reviewer_hallucination_2026_05_18.md` — DI claims with specific line numbers from reviewer agents require `git log -p` + `grep` proof before trust; runtime logs are always the source of truth. `project_tele2_media_path_2026_05_18.md` — third instance of "Ktor on Tele2 stalls 30s+ between requests; native OkHttp fresh client per call fixes it" — any new Android HTTPS path on RU LTE = native OkHttp, not Ktor. **🎉 2026-05-18 late — PR-M2a merged (#174, master `bc94a4d4`) — voice-note recorder profile shrinks 5-sec voice by ~40 % without changing transport.** `ChatScreen.startChatRecording` + `GroupChatScreen.startGroupRecording` move from music-grade (48 kbps Opus @ 48 kHz / 64 kbps AAC @ 44.1 kHz) to voice-grade (24 kbps mono 16 kHz; OPUS on API 29+, AAC_ELD on API 26-28). `VoiceFileStore.mimeToExtension` adds an explicit `audio/m4a` → `.m4a` branch so the AAC-fallback receive path lands on the correct extension. Diagnostic logs `VOICE_REC config encoder=... sampleRate=16000 bitrate=24000 channels=1 outputFormat=... mime=...` (pre-start) and `VOICE_REC complete durationMs=... bytes=... bytesPerSec=... mime=...` (post-stop) surface real-world payload to logcat. Test #62 measured 5-sec voice drop from 39-57 KB to 23.5 KB (~40-60 % reduction) and 35-sec voice 150 KB / 89 chunks. The architect-suggested M2a.1 follow-up (bitrate 24 → 16 kbps to hit the strict ≤ 20 KB target) was prepared and then vetoed by Vladislav ("нам надо, чтобы звук был идеальным, почти в HD"); committed work was reset on the feature branch before CI re-fired. Locked decision: 24 kbps is the production floor; 16 kbps reserved for a future "Data Saver" toggle. Performance optimisation continues at the transport layer (PR-M2b download parallelism = 2, then PR-M2c upload parallelism, then optional PR-M2d batch endpoint) not the codec layer. Voice-quality preference logged in agent memory as `feedback_voice_quality_priority.md`. **🛑 2026-05-18 evening — PR-M2c.0 cap probe complete, M2c (bigger chunks) blocked on the Helsinki path.** PR #179 (master `c05f4127`) made the relay's media body cap fully config-driven — previously the axum `DefaultBodyLimit` middleware was hard-coded to the 3072 constant while the in-handler check read from config, so env-var overrides were silently ignored at the middleware layer (caught during the first probe attempt). With the fix and `RELAY_MAX_MEDIA_UPLOAD_BODY_BYTES=9000` deployed, the probe v2.1 (state-separated outcome enums + 2400-byte control + SHA-256-prefix logging + final verdict based on `largestFullRoundtrip` not `largestStoredSafe`) ran cleanly on Tecno Tele2 LTE. Results: 2400 (control) `3/3 stable_full`; 5500 and 6500 `relayStored 5/5` but `fullRoundtrip 0/5` (relay accepted POST, but Tele2 dropped both the 201-response body and the GET-body on download — same "Tele2 Layer B" pattern documented for `/prekeys/publish`); 7168 `0/3` (Tele2 cut even the request body). Final probe verdict: `stableMaxBodyFullRoundtrip=2400 largestUploadOnly=6500 recommendedRawChunkBytes=1700`. Locked decision: do not ship M2c bigger chunks on `relay.phntm.pro`; production `TARGET_RAW_CHUNK_BYTES` stays at 1700. The Phase 1 plan from the audit (Section 5) is rejected as not feasible on the current single-relay infrastructure. Next track: PR-INFRA-MediaRO — deploy a second `phantom-relay` (full binary) at a different VPS/ASN (FlokiNET Romania candidate; bridge2 deployment exists as a reference but is WebTunnel-only), allocate a media subdomain, decide auth model (federated identity vs per-relay registry), re-run the probe with two `MediaProbeEndpoint` entries, and design `mediaRelayId` extension to `VoiceManifestV2` if Romania gives `fullRoundtrip 5/5` at 5500/6500. Probe code retained on `diag/m2c0-media-route-probe` so the re-run is one-config-edit-plus-rebuild, not a probe rewrite. Memory entry `feedback_tele2_media_path_ceiling_2026_05_18.md` records the architectural insight: Tele2 LTE full-roundtrip ceiling on a single Helsinki path is ~2400 bytes wire body; the lever for performance is route diversity, not chunk size / codec / parallelism / in-handler cap. Audit doc `docs/design/voice-delivery-audit-2026-05-18.md` Section 8 captures the data and rejection. **🎉 2026-05-19 — voice-delivery acceleration trilogy merged (PR-M2d.1a #182 → PR-M2d.1b #183 → PR-M2e #184).** Tests #67a + #67b → #67b.1 → #67b.2 + #68 confirmed each PR in isolation. M2d.1a (`2a9ba08c`) added `Prefer: return=minimal` upload semantics — client requests headers-only 204 with `X-Chunk-Stored/Duplicate/Idx`, eliminating the body-drop OkHttp read-timeout class on Tele2 LTE (Test #67a logs show every Tecno upload as `status=204 bodyMs=0`); idempotency-aware retry fallback fires only when relay actually accepted (`bodyReadError != null && statusCode in [200, 201]`). M2d.1b (`f804e0d8`) added an in-memory `MediaProgressBus` (`StateFlow<Map<rowId, Progress>>`) keyed by `localMsgId` on sender and `mediaId` on receiver, with `MEDIA_TX upload_progress` / `MEDIA_RX download_progress` per-chunk logs, `AudioBubble` rendering `Uploading N/M` (sender) and `Downloading N/M` (receiver) instead of a static spinner, and a `voiceSendInProgress` UI guard that closes the 334 ms recorder re-entry bug surfaced during Test #67b (mic button now ignores bounced taps with `VOICE_REC ignored_start reason=finalizing_or_sending`). Status-gate dropped from the upload bubble check after Test #67b logs proved the row was being loaded as `status=QUEUED` between INSERT and updateStatus commit — the bus entry itself is the authoritative live-upload signal. Visual style on the sent (cyan) bubble switched to `BgDeep` ink with 12 sp Medium label and a thicker rounded progress bar; still considered "raw" but acceptable as a non-blocker — UI/UX polish belongs to a separate dedicated track (read receipts, presence, status icon consistency, failed/retry states, message bubble layout). M2e (`fd53b676`) restructured the upload → manifest → download chain into two overlapping phases: `VoiceV2Sender.uploadVoice` accepts an `onEarlyManifest` callback that fires exactly once after `EARLY_MANIFEST_AFTER_CHUNKS = 3` chunks have committed; the manifest is built up front (mediaId / mediaKey / nonce / sha256 known immediately after encrypt); DMS hoists the manifest-send block into a `suspend fun sendManifestEnvelope(manifest)` lambda used by both the early callback and a tail fallback, guarded by a local `manifestSent` boolean (guaranteed exactly one `MEDIA_TX manifest_sent` per `mediaId`); `UPLOADING → SENT` flips immediately after manifest leaves the device while upload tail continues in background and the bubble counter keeps ticking until `upload_complete` (bus survives until `finally`). On the receiver, the old `ByteArray?` return type that collapsed 404 and auth-exhausted into the same `null` was replaced with a sealed `ChunkOutcome.Ok / NotReady / Terminal`; dynamic fresh-task window `(chunkCount × 1500 ms).coerceIn(120_000, 300_000)` (15 chunks → 120 s, 82 → 123 s, 107 → 160 s, 200 → 300 s cap) means 404 inside the window is `chunk_not_ready_yet` with backoff `1s → 2s → 3s cap`, retry the same idx, and only past the window does it fall to `markFailed(media_chunks_gone)`. Parameters locked by Vladislav 2026-05-19 after rejecting the original proposal of fixed 60 s + 10 retries × 2 s as too short for 100+-chunk voice on Tele2 LTE upload tail. **Test #68 PASS** on 82-chunk voice: receiver started downloading **~57 s before sender finished uploading** (timezone-aligned timestamps: sender `early_manifest_sent` at 22:44:36, sender `upload_complete` at 22:45:35, receiver `download_progress received=1` at 03:44:38 = sender's 22:44:38, receiver `download_complete` at 03:46:08). Zero false `media_chunks_gone`, zero `decrypt_failed`, voice playable on both endpoints. Architect: total wait dropped from ~150 s (`upload 60 s + download 90 s`) to ~90 s (`max(upload 60 s, download 90 s)`), ~40 % improvement on long voice. Still subjectively long because per-chunk ~1.1 s GET RTT on the receiver is the new bottleneck — to be addressed by PR-M2f binary `/media/v3` next (removes JSON + Base64 ~33 % wire overhead so raw ciphertext per chunk can rise from ~1700 to ~2200-2300 within the same Tele2 ~2400-byte full-roundtrip ceiling, expected ~25-35 % fewer chunks). Yellow-dot first-message delay on text after adding a contact (visible across Tests #67b through #68) is a separate prekey/bootstrap follow-up unrelated to media PRs; logs pinpoint `PREKEY_TRACE upload_fail SocketTimeoutException elapsedMs=184182` — same class as the H1e half-open WS pattern. Memory entries `project_m2d_roadmap_2026_05_18.md` (locked PR sequence) and `project_m2e_locked_params_2026_05_19.md` (dynamic fresh-task window math) capture the architectural reasoning. **🎉 2026-05-19 evening — M2f trilogy closed (PR-M2f #186 → PR-M2f.1 #187 → PR-M2f.1b #188 → PR-M2f.2 #189), production voice chunk size locked to 3200 bytes.** PR-M2f added the binary `/media/v3/{mediaId}/{idx}` endpoint pair (raw `application/octet-stream` body, 204 No Content + `X-Chunk-Stored/Duplicate/Idx` + `ETag: "<sha256_hex>"` + `Cache-Control: no-store, no-transform` on POST; `200 OK` + `application/octet-stream` + `ETag` + `X-Chunk-Total` on GET) alongside the legacy v2 pair, sharing the same in-memory `MediaStore` and idempotency contract. `/auth/session` response gained an extensible `media_capabilities: { binary_v3, max_upload_body_bytes }` nested object; older relays without the field default to `binary_v3=false` and clients stay on v2 (kotlinx-serialization safe default). `AndroidNativeOkHttpMediaUploadTransport` got a `binaryV3Enabled: () -> Boolean` lambda wired to `restOrchestrator.capabilities.value.mediaBinaryV3` with a sticky 404/405 fallback to v2 on the capability-stale runtime case only (network errors stay in the existing retry loop). `RelayCapabilities` gained `mediaBinaryV3` + `mediaUploadBodyBytes` (SAFE_DEFAULTS both off). 7 new relay integration tests; 23/23 media tests green. PR-M2f.1 added `phantom.android.diagnostics.ChunkSizeProbe` (`BuildConfig.DEBUG`-gated 5-size matrix `1700/2200/2300/2400/2600`) + `VoiceV2Sender.chunkSizeProvider` lambda clamped `[1024, 8000]` + `MEDIA_TX chunk_size_selected` log + `chunkSizeBytes=N` field on the existing `chunk_split` log + Settings → Diagnostics dropdown. PR-M2f.1b extended the matrix with `2800/3000/3200/3500` after Test #70 invalidated the prior "Tele2 ceiling ~2400" assumption (that ceiling came from v2 JSON+Base64 measurements; v3 binary frees ~600-700 bytes by removing the 33% inflation overhead). PR-M2f.2 locked production `TARGET_RAW_CHUNK_BYTES = 3200` per Vladislav's never-ship-max-passed-once policy (3500 stable both directions on Test #70.2 → ship 3200 as the safe one-tier-under margin) and replaced `EARLY_MANIFEST_AFTER_CHUNKS = 3` with `EARLY_MANIFEST_AFTER_BYTES = 5100` + dynamic recompute `max(1, ceil(5100.0 / chunkSize)).coerceAtMost(total)` so the M2e overlap budget stays at the original 5100 bytes proof-of-life regardless of chunk-size knob (3 chunks at 1700/2400, 2 chunks at 3200/3500). **Test #71** on production parameters confirmed end-to-end: `MEDIA_TX chunk_split chunkSizeBytes=3200` both ends, `early_manifest_sent afterChunks=2 total=8/44` for long voice, zero `sha256_mismatch` / `decrypt_failed` / `media_chunks_gone` / `MEDIA_V3 fallback`, voice playable both directions, 30-second voice dropped from ~82 chunks → 44 chunks (Tecno-sized) with `max(upload, download) ≈ 50 s` vs ~150 s pre-M2. Tecno download path notably dropped from ~90 s → ~15 s. Emulator-side `bytesPerSec ≈ 2200` (Tecno ≈ 4500) is normal Android `MediaRecorder` VBR/microphone variance — chunker just slices the produced ciphertext at 3200, half-sized file → half-sized chunk count (22 chunks for 31-sec voice from emu vs 44 for the same length from Tecno). Architect verdict: "M2f.2 PASS, transport now has roughly 3× headroom over the original v2 ceiling; next real win is per-chunk RTT, not body size." Three open follow-ups: **PR-M2h pooled OkHttp download A/B** (next performance track — each fresh client + fresh TLS handshake pays ~600-800 ms of the ~1.1 s/chunk RTT; pool reuse with sticky-fallback-to-fresh-on-stall could give another large win); **PR-D1e contact bootstrap fast path** (first text message after `add contact` hangs ~10-20 s on `PREKEY_TRACE upload_fail SocketTimeoutException elapsedMs=8021` — independent of media path); **UI stale-bubble track** (receiver `AudioBubble` briefly flashes "Downloading" after `download_complete` on some Test #70.2 sizes; Compose state-refresh issue, tracked separately).  
**Strategic posture (2026-05-14, reaffirmed 2026-05-15):** no fixed deadlines — focused on production stability and external audit readiness rather than premature release pressure. Tags are not goals; functional quality is the goal. **Strategic pivot 2026-05-15:** Tor demoted to text-only emergency fallback (cannot carry WebRTC for calls — no UDP through onion, latency too high, bandwidth insufficient); Direct or Reality remains required for voice messages and calls; the planned `TransportCapabilities` data class (text/voiceMessages/calls/realtimeUdp flags) is queued as PR-R1a in a parallel design track. Multi-VPS Reality pool is **NOT** the next infrastructure step — the data showed the bottleneck is the OkHttp publish bug, not Hetzner reachability; the cheaper-first experiments (alternative SNI on the same VPS, then Oracle Cloud Always Free if needed) are queued behind PR-R0 validation, not ahead of it.

---

## Большая картина — 4 параллельных трека

> **⚠️ SUPERSEDED block — strategic pivot 2026-05-14.**
> The ASCII timeline and 4-track grid below carried hard release deadlines (~~1 июня release / 30 июня secondary funding~~) that were locked **before** Vladislav's 2026-05-14 "no deadlines, Quality > Speed" pivot. Both dates are formally dropped. The grid is preserved as the historical track structure (Tracks A/B/C/D/E still exist as research backlogs and as the lens through which earlier merged PRs are grouped) — but the schedule overlay is no longer authoritative. Read the **Status block** at the top of this file and the **Session journal** in `docs/PROJECT_LOG.md` for what's actually being worked on now.

```
                                         ~~Release~~    ~~Secondary~~
  Сегодня                              ~~(1 июня)~~  ~~(30 июня)~~
  2026-05-03 ──────────── 4 недели ────────►│────── 4 нед ──►│──── ... до v1.0
                                            │                │
  Track A — RELIABILITY SPRINT ─────────►│  │                │
  (5 PR, ~13 дней работы)                   │                │
                                            │                │
  Track B — SECURITY SPRINT ─────────►│      │                │
  (10 P1 блокеров до Kickstarter)            │                │
                                            │                │
  Track C — RELEASE POLISH ───────►│         │                │
  (4 фазы repo polish, ~6 часов)              │                │
                                            │                │
  Track D — Alpha 2 FEATURES ───────────────►│ продолжение ►  │
  (attachments, groups, channels, etc)
                                            │                │
  Track E — CENSORSHIP RESISTANCE ───────►✅│                │
  (Stage 5E Xray production-validated 2026-05-07)
```

~~Track A + Track C идут параллельно. Track B стартует после Track A. Track D — после Track B items 1–4. Track E — закрыт основным объёмом, два cleanup-PR в backlog.~~ — superseded by the 2026-05-14 strategic pivot. Tracks A/B/C/E are functionally complete (last items rolled into the M1w/M2/H1/H2/D-track work tracked in the session journal); Track D has been re-decomposed into smaller per-feature sprints (M1w / M2* / REC* / D-track / C-track) on quality-driven cadence rather than calendar-driven sprints.

---

## Track A — Reliability Sprint

**Цель:** Alpha-3 release-candidate quality для текста + голоса + звонков. Закрывает все user-visible bugs surfaced by real-device QA на Tecno HiOS + Pixel emulator.

| # | PR | Задача | F-numbers | Оценка | Статус |
|---|----|--------|-----------|--------|--------|
| 1 | [#28](https://github.com/LiudvigVladislav/Phantom/pull/28) | Transport reliability sprint (ADR-010, abandon-and-restart, AlarmManager, MulticastLock) | — | — | ✅ merged `12ac26c5` |
| 2 | [#29](https://github.com/LiudvigVladislav/Phantom/pull/29) | Calls UX | F-03, F-07, F-10, F-15 | ~2 дня | ✅ merged `45338fb8` |
| 3 | [#30](https://github.com/LiudvigVladislav/Phantom/pull/30) | Calls media (mic permission, audio mode, чёрный экран) | — | ~1 день | ✅ merged `c62fbfff` |
| 4 | docs/calls-experimental | Calls=experimental decision + Track A schedule lock | — | 30 мин | ✅ merged `d094ca8f` |
| 5 | feat/voice-message-chunking-pr3 | **Voice messages chunking** | F-05 | ~5 дней | ✅ merged `41b9fb94` (#32, 2026-05-04). ✅ 8 KB chunk-size already on master (`AUDIO_CHUNK_BYTES = 8 * 1024` in `DefaultMessagingService.kt:139` — landed before this note was written; no orphan, verified 2026-05-14) |
| 6 | [#65](https://github.com/LiudvigVladislav/Phantom/pull/65) | Data integrity edges | F-08, F-01, F-09, F-04 | ~3 дня | ✅ merged `df127bc1` (#65, 2026-05-08) |
| 7 | [#67](https://github.com/LiudvigVladislav/Phantom/pull/67) | Storage durability | F-02, F-12, F-06, F-13 | ~3 дня | ✅ merged `367a3d61` (#67, 2026-05-08) |
| 8 | [#68](https://github.com/LiudvigVladislav/Phantom/pull/68) | UX cleanup + small fixes | F-14, F-21, F-24 + 3 fixes | ~2 дня | 🟡 PR open — awaiting merge |
| 9 | (PR 2.6) | Calls audio plumbing — `JavaAudioDeviceModule` + AudioFocus + suppress reconnect during call | — | 2–3 дня | 🟦 **отложен post-Phase-5** |

**После завершения Track A** = Alpha-3 release-candidate. Voice + text + calls работают надёжно (calls = experimental, остальное production-quality).

### Transport reliability mini-sprint (2026-05-10/12) — 11 PRs merged, closed-success ✅

Triggered by real-device cross-device tests on Tecno МТС ± VPN that surfaced three concrete failure modes: 70-second connect cycles from keepalive forceReconnect during WS handshake, Reality probe timing out at the wrong (~10 s) inner-socket budget, and Tor on МТС stalling 10+ minutes with zero user feedback. Resolved over three days; all PRs merged into master.

**🎉 Outcome (Test #6, 2026-05-11 21:25-21:32 МТС Wi-Fi without VPN, Tecno Spark Go):** Ghost mode reached `Online via Tor · Ghost` in ~6 minutes on the very first KitchenSink (1/4) attempt of PR-E's Briar-imported bridge pool — bootstrap walked 0 % → 30 % in 1 second, paused at 50 % for ~5 min while Tor built guard circuits, then climbed 50 % → 100 % in another minute. Probe 200 OK over the onion address, WebSocket handshake successful immediately after `Ready`. **First captured logcat proof of Ghost-without-VPN on a Russian carrier network in PHANTOM's history.** Compare: PR-D + old snowflake bridges in the same scenario timed out at 30 % after 12 minutes. The Google-AMP-cache snowflake entries in `bridges-s-ru` (fronted on `www.google.com`) are the most likely cause of the win.

| # | PR | Задача | Статус |
|---|----|--------|--------|
| 1 | [#103](https://github.com/LiudvigVladislav/Phantom/pull/103) | SOCKS connect-timeout, per-kind probe budget, keepalive guard | ✅ merged `647f369d` |
| 2 | [#104](https://github.com/LiudvigVladislav/Phantom/pull/104) | Instrument `XrayService.startBlocking` + robust libXray response parser; bump Tor probe to 90 s | ✅ merged `2f67e218` |
| 3 | [#105](https://github.com/LiudvigVladislav/Phantom/pull/105) | Ephemeral Xray SOCKS port, error-field parser, per-kind probe timeout | ✅ merged `18317548` |
| 4 | [#106](https://github.com/LiudvigVladislav/Phantom/pull/106) | Keepalive guard for WS handshake, `lastPongMark` reset, Reality probe → 20 s | ✅ merged `6b4c4ff5` |
| 5 | [#107](https://github.com/LiudvigVladislav/Phantom/pull/107) | Sync Reality outer probe budget, stream Tor bootstrap %, defensive `tor.stop()` on prepare failure | ✅ merged `2ad57a09` |
| 6 | [#108](https://github.com/LiudvigVladislav/Phantom/pull/108) | **PR-A1 (diag):** sync OkHttp inner timeouts to `callTimeout`, log `vpnActive=true|false` on every `connect()` | ✅ merged `a953d3e4` |
| 7 | [#109](https://github.com/LiudvigVladislav/Phantom/pull/109) | **PR-A2:** filter Reality from chain when `vpnActive=true` (Caddy-log audit confirmed Reality+VPN never reaches the relay edge); ISSUE-015 in KNOWN_ISSUES.md | ✅ merged `75775c00` |
| 8 | [#110](https://github.com/LiudvigVladislav/Phantom/pull/110) | **PR-B:** staged Tor-bootstrap UX with time-keyed copy + live percent (Initial → Negotiating → Searching → Slow → Throttled) | ✅ merged `53a02967` |
| 9 | [#111](https://github.com/LiudvigVladislav/Phantom/pull/111) | **PR-C:** sequential Tor bridge profile rotation — `obfs4-only` (180 s) → `webtunnel-only` (120 s) → `snowflake-only` (180 s) → `mixed` (240 s) with per-profile `tor.stop()` between attempts; UI shows `<profile> (k/4)` | ✅ merged `ed143c60` |
| 10 | [#112](https://github.com/LiudvigVladislav/Phantom/pull/112) | **PR-D:** rotation order retuned per Test #5 МТС data — `Mixed (600 s)` → `Snowflake (360 s)` → `Obfs4 (90 s)` → `Webtunnel (90 s)`; Ghost-mode AllFailed copy now actionable ("Tor is blocked or slowed by this network. Try Private/Reality or enable a VPN."); `KNOWN_ISSUES` ISSUE-016 documents the upstream TSPU root cause | ✅ merged `41506995` |
| 11 | [#113](https://github.com/LiudvigVladislav/Phantom/pull/113) | **PR-E:** import Briar's RU-tuned bridge set (4 snowflake `bridges-s-ru` with Google AMP cache fronted on `www.google.com`, 9 non-default obfs4, 1 meek_lite); new `BridgeProfile.KitchenSink` puts ~17 bridges in one `enableBridges()` call (Briar's empirical strategy) and runs first; rotation order: `KitchenSink (600 s)` → `SnowflakeOnly (420 s)` → `Obfs4Only (180 s)` → `MeekLite (60 s)`; explicit privacy properties matrix added to ISSUE-016 + source-level KDoc | ✅ merged `<latest>` |

**State after mini-sprint (per PR-E + Test #6 confirmation):**

- ✅ Standard works on every tested network (МТС ±VPN, other carriers).
- ✅ Private without VPN works via Reality (production-quality).
- ✅ Private under VPN works via Tor (Reality auto-skipped per audit evidence).
- ✅ Ghost works via Tor on uncensored / VPN-protected networks (mixed reaches Ready in 2-7 min).
- ✅ **Ghost on МТС WITHOUT VPN works** (Test #6 confirmed: ~6 min via KitchenSink with Briar's `bridges-s-ru` Google-AMP-cache snowflake). Single-test caveat — worth a few more МТС sessions before claiming production stability — but the architecture-side question is answered.

**Privacy trade-off accepted (Vladislav A+C, 2026-05-12):**
PR-E's Snowflake-via-Google-AMP fallback routes broker-discovery TLS through Google's AMP cache. Google sees the IP making encrypted TLS to its CDN with a Snowflake-style request pattern; Google does NOT see PHANTOM identity, onion address, contacts, or message content (Tor circuit traffic flows over WebRTC DataChannel directly to a volunteer browser proxy, not through Google). This pattern is the same one Tor Browser ships by default for RU users and that Briar ships in `bridges-s-ru`. Documented in `KNOWN_ISSUES.md` ISSUE-016 with full "what Google sees / does not see" matrix.

**Deferred follow-ups:**
- Telemetry PR (architect's third recommendation — average bootstrap time, bridge success rate, country/network heuristics) — not blocking for NLnet.
- Operations work to deploy additional bridges on non-blocked CIDR (snowflake on a non-Hetzner / non-FlokiNET ASN, obfs4 on additional ranges). Tracked separately post-Alpha-2; needs VPS budget.
- BridgeDB-on-device fetcher (Vladislav's plan Option 3): Briar research showed Briar does not have one either — value is in bridge data freshness, not the fetcher per se. Deferred until bridge-freshness becomes the bottleneck.
- Cross-operator testing (Beeline / Megafon / Tele2): needs additional SIM cards, deferred until accessible.
- Retrospective ADR documenting bridge rotation strategy (architect-recommended).

### First-message reliability sprint (2026-05-13) — PR-G3 + PR-G4 merged ✅

Triggered by Test #28 (2026-05-12) which reproduced the recurring "yellow dot for two minutes on first message to a new contact" symptom. PR-G3 (observability) and PR-G4 (REST forced HTTP/1.1) closed the root cause within one session.

| # | PR | Задача | Статус |
|---|----|--------|--------|
| 1 | [#123](https://github.com/LiudvigVladislav/Phantom/pull/123) | **PR-G3 (diag):** structured `PREKEY_TRACE` logs across `PreKeyLifecycleService`, `PreKeyApiClient`, and `DefaultMessagingService` — bootstrap/upload/verify lifecycle, HTTP-level URL+status+elapsed-ms, distinguish bundle-fetch result `200|404|timeout|429|http<code>` (was previously collapsed to "treated as 404") | ✅ merged `f90ac90c` |
| 2 | [#124](https://github.com/LiudvigVladislav/Phantom/pull/124) | **PR-G4:** pin REST `OkHttpClient` to `Protocol.HTTP_1_1` only (WS already pinned to H1.1, iOS uses Darwin, JVM uses Ktor defaults — all unchanged) | ✅ merged `245c6f09` |
| 3 | (commit `3db97b49`) | **PR-H1a (diag):** per-WS-session epoch `[gen=N s=M]` tagging client-side + `conn_id` + `ack_deliver_received`/`ack_deliver_removed_from_store` server-side. Ruled out zombie-writer hypothesis; pinpointed strict-FIFO violation in outbox flush as the real cause of Test #33 MAC failures | ✅ merged `3db97b49` |
| 4 | [#127](https://github.com/LiudvigVladislav/Phantom/pull/127) | **PR-H2a:** strict FIFO outbox via per-envelope monotonic `sequenceTs` + `outboundSendMutex` serializing live-send vs flush. Wire format unchanged. 5 unit tests including Test #33 layout | ✅ merged `674ce231` |
| 5 | [#128](https://github.com/LiudvigVladislav/Phantom/pull/128) | **PR-H2a.2:** close live-send race during FIFO flush — second mutex layer caught by 7-times-measure review before H2a push (live `send()` could have observed the cleared outbox and raced onto the wire with a higher `sequenceTs`); holds `outboundSendMutex` across the entire flush-Send loop | ✅ merged `72e59ce9` |
| 6 | [#129](https://github.com/LiudvigVladislav/Phantom/pull/129) | **PR-H2b:** idempotent envelope ledger blocks redelivered MAC errors. New `processed_envelopes` table records every decrypted envelope id regardless of payload type; guard runs BEFORE `ratchet.decrypt`. INSERT OR IGNORE for race-safety; 8-day TTL (one day longer than relay store). Forward-only `15.sqm` migration; schema v15 → v16. Marks PROCESSED on success, FAILED_MAC on decrypt failure (debug column only). 6 storage contract tests + 2 messaging tests. Legacy `messages.id` guard kept as defence-in-depth | ✅ merged `7008cf3e` |
| 7 | [#131](https://github.com/LiudvigVladislav/Phantom/pull/131) | **PR-H1b (diag):** per-session WS `SessionStats` (pings_sent, pongs_received, missed_pongs, ping_send_failures, inbound_frames, since_last_*_ms) + server-side `event="session_summary"` log line at unregister with `close_origin` ∈ {client,server,error,none} + `closeReason.await()` extraction. Test #35 then revealed half-open TCP black-hole pattern: server `pings_received=2-5` vs client `pings_sent=11`, `since_last_ping_ms ≈ 153 s` on every dying session — middlebox NAT eviction (МТС) + Tecno HiOS aggressive radio park silently dropping outbound packets while OS still considers socket healthy | ✅ merged `0baa4196` |
| 8 | [#132](https://github.com/LiudvigVladislav/Phantom/pull/132) | **PR-H1c:** stale-socket recovery — six-layer fix. (1) `lastInboundFrameElapsedMs` on `RelayTransport` interface — watchdog triggers off ANY inbound frame, not just Pong. (2) OkHttp `pingInterval(15s)` — independent dead-socket detection in 15 s. (3) `ping_send_failed` → immediate `forceReconnect()` + `break` (was log-and-continue). (4) `TransportState.Reconnecting` distinct state — UI banner "Reconnecting…" instead of going dark. (5) AlarmManager proactive reconnect at 45 s stale-inbound (< 60 s pong-watchdog), safety net for OEMs with throttled AlarmManager. (6) Server-side TCP SO_KEEPALIVE on listener (idle 15s, interval 5s, retries 3) via `socket2` `#[cfg(unix)]`. **Test #37 verified** end-to-end on twin-emulator + Tecno МТС, 12 messages each direction: detection 155 s → 30–46 s, recovery 5 s → ~1 s, zero loss (H2b ledger neutralises re-delivered envelopes seen as `Duplicate envelope (already in ledger)`). UX: routine "Reconnecting…" badge instead of "lagging" outage | ✅ merged `e946caba` |

**What the new traces revealed in Test #29.** With a clean wipe and emu started before phone, phone's bundle fetch for the emu identity timed out at exactly **8009 ms × 4 in a row**; emu's own publish hung **20.7 s then 39.5 s** before `SocketException: Connection reset` from `Http2Reader.readConnectionPreface`. Yet `verify_status` returned `opks_remaining=100` for the same identity within 17 s, and a `curl` from the relay VPS itself fetched the bundle in **242 ms** (HTTP 200, 634 bytes). Caddy access log showed phone's failed bundle GETs **not arriving at Caddy at all** — requests died inside OkHttp before any bytes hit the wire. Failed POSTs from emu logged with `bytes_read: 0` after 30 s and `408 Request Timeout`: client opened the H2 stream, sent headers (`Content-Length: 13903`), then never delivered the body. **Root cause: OkHttp's HTTP/2 implementation gets stuck on REST requests** — both on stream upload and on stale-connection reuse from the pool.

**Test #30 (post-PR-G4 retest):**
- Emu publish bootstrap: **829 ms → 201** (was: 20.7 s + 39.5 s timeouts).
- Phone publish bootstrap: **541 ms → 201**.
- Phone fetch emu bundle on first message: **151 ms → 200 OK on the first try** (was: 8009 ms timeout × 4, then sweep eventually succeeded ~3 minutes later).
- First message envelope sent within **484 ms of `send_start`**, no DEFERRED, no WAITING. **Yellow-dot reproduction step is gone.**

**Deferred follow-ups:**
- **PR-H1e (next priority — diagnostic sprint).** Architect-proposed root-cause investigation: WS dies every 30–50 s on BOTH МТС cellular AND emulator-on-dev-Wi-Fi (no shared carrier path) — points at our stack, not pure carrier behaviour. Scope: (A) tcpdump on relay during one dying session (FIN / RST / outbound Ping packets / TLS close_notify); (B) WS `pingInterval(5s)` experiment vs current 15 s — does middlebox count 15 s as idle?; (C) disable app-level Ping temporarily, leave only OkHttp WS Ping — four heartbeat layers may trip DPI; (D) force `Protocol.HTTP_1_1` on WS upgrade — same hypothesis class that fixed REST in PR-G4. Diagnostic, not a fix sprint.
- **PR-H1d (deferred).** Server-side superseded-close (close old `state.clients[identity]` actively, log `event="superseded" old_conn_id new_conn_id`) + client-side explicit `session.close()` with 300 ms timeout before `forceReconnect`. Improves operational hygiene (zombie sessions on server drop from ~153 s to ~1 s) but changes nothing user-visible — reconnect already 1 s, messages already 100 % delivered, ledger already neutralises duplicates. Reconsider only if H1e finds the cycle is unfixable at root and reconnect storms become permanent reality, or if relay starts suffering FD/memory pressure under load.
- **PR-H2c:** skipped-message-keys in the ratchet (per Signal Spec) + bounded retry on MAC error + dead-letter log. PR-H2a/a.2 closed the *client-side* reorder source (outbox FIFO); PR-H2b made redelivered envelopes idempotent (no double-advance of the chain). H2c is the third leg — making the receiver tolerant of *legitimate* future-message reorder (TCP retransmit pause, multi-path mobile, Tor circuit shifts) by buffering skipped chain keys instead of MAC-failing. Without H2c, any future scenario that legitimately re-orders frames between sender's wire and receiver's decrypt will surface as a MAC fail. The H2b ledger then prevents *that* failure from being re-tried-and-failed-again on relay redelivery, but the original message is still lost.
- ~~WS pong timeout cycle every 60–110 s on both devices remains as **PR-H1b/c** material~~ — **closed by PR-H1b (#131) + PR-H1c (#132) merged 2026-05-13 night, Test #37 verified.** Detection 155 s → 30–46 s, recovery 5 s → ~1 s, zero loss. See rows 7–8 in the table above.
- One stale-H1.1 case on `verify_status` (30 053 ms `SocketTimeoutException` from `Http1ExchangeCodec`). Background path, low priority — connection-pool tuning.
- PR-G5 (UX text + bootstrap gate + fast retry) proposed by the architect — now genuinely cosmetic since PR-G4 fixed the underlying race. Deferred until WS stability lands.

---

## Track B — Security Sprint (Beta-tier блокеры)

**Цель:** security clearance для Beta release + Kickstarter announcement.

**Items 1–4 = Alpha-2 snapshot.** После их закрытия можно тегать `v0.1.0-alpha.2` и подавать external funding programme с честным "first four most user-visible privacy claims closed."

| # | Finding | Что сделать | Оценка | Блокирует | Статус |
|---|---------|-------------|--------|-----------|--------|
| 1 | **F22** SPK/OPK private keys plaintext SQLite | Keystore-wrap (AES-256-GCM) | ~2 дня | Alpha-2 | ✅ merged `6737be91` + `2bcd891e` + `22f0c30c`; QA-pass телефон+эму 2026-05-08 |
| 2 | **F19+F20** Call signalling no E2EE / no Sealed Sender | Wrap SDP/ICE in Double Ratchet + Sealed Sender | ~3 дня | Alpha-2 | ✅ merged `569b868e` (#62, 2026-05-08) |
| 3 | **F8** RatchetState plaintext SQLite | Keystore-wrap RatchetState blob перед записью | ~2 дня | Alpha-2 | ✅ merged `bb36705f` (#60, 2026-05-08) |
| 4 | **F2+F13** SenderKey signing dead | ADR-017 (remove signing) уже draft → реализовать | ~3 дня | Alpha-2 | ✅ merged `c1fe16ed` (#63, 2026-05-08) |
| 5 | **F1** Group control msgs outside Double Ratchet | Wrap SKD/leave/add в Double Ratchet | ~3 дня | Kickstarter | ✅ merged `cceb83fd` (#70, ADR-026, 2026-05-08) |
| 6 | **F3** SenderKey KDF bare SHA-256 | Заменить на HKDF-SHA256 с domain separation | ~1 день | Kickstarter | ✅ merged `1a04dadc` (#69, 2026-05-08) |
| 7 | **F4** Member-leave не ротирует ключи | Full key rotation на leave/remove | ~2 дня | Kickstarter | ✅ merged `6a38e232` (#71, 2026-05-08) |
| 8 | **F11+F26** Shared relay token | Per-user signed challenge | ~3 дня | Kickstarter | ✅ merged `6e90e3c6` (#72, ADR-027, 2026-05-08) — production-validated 2026-05-09 |
| 9–17 | P2 batch (F6, F7, F9, F10, F12 retry, F14, F18, F23, F25) | Зачистка во время Beta-полировки | — | — | ⬜ |

**Verified clean / уже закрыто:**
- ✅ Identity DH private key Keystore-wrapped
- ✅ **F22** SPK/OPK private keys Keystore-wrapped — `6737be91` (impl) + `2bcd891e` (инструментированный тест) + `22f0c30c` (SECURITY_ROADMAP). QA-pass 2026-05-08.
- ✅ Sealed Sender на каждом regular 1:1 message type
- ✅ F15 (identity-as-ratchet-key) — fixed via fresh ephemeral DH
- ✅ F17 — notification callback exception swallow logged
- ✅ F12 partial — SPK Ed25519 sig verify + OPK atomic single-use
- ✅ **F8** RatchetState Keystore-wrapped — `bb36705f` (#60). Read receipts via Sealed Sender — `f62569e6` (#59). 2026-05-08.
- ✅ **C-2** Read receipts routed through sealed DR pipeline — `f62569e6` (#59, 2026-05-08)
- ✅ **F19+F20** Call signalling through Double Ratchet + Sealed Sender; ADR-025 — `569b868e` (#62, 2026-05-08)
- ✅ **F2+F13** Dead SenderKey signing keypair removed; ADR-017 Accepted; migration 14.sqm — `c1fe16ed` (#63, 2026-05-08)
- ✅ **F3** SenderKey KDF: bare SHA-256 → HKDF-SHA256 with iteration-bound salt + `_v2` info strings — `1a04dadc` (#69, 2026-05-08)
- ✅ **F1** Group control messages (invite/SKD/leave/add) + group ciphertext broadcasts now route through DR + Sealed Sender; ADR-026 — `cceb83fd` (#70, 2026-05-08)
- ✅ **F4** Member-leave proactively rotates own SenderKey + redistributes fresh SKD to remaining members — `6a38e232` (#71, 2026-05-08)
- ✅ **F11+F26** Shared WS token replaced with per-user Ed25519 signed-challenge auth (TOFU first connect + 1:1 binding); ADR-027 — `6e90e3c6` (#72, 2026-05-08). Production-validated on Tecno МТС + emulator 2026-05-09. RELAY_TOKEN BuildConfig field removed from APK.

---

## Track C — Release Polish

**Цель:** репо в состоянии «reviewer тратит 5 минут и думает "это серьёзно"» к Alpha-2 release window.

Утверждённый план: [`~/.claude/plans/cosmic-sparking-otter.md`](../../.claude/plans/cosmic-sparking-otter.md) (на машине разработчика).

| Phase | Что | Время | Статус |
|-------|-----|-------|--------|
| 1 | Repo cleanup + .gitignore (remove `google-services.json`, `.kotlin/`; add 12 lines; commit `PHANTOM_ROADMAP_2026.md` + `ARCHITECTURAL_DECISIONS_TODO.md`) | 30 мин (factually ~2 hours with Firebase rotation + plugin conditional fix) | ✅ done 2026-05-08 (#46 + #47) |
| 2 | Funding plumbing — `funding.json` (secondary funding programme hard prerequisite) + `.github/FUNDING.yml` | 1 час | ✅ done (#49) |
| 3 | README polish — License → AGPL-3.0, Status → Alpha 2, ссылки на Threat Model + ADR + Codeberg, softer Funding wording, Mermaid диаграмма, 5 новых секций; правки в RELEASE_NOTES + CONTRIBUTING | 2 часа | ✅ done (#49 + #51 + #52 + #55) |
| 4 | English exec summary для Threat Model + Doctrine; ADR index `docs/adr/README.md`; `ARCHITECTURE.md` в корне | 2 часа | ✅ done (#50 + #52) |

**Итого Track C:** один день работы, не блокирует другие треки.

**Phase 5 (отложено):**
- Demo GIF (5 сек, эмулятор)
- Pinned "Roadmap to v1.0" issue на GitHub
- PGP key для security@phntm.pro
- Полные английские переводы Threat Model + Doctrine
- `.github/ISSUE_TEMPLATE/` + PR templates

---

## Track E — Censorship Resistance (Stage 5)

**Цель:** PHANTOM работает в RU без VPN, без Orbot, без сторонних приложений. Censorship resistance — central pitch проекта.

Track E architecture is split across two formal ADRs: [`ADR-016`](../adr/ADR-016-tor-unified-push-hybrid-transport.md) (Tor + UnifiedPush hybrid; Stages 5A–5D bridge progression) and [`ADR-019`](../adr/ADR-019-Xray-REALITY-Outer-Transport.md) (Xray VLESS+REALITY as outer transport; Stage 5E, supersedes 5C/5D for RU carrier traffic). All Stage 5 commits have landed on master via numbered PRs; no separate sprint branch remains open.

| Stage | Что | Статус | Доказательство |
|-------|-----|--------|----------------|
| 5A | kmp-tor embedded daemon + lifecycle | ✅ done | merged ранее |
| 5B | onion address resolution + RELAY_ONION_URL config | ✅ done | merged ранее |
| 5C | WebTunnel bridges (operator-controlled, Hetzner) | ⚠️ stalled at 14 % bootstrap on RU MTS | Test 11 / 2026-05-06 — TSPU 16-KB curtain |
| 5D | Multi-bridge fan-out (FlokiNET, non-Hetzner) | ⚠️ same TSPU stall on FlokiNET RO | Test 13 / 2026-05-06 — curtain is behavioural, not ASN-based |
| 5E.A | Server-side Xray VLESS+REALITY (Hetzner :8443) | ✅ done | NekoBox validated; deploy/xray/ rendered + running |
| 5E.B.1 | GitHub Actions workflow build-libxray.yml | ✅ done | `5cca2976` + `f5e21fb3` |
| 5E.B.2/3 | KMP module `:shared:core:xray` + libXray vendoring | ✅ done | `96fcbf1a` |
| 5E.B.4 | Wire XrayService into PhantomMessagingService | ✅ done | `98245f69` |
| 5E.B.5 | Production validation Tecno МТС без VPN | ✅ done | Test 14 / 2026-05-07 — text + voice 5 sec (chunks 55 KB) пролетают через REALITY |

**Backlog после Stage 5 (cleanup, не блокеры для Alpha-2):**

- ✅ **Prekey republish fix** — merged `1fc454cc` (#53, 2026-05-07). Client-side: verify and republish bundle on every WS reconnect. Closes the long-offline user → bundle 404 path.
- ✅ **Restore strict Xray routing** — done 2026-05-08 in PR #45 (commit `d7ba3a41`). Three-rule chain (multi-syntax domain match + port-443 fallback + catchall blackhole). Verified end-to-end via Caddy `remote_ip: 172.18.0.7`.
- ✅ **ADR-019 Xray REALITY rationale** — done 2026-05-08 in PR #48 (commit `20e71fbb`). 371-line ADR covering five sub-rationale subsections + threat model + known limitations.
- ✅ **PR в master** для Stage 5 — done 2026-05-08 morning as #43 (squash merge).

**Production endpoint** (запекён в `OperatorXrayConfig.kt`): `65.108.154.152:8443`, SNI `www.microsoft.com`, capability UUID `09c6fd0e-…`. Single shared UUID — capability-style auth, не identity. Rotation playbook в `deploy/xray/render-config.sh` header.

---

## Track D — Alpha 2 Features

После Track A + Track B items 1–4:

| Feature | ADR | Оценка | Статус |
|---------|-----|--------|--------|
| Voice notes без хака | — | покрыто PR 3 (Track A) | 🟡 |
| Attachments (photos/files via encrypted MinIO) | ADR-013 (draft в `ARCHITECTURAL_DECISIONS_TODO.md`) | ~2 недели | ⬜ |
| Stable groups с правильным crypto | требует Track B 5/6/7 | ~2 недели | ⬜ |
| Public channels (read-only broadcast) | новый ADR | ~1 неделя | ⬜ |
| Hosted username directory | ADR-007 (draft) | ~1 неделя | ⬜ |
| Pluggable transports (obfs4) | ADR-015 (draft) | ~3 недели | ⬜ |
| iOS port (Phase-1 milestone) | ADR-014 (draft) | 3–4 месяца | ⬜ |

---

## Реалистичный таймлайн

> **⚠️ SUPERSEDED — strategic pivot 2026-05-14.**
> Every date in this section is formally dropped. The 4-week Track A → Track B → Alpha-2 release → external-funding window plan was built around fixed dates that no longer exist. Vladislav's pivot reframes the project as Quality > Speed, Honest > Polished, with no tag chasing and no calendar pressure. The current authoritative tracker is the **Status block** at the top of this file plus the **Session journal** in `docs/PROJECT_LOG.md`.
>
> Items struck through below WERE delivered (Tracks A/B/C/E completed on master through May 2026), even though the calendar they were delivered against is no longer the calendar we're operating on. Items NOT struck through and NOT marked "(superseded)" never started in calendar form — they continue as Open follow-ups in `docs/PROJECT_LOG.md` without a date.

### ~~Неделя 1 (May 3–9) — Reliability + Release Polish~~ — completed; calendar superseded

- [x] ~~**Сегодня (3 May):** ручной тест PR 3 на Tecno ↔ emu → merge если ОК~~ — PR 3 voice chunking merged `41b9fb94`
- [x] ~~**May 4–5 (пн-вт):** Track C Phase 1 + Phase 2 (репо cleanup + funding plumbing)~~ — Track C Phases 1–4 all merged
- [x] ~~**May 6–8:** PR 1 data integrity~~ — PR #65 merged `df127bc1`
- [x] ~~**May 9 (выходные):** Track C Phase 3 + Phase 4~~ — done in #50 + #52

### ~~Неделя 2 (May 10–16) — PR 4 + Security старт~~ — completed; calendar superseded

- [x] ~~**May 10–13:** PR 4 storage durability~~ — PR #67 merged `367a3d61`
- [x] ~~**May 14–16:** PR 5 UX cleanup + старт Track B F22~~ — #68 merged + F22 wrap shipped via #56 `d862f3d0`

### ~~Неделя 3 (May 17–23) — Security gate перед Alpha-2 release~~ — security work completed; release tag dropped

- [x] ~~**May 17–19:** F22 завершить~~ — PR #56 `d862f3d0` (now codified as ADR-023 Accepted)
- [x] ~~**May 20–22:** F8 RatchetState wrap~~ — PR #60 `cc48a333` (ADR-024)
- [x] ~~**May 23:** старт F19+F20 call signalling E2EE~~ — F19+F20 shipped via #62 + ADR-025

### ~~Неделя 4 (May 24–31) — финальный спринт к Alpha-2 release~~ — superseded by strategic pivot

- [x] ~~**May 24–27:** F19+F20 + F2+F13~~ — all delivered earlier as part of Track B closure (#62, #63, #69, #70, #71, #72)
- [ ] ~~**May 28–29:** Tag `v0.1.0-alpha.2`, Alpha-2 snapshot prep~~ — **superseded**: tags are not goals per the 2026-05-14 pivot; the technical content that would have shipped under that tag is on master already, but no formal tag has been cut and none is planned.
- [ ] ~~**May 30–31:** Финальное ревью release draft V2, proofread, чек-лист~~ — **superseded**: no release event.
- [ ] ~~**June 1, 12:00 CEST:** Release Alpha-2 + первое окно external funding submissions~~ — **superseded**: no scheduled release event. External funding was decoupled from a calendar event in the same pivot (OTF #22145 + FUTO were submitted 2026-05-01 ahead of the original plan; NLnet was retired in the 2026-05-12 grants pivot; EU routes blocked by lack of EU collaborator; DRL deprioritised as Russian-citizen path). See agent memory `project_active_grants_2026_05_14.md` for the live grant status.

### ~~Июнь — продолжение Security + дополнительные funding submissions~~ — superseded by strategic pivot

- [ ] ~~**June 1–7:** мигание после Alpha-2 release, продолжение Track B (F1, F3, F4)~~ — F1/F3/F4 already shipped earlier (#69, #70, #71).
- [ ] ~~**June 8–14:** реализация attachments (Track D первая фича) ИЛИ groups hardening~~ — **superseded**: not on the current queue; the current queue is the doc-honesty pass, REC follow-ups, notifications diagnostic, PR-D1e bootstrap, calls track. See `docs/PROJECT_LOG.md → Open follow-ups`.
- [ ] ~~**June 15–22:** Track B F11+F26 (relay token), Track B P2 batch~~ — F11+F26 shipped earlier via #72; P2 batch is intentionally deprioritised under the pivot.
- [ ] ~~**June 23–29:** Submit secondary external funding (window end of June)~~ — **superseded by 2026-05-12 grants pivot** (NLnet снята; calendar-driven funding cadence retired).

### ~~Июль — Сентябрь (Alpha 2 → Beta)~~ — superseded by strategic pivot

All bullets below are **superseded** as calendar items. The underlying intent (attachments, groups hardening, public channels, username directory, pluggable transports, iOS port) is retained as a research backlog in `docs/project/PHANTOM_ROADMAP_2026.md`, but is not on any active sprint. The current priority is the locked queue (doc honesty → REC follow-ups → notifications diag → PR-D1e → network matrix → calls → voice A/B) tracked in `docs/PROJECT_LOG.md → Open follow-ups` + the next-session lock at `docs/tracks/doc-honesty.md`.

- [ ] ~~Attachments полностью~~
- [ ] ~~Groups hardening (F1, F3, F4 закрыты)~~ — F1/F3/F4 already merged
- [ ] ~~Public channels~~
- [ ] ~~Username directory~~
- [ ] ~~Closing remaining Track B P2 items~~
- [ ] ~~Pluggable transports (obfs4) старт~~ — partial: pluggable transports moved into the Track D current scope via Stage 5E (Reality / Xray), and Tor with Briar's bridge import (PR-E). obfs4 in the original-vehicle sense remains a research backlog item.
- [ ] ~~iOS port старт (Phase-1 milestone if external funding lands)~~ — **superseded**: iOS deferred to Beta per `feedback_strategy_decisions_2026_05_14.md`.

### ~~Октябрь — Декабрь (Beta)~~ — superseded by strategic pivot

All bullets below remain as research-backlog items in `docs/project/PHANTOM_ROADMAP_2026.md`. They are not on any active calendar.

- [ ] ~~Calls 1:1 voice/video — вернуться к PR 2.6 когда транспорт стабилен~~ — replaced by the C-track plan (C1/C2/C3) in `docs/PROJECT_LOG.md → Open follow-ups`.
- [ ] ~~Desktop client (Compose JVM)~~
- [ ] ~~BLE / Wi-Fi Direct local transport~~
- [ ] ~~Multi-device identity (linked-device trust)~~

### ~~Q1 2027 (v1.0)~~ — superseded by strategic pivot

All bullets below are now research-backlog only. No v1.0 calendar exists.

- [ ] ~~iOS финал~~
- [ ] ~~Self-hosted relay kit для businesses~~
- [ ] ~~Third-party аудит (Cure53 / Trail of Bits)~~ — deferred until an audit can be funded without grant-calendar pressure.
- [ ] ~~F-Droid / Google Play / App Store launch~~
- [ ] ~~UnifiedPush integration (Phase 5)~~ — **explicitly retired** in the 2026-05-14 pivot; UnifiedPush is no longer on the roadmap (metadata-leak posture per ADR-001 + Threat Model v0). See ISSUE-001 long-term-direction note in `KNOWN_ISSUES.md`.

---

## Зависимости

```
PR 3 (voice) ──┬─► PR 1 ──► PR 4 ──► PR 5 ──► Track A done
               │
Track C ──────┘  (параллельно, любой день)

PR 5 done ──► Track B F22 ──► F8 ──► F19+F20 ──► F2+F13 ──► Alpha-2 snapshot
                                                              │
                                                              ▼
                                                          tag v0.1.0-alpha.2
                                                              │
                                                              ▼
                                                          Alpha-2 release (1 June) + external funding window opens

Track B 5–8 → продолжение в июне → Kickstarter clearance

Track D (Alpha 2 features) → начинается после Track B 1–4 минимум
```

---

## Риски и mitigation

> **⚠️ Original Alpha-2-release risks superseded by 2026-05-14 strategic pivot.** The four risks below were framed against a fixed Alpha-2 release calendar that no longer exists. Risk 3 in particular was the load-bearing argument for the UnifiedPush plan that has since been retired. Read the **Status block** at the top of this file for current architectural risks; the originals are preserved for archival reasons.

1. ~~**24 дня до Alpha-2 release window — реалистично только если security work идёт без задержек.** Mitigation: даже если успеем закрыть только F22 + F8, это уже обоснованный snapshot. Release draft V2 уже признаёт что F19+F20 в работе.~~ — superseded by pivot. Track B closed in full earlier in May; no release window remains as a deadline.
2. ~~**Track B сложнее чем оценено** — security work часто всплывает с edge cases. Mitigation: каждый item — отдельный PR, не бандлим.~~ — confirmed but no longer schedule-relevant (Track B already closed).
3. ~~**Voice/calls остаются нестабильными до Phase 5 (UnifiedPush, Feb 2027)** — это known limitation, calls=experimental в Alpha. Не блокирует Alpha-2 release.~~ — **explicitly retired by pivot**: UnifiedPush is permanently off the roadmap. Voice messages are now production-quality on the M1w encrypted media-upload path; calls remain unproven on RU mobile (C-track in `docs/PROJECT_LOG.md → Open follow-ups`).
4. ~~**Solo founder bottleneck** — если Vladislav заболеет/занят неделю, release window под угрозой. Mitigation: Phase 4 release polish даёт максимальный снимок репо, выпуск может идти даже без полного Track B closure.~~ — confirmed as a permanent risk (still solo development) but no longer release-window-relevant.

**Current architectural risks (2026-05-21):** see the top-of-file Status block. Headline items: Tele2 LTE WS Frame.Text drop (mitigated by REST fallback, see ISSUE-018); single-relay Tele2 media ceiling (route diversity via PR-INFRA-MediaRO is the long-term answer, see ISSUE-020); receiver-side media cancel unsupported (blocks M2e re-enable, see ISSUE-023); calls on RU LTE unproven (C-track queued).

---

## External funding

The canonical funding-channel list lives in [](../../funding.json) at the repository root. That file is the source of truth for what funding programmes are pursued and on what schedule.


---

## Что мы делаем СЕЙЧАС (живой указатель)

> Обновляется по мере прогресса. Когда задача меняется — менять этот блок и резерв-помечать в треках выше.

**Current focus (2026-06-17):** ⚡ Reliability backbone functionally complete + **voice transport-independent contract empirically confirmed on Tele2 LTE**. Voice notes delivered bidirectionally (Tecno ↔ Emu, four voice notes, 9-15 chunks each) DESPITE active Direct WSS thrash (Tecno-side `ws_ping_timeout_diag=32`, emu-side 14 mode switches). ADR-028 §"Layer 3" + multi-transport framing 2026-06-09 ("voice MUST NOT depend on live WS") verified field-side. PR #323 T2 fix + Sprint 2b-C OPK lifecycle held during the voice smoke. Stage 2B-D Tele2 LTE integration smoke PASS + Round 14 paced padded poll LIVE on production relay (PR #310 squash `345d9761`, VPS deploy ~14:59 UTC) + `LONGPOLL_V2_ENABLED` release pin flipped `"0"` → `"1"` (PR #320 squash `a213b3ab`). Sprint 2b OPK lifecycle closed end-to-end (Sprint 2b-A `30617121` + 2b-B `b40c2908` + 2b-C `7e421728`). **T2 carrier-ceiling prekey-publish bug CLOSED** (PR #323 `5cf18e5f`).

**Next-track direction locked 2026-06-17 (direction only, NOT scope-lock):** **DWS-UX hardening** — explicitly NOT "make Direct WSS reliable like long-poll". The intent is "Direct is fast when it can be, long-poll is reliable when Direct dies — make degradation fast, quiet, and understandable". Seven open questions: idealise connection / UI Recovering mismatch / faster honest WS → REST switch / battery + log drain from repeated `ws_ping_timeout` / show vs hide "Direct degraded" UI / WS reconnect loop ceiling / first-message yellow dot 8s (PR-G3/G4-era artifact returning). Pre-conditions: production observation window ≥ 2026-06-23 + ADR-011 docs honesty pass. Direction-pointer captured in agent memory; code work does NOT begin without explicit greenlight. Other tracks queued: (3) Calls TURN + signaling fallback (`RC-CALLS-TURN1`, requires TURN procurement decision first per Ruflo Layer 2 verdict + 2026-06-16 deep audit), (4) Reality libXray Arm G (parked), (5) Tor / Ghost parity, (6) Attachments (zero LOC).

**Activity on 2026-06-15 → 2026-06-17:** Sprint 2a (PR #311 `df34222d`) + Sprint 2b-A/B/C (#315 / #316 / #317) + M-OPK-3 docs amendment (#318) + Round 14 (#310) + Stage 2B-D rollout flag flip (#320) + T2 instrumentation (#321 / #322) + T2 fix (#323) + T2 close-point docs (#324) + Direct WSS recon Layer 1 + Layer 2 adversarial verdict (2026-06-16) + Direct + media truth audit four-agent fan-out + independent adversarial verdict (2026-06-16) + voice smoke PASS on Tele2 LTE (2026-06-17).

**Activity on 2026-06-09 → 2026-06-10:**

- Trek 1 Stage 1 / RC-LIBXRAY-REALITY-WIRE1 V3 xhttp matrix CLOSED FAIL (commit `df082528`, 2026-06-09). App-level matrix exhausted; libXray Android gomobile incomplete-write of the Reality ClientHello localised at wire level. Reality demoted to diagnostic surface; sub-track scoped but not started.
- Trek 2 Stage 1.x scope-doc trio PR #300 / #301 / #302 (2026-06-10).
- Trek 2 Stage 1.x server prerequisite SHIPPED PR #303 (2026-06-10, master `e3794e6e`). 162 relay tests pass; `cargo clippy -D warnings` clean. Operator must provision `RELAY_SEQ_MAC_KEY` in VPS `.env` before redeploying.
- Trek 2 Stage 2B-A scope merged PR #304 (2026-06-10, master `9c02a3c8`). Seven client locks L1-L7 + 7-test matrix + 3-commit boundary locked.

**Next:** Stage 2B-A implementation PR. Fresh branch `feat/pr-trek2-stage2b-a-client-shell` from current master. Re-verify the 7 server-contract points (pinned at scope time against `e3794e6e`) against current master before starting; pause if drift. Implementation strictly inside L1-L7. Tele2 LTE smoke is NOT required for Stage 2B-A (shell-only, release pin `"0"`, M4 contract test guards it); the smoke gate transfers to Stage 2B-B which DOES exercise the runtime path.

**Previous (2026-05-09 archived):** **ВСЯ Track A + ВСЯ Track B (1–8) закрыты.** Repository security gate для Alpha-2 + Kickstarter обоих пройден. Council-revised план задумывал submit на день 15 с одним security item (F22) — фактически закрыли 8 items за два рабочих дня.

**Что закрыто за сессии 2026-05-08 → 2026-05-09:**
- Track A: PR #65 data-integrity (F-08/F-01/F-09/F-04), #67 storage durability (F-02/F-12/F-06/F-13), #68 UX cleanup (F-14/F-21/F-24 + 3 QA fixes)
- Track B: #56–#58 (F22 + tests + roadmap), #59 (C-2 read receipts), #60 (F8 RatchetState wrap), #62 (F19+F20 calls E2EE + ADR-025), #63 (F2+F13 + ADR-017 Accepted), #69 (F3 HKDF), #70 (F1 group control + ADR-026), #71 (F4 leave rotation), #72 (F11+F26 + ADR-027)
- Production validation 2026-05-09: signed-challenge auth работает на Tecno МТС без VPN через Xray; text + voice + call signaling round-trip OK на двух устройствах

**Что НЕ кодовая работа, ручные действия Vladislav:**

> Most of the items below are **superseded** by the 2026-05-14 strategic pivot (no fixed release tags) and the 2026-05-12 grants pivot (NLnet снята; OTF + FUTO already submitted on 2026-05-01). Preserved here for archival reasons; the current Vladislav-side TODO list is **not** this list — see agent memory `project_active_grants_2026_05_14.md` for live funding state.

- Demo video Stage 5E (council plan Day 3 — ещё не снято) — still relevant, not on a calendar.
- Donation rails: Open Collective / Liberapay / Polar / BMAC — still relevant, not on a calendar.
- ~~FUTO Microgrants application~~ — submitted 2026-05-01.
- Public write-up HN + Хабр — still relevant, not on a calendar.
- ~~NLnet draft V2 финализация + submission через portal~~ — **superseded by 2026-05-12 grants pivot** (NLnet снята).
- ~~FLOSS/fund submission~~ — superseded; OTF #22145 was the funding action and is already submitted (2026-05-01).
- ~~Tag `v0.1.0-alpha.2`~~ — **superseded by 2026-05-14 strategic pivot** (tags are not goals; no release event planned).

**QA bugs reported 2026-05-09 (parked, не блокеры):**
1. **Mic one-way audio при звонке** — phone↔emu testing показал что одна сторона не слышит другую. Совпадает с известным deferred PR 2.6 ("Calls audio plumbing — JavaAudioDeviceModule + AudioFocus + suppress reconnect"). Severity: medium (calls = experimental в Alpha).
2. **После hangup возврат на ChatList, не на ChatScreen** — нав баг, ~5 строк. Severity: low.
3. **Реакции не доходят до собеседника** — send-path или receive-path сломан для TYPE_REACTION. Нужна диагностика. Severity: medium.
4. **Нет "Удалить чат" в long-press menu на ChatList** — фича отсутствует, нужна с подменю "Удалить у себя / Удалить у обоих" (аналог `deleteMessageForBoth`). Severity: low.
5. **Дизайн полировка + значки не соответствуют референсам** — отдельный sprint, ui-prototyper agent + Vladislav как arbiter. Severity: low (но visible to NLnet reviewers).

Полный 25-day plan в `~/.claude/projects/.../memory/plan_25_days_to_release.md` (локально, не в репо).

**Активные ветки:** все merged. Master в clean state.

**Известные follow-ups (deferred):**

> The "Alpha-2 blockers" / "post-NLnet roadmap" framing below was superseded by the 2026-05-14 strategic pivot + the 2026-05-12 grants pivot. The bullets remain useful as research-backlog items but are no longer ordered against any release / grant calendar.

- Track B P2 batch (F6, F7, F9, F10, F12 retry, F14, F18, F23, F25) — intentionally deprioritised under the strategic pivot (Beta-tier polish).
- ~~Track D Alpha-2 features (attachments, stable groups, channels, username dir, iOS port) — post-NLnet roadmap~~ — research backlog only; iOS deferred to Beta per `feedback_strategy_decisions_2026_05_14.md`.
- PR 2.6 calls audio plumbing — folded into the **C-track** in `docs/PROJECT_LOG.md → Open follow-ups` (C1 capabilities / C2 Reality endpoint pool / C3 TURN-TLS or Opus-over-Reality).
- Firebase Console SHA-1 fingerprints — **moot**: FCM removed entirely per the 2026-05-14 pivot.
- ADR-020/021/022 (adaptive transport, multi-server Xray, iOS XCFramework) — research backlog; ADR-020 partially in production through current adaptive-transport orchestrator, ADR-021 still Reserved, ADR-022 still draft.

---

## Связанные документы

- [`docs/project/EXECUTION_PLAN_2026_05_01.md`](EXECUTION_PLAN_2026_05_01.md) — детали Track A + Track B
- [`docs/project/PHANTOM_ROADMAP_2026.md`](PHANTOM_ROADMAP_2026.md) — внутренний 12-месячный execution plan
- [`docs/project/ARCHITECTURAL_DECISIONS_TODO.md`](ARCHITECTURAL_DECISIONS_TODO.md) — 10 pending ADR драфтов
- [`KNOWN_ISSUES.md`](../../KNOWN_ISSUES.md) — публичный список багов и архитектурных choices
- [`ROADMAP.md`](../../ROADMAP.md) — публичный высокоуровневый roadmap
- Локально (untracked): release draft V2, Привлечение инвесторов PDF
